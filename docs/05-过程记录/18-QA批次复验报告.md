# 18 · QA 批次复验报告（卡 `QA-BATCH-1`）

> 批次：A（BUG-008/009 Spike 证据修正）· B（DOC-3 代码项）· C（P1-8 日志落盘 L2/L3）
> 版本三连：`0.1.0` (1) @`5f3cf3d`（main，`git merge --ff-only main` → already up to date）
> 复验人：QA（Angela · `worker-qa-5`）｜ 日期：2026-09-22 ｜ 签字人：god（待签）
> 方法：**不采信作者自述**——A 用独立脚本从 artifact 复算、B 用脚本数 + 反证编译失败、C 用 QA 自己出的 release 包装机复跑。
> 边界：不改功能代码（反证改动逐条撤回、`git status` 快照逐字节一致）；不 commit / push；不改 `docs/01–04`。
> 证据：`hive/agents/worker-qa-5/evidence/BATCH1/`（**不入公开仓库**），下文只写文件名。

---

## 1. 结论摘要

| 段 | 待验项 | 作者 | QA 判定 |
|---|---|---|---|
| **A** | BUG-008 / BUG-009（Spike 报告证据修正） | `worker-dev-b-3` | ✅ **通过**；BUG-008 关闭，BUG-009 半数**误报**（见 §3） |
| **B** | DOC-3 代码项（`EventCodes` 45→46） | `worker-dev-a-8` | ✅ **通过**（含一条反证编译失败） |
| **C** | P1-8 日志落盘与轮转（L2/L3） | `worker-dev-a-9` | ✅ **L2/L3 功能全部复现**；3 条口径/范围问题登记为新缺陷（不阻塞功能判定） |

**整体判定：三件全部通过复验**，无 S1/S2 未关闭缺陷。新登记 3 条 S4/S5 口径类缺陷（§5），均有明确归属与改动位置。

---

## 2. 复验环境

| 项 | 值 |
|---|---|
| 基线 | `5f3cf3d`（= `main`，含 P1-6/P1-8 与 DOC-3） |
| 独立出包 | `./gradlew check assembleRelease` → **BUILD SUCCESSFUL in 3m 33s**（1340 tasks）；APK **7,087,111 B / SHA256 `8c3bc2e0…`**；`apksigner` 证书 `a98e8d4b…`（与前几轮同一张） |
| 单测 | 全仓 **395 条（debug/release 变体合计）/ 0 失败 / 2 跳过**；`core:log` 76、`core:source` 108、`core:player` 88、`core:domain` 71、`core:data` 36、`feature:channels` 10、`core:common` 4、`app` 2 |
| lint | **0 error / 46 warning**（`app` 33、`core:log` 8、`feature:channels` 4、`core:player` 1） |
| 设备 | Sony BRAVIA 4K VH21 · Android 12 / API 31 · armeabi-v7a · `<TV-LAN-IP>:5555` |
| 工具 | JBR `/Applications/Android Studio.app/Contents/jbr/Contents/Home`；`ANDROID_HOME=~/Library/Android/sdk`；签名口令取自共享树 `local.properties`（env 传入，不落盘、不回显） |

---

## 3. A 段 · BUG-008 / BUG-009 复验（作者 `worker-dev-b-3`）

### 3.1 百分位抽算（S1 p50 / p90 / p95）

QA **不用作者的脚本**，直接读 artifact 自己算（nearest-rank = 排序后第 `ceil(p/100×N)` 个，1 基，无插值）：

| 项 | 报告 §0/§2 写的 | QA 复算（`s1_s1a.json`，n=20，errors=0） | 结论 |
|---|---|---|---|
| S1 p50 | 1112 ms | **1112 ms** | ✅ |
| S1 p90 | 5035 ms | **5035 ms** | ✅ |
| S1 p95 | 5244 ms | **5244 ms** | ✅ |
| S1 平均 / max | 2138.8 / 6215 ms | 2138.8 / 6215 ms | ✅ |

顺手复算 S5（`s5_s5reuse.json` n=30 / `s5_s5recreate.json` n=10）：p50 **513** / **389**、p95 **3313** / **3764**，与报告 §5 表一致。

**算法写死在报告方法节** ✅：`07-Spike报告.md` §1.2 明确「百分位：全报告统一用 nearest-rank…第 `ceil(p/100 × N)` 个（1 基）样本值；不做线性插值」，并给出与线性插值的差异示例。

> **台账勘误**：BUG-008 证据列原文写「S5 p95=3313 与 artifacts 按 nearest-rank 计算（5035 / 5244 / **3282**）不符」——`3282` 是 `floor(p/100×N)` 的取法；按报告定义（`ceil`）S5 p95 = **3313**，即报告值本身就对。已在台账该格更正（§5 无新缺陷，属 QA 自己的口径笔误）。

### 3.2 S6 冷启动：5 次是否都有 artifact、数字是否与文件一致

`evidence/colds6/run-1..5.txt`（每次一段，含 `am start -W` 输出与夹具日志）+ `artifacts/s1_colds6-1..5.json` 全部存在，逐条比对：

| 次 | 首页首帧 | 视频首帧 | TotalTime / WaitTime | artifact 内的 `firstFrameSinceProcessStartMs` |
|---|---|---|---|---|
| 1 | 316 ms | 1500 ms | 898 / 909 | 1500 ✅ |
| 2 | 291 ms | 1511 ms | 866 / 878 | 1511 ✅ |
| 3 | 301 ms | 1588 ms | 872 / 888 | 1588 ✅ |
| 4 | 299 ms | 1487 ms | 879 / 894 | 1487 ✅ |
| 5 | 308 ms | 1545 ms | 879 / 886 | 1545 ✅ |

报告 §6 写的「首页 291–316 ms、视频 1487–1588 ms、TotalTime 866–898 ms」与上表**逐条一致** ✅；5 次都是真冷启动（`am force-stop` 后进程号每次不同）。旧表那几行（280–307 / 808–849）在报告里已明确作废，不再引用 ✅。

### 3.3 作者对 BUG-009「S2 到 READY 528–682 原本就有 artifact」的反驳

**反驳成立** ✅：

| artifact | 是否有 `s2` 判定块 | `entries[].s2.readyAfterMs` |
|---|---|---|
| `s2_s2aac2.json`（现行） | **有** | 682 / 591 / **528** ← 报告 §3.3 的「528–682 ms」正是这三个值 |
| `s2_s2aac_sw.json`（22:25 首次试跑，已被取代） | 无（只有 `costMs` 384/679/493） | — |

即：QA 上一轮（`worker-qa-4`）看的是**已被取代的**首次试跑文件，字段其实一直存在。报告 §3.3 现在也给每行标了 artifact 文件名，可追溯 ✅。因此 BUG-009 的 S2 一半是**误报**，S6 一半是**真缺陷且已修复** → 台账结论改为「**部分误报**」并关闭。

**A 段判定：✅ 通过。**

---

## 4. B 段 · DOC-3 代码项复验（作者 `worker-dev-a-8`）

### 4.1 独立核对 `EventCodes.ALL` 与 `docs/03 §3.3`（脚本，非肉眼）

脚本 `evidence/BATCH1/check-eventcodes.py`（QA 自写，从源码正则抽取 + 展开文档表格的 `/` 与 `_FAIL` 简写）：

```
[kt]  const val declarations : 46  (unique 46)
[kt]  ALL set entries       : 46  (unique 46)
[kt]  duplicate values      : none
[kt]  declared but not in ALL: []
[kt]  in ALL but undeclared : []
[doc] docs/03 §3.3 codes     : 46
[cmp] doc - kt: []   [cmp] kt - doc: []
VERDICT: PASS (46 == 46, 1:1, no dups)
```

输出存 `eventcodes-check.txt`。三边（`const val` / `ALL` / 文档表）**都是 46、无重复、一一对应** ✅。

### 4.2 反证：引用不存在的事件码必须编译失败

| 步骤 | 命令 | 结果 |
|---|---|---|
| 反证 | `PlaylistParsers.kt:24` 改 `EventCodes.SRC_PARSE_FAIL` → `SRC_PARSE_FAILL`，跑 `:core:source:compileDebugKotlin` | **FAILED**：`e: …PlaylistParsers.kt:24:47 Unresolved reference 'SRC_PARSE_FAILL'` → `BUILD FAILED in 1m 8s` |
| 撤回 | 改回原行 | `git status` 与实验前**逐字节一致**（`git-status-before.txt` vs `git-status-after-revert.txt`，`diff` 无差异 → `SNAPSHOT IDENTICAL`） |

即：事件码是**编译期**约束，不是靠自觉；业务代码里写错码根本出不了包 ✅。

### 4.3 `./gradlew check` 绿

`./gradlew --no-daemon check assembleRelease` → **BUILD SUCCESSFUL in 3m 33s**；`check` 覆盖单测 395 条 0 失败、lint 0 error ✅。（同一轮顺带产出 C 段要用的 release 包。）

**B 段判定：✅ 通过。**

---

## 5. C 段 · P1-8 日志落盘复验（作者 `worker-dev-a-9`）

装机的是 **QA 自己出的** release 包（`8c3bc2e0…`，证书 `a98e8d4b…`）。每个关键步骤前后都核对了设备上 `pm path` 指向的 `base.apk` 哈希 = 我的构建，避免"验到别人的包"（本轮真踩到，见 §5.7）。

### 5.1 落盘 + `adb pull` + JSONL 可解析 + 字段与 §4 一致

| 检查 | 结果 |
|---|---|
| 落盘 | 冷启动后 `…/files/logs/iptv-20260922.log` 出现并追加 `APP_START`（293 B → 879 B，多次冷启动逐条追加） |
| `adb pull` | `12 files pulled, 0 skipped`（`pull-run1/`、`pull-final/`） |
| JSONL 可解析 | 逐行 `json.loads`：**20,020 / 20,020 行 0 解析失败**（含脚本灌入的 2 万行） |
| 字段与 §4 一致 | 行内键 = `seq, ts, level, category, code, message, fields, sessionId` **8 键齐备**，外加 §4 之外追加的 `elapsedMs, thread`（作者在 13 报告 §2 已声明）；`level` 取值 INFO/WARN/DEBUG 与 docs/03 §3.2 一致 |

样例（设备原文）：`{"seq":1,"ts":…,"level":"INFO","category":"APP","code":"APP_START","message":"app started","fields":{"version":"0.1.0 (1)","device":"Sony BRAVIA 4K VH21 / Android 12 (API 31) / ABI armeabi-v7a,armeabi","maxMemoryMb":192},"sessionId":"app-38f7","elapsedMs":4355719,"thread":"main"}`

### 5.2 反证降级：写失败不崩、不空转、有退避，还原后自愈

做法：`mv …/files/logs …/files/logs-bak && echo placeholder > …/files/logs`（用同名**文件**占位，目录建不出来）→ 冷启动 → 开设备页。

| 检查 | 实测 |
|---|---|
| 进程 | `pidof ilab.iptv.player` = 7173（存活）；`logcat` 中 `FATAL`/`ANR in ilab` 计数 **0** |
| 设备页显示 | `文件日志异常：cannot create /storage/emulated/0/Android/data/ilab.iptv.player/files/logs（53944 ms 后重试）`；`文件计数：写入 0 行 / 轮转 0 / 删除 0 / 写失败 1 / flush 失败 0 / 降级跳过 1` |
| 不空转 | 12 s 后再截图：**计数完全不变**（写失败 1 / 降级跳过 1），只有重试倒计时 53944 → 33842 ms 在走 |
| 内存环不受影响 | 同一页内存环照常显示 `APP_START` —— 文件出口降级不拖累日志主干 |
| 还原后自愈 | `mv …/logs <-> …/logs-bak` 还原目录 → 冷启动 → 基础文件 **879 B** 且末行是新一次 `APP_START`（sessionId `app-41dc`） |

证据：`c6-degraded.png`、`c7-degraded-12s-later.png`。

### 5.3 轮转（按大小）

用脚本把基础文件灌到 **3,244,183 B（> 2 MB 阈值）**，再冷启动：

```
-rw iptv-20260922.2.log 3244183   ← 旧内容整段改名为段文件（段号 = 当天最大 + 1，不覆盖）
-rw iptv-20260922.log       293   ← 基础文件重开，触发轮转的那条事件落在新文件里
```

证据：`c-rotation.txt`。**「最新数据始终在基础文件」成立** ✅。

### 5.4 清理边界（超大 / 过期 / 超份数 + 不误删当天数据）

造边界：目录里放 1 份 21 天前的 `iptv-20260901.log` + 10 份当天段文件，合计 **24 份** → 冷启动后：

| 检查 | 实测 |
|---|---|
| 份数上限 | 24 份 → **12 份**（上限 12 生效） |
| 过期件 | `iptv-20260901.log`（7 天窗口外）**已删除**（`ls` 报 No such file） |
| 当天活跃文件 | `iptv-20260922.log` **保留且继续追加**（586 B，末行是新 `APP_START`） |
| 删除顺序 | 先删最旧（`20260921.*` 与更早的当天段文件），活跃文件受保护 |

证据：`c-cleanup.txt`。

### 5.5 脱敏抽查（按 docs/03 §11）

**先说事实**：本轮设备日志里**没有 URL 行**——P1-2 的频道表走的是本地夹具，没有网络抓取，所以"从设备日志抽 3 行看 URL"这一步**在本轮数据上无法成立**（不是通过，是没数据）。改做**等价手段**：写一个临时 JVM 探针（真实 `LogBus` + 真实 `FileSink` + 真文件系统，跑完即删、`git status` 复原为干净），让一条事件带 URL 落盘，看**文件里的字节**：

```
{"seq":1,…,"message":"fetched http://192.168.10.10:8080/iptv/live/secret-channel.m3u8?token=***&user=qa",
 "fields":{"url":"https://cdn.example.com/a/b/c/live.m3u8?auth=***&key=***",
           "userinfo":"rtsp://***@10.0.0.9:554/live/x"},…}
```

| §11 规则 | 实测 |
|---|---|
| 查询参数 `token/key/pwd/pass/sign/auth/secret/expire` → `***` | ✅ 全部掩掉（大小写不敏感、URL 与纯文本都能命中） |
| 用户信息 `scheme://user:pass@host` | ✅ → `scheme://***@host` |
| **URL 路径 → `scheme://host` + 8 位 hash** | ❌ **未实现**（`/iptv/live/secret-channel.m3u8`、`/a/b/c/live.m3u8` 原样落盘） |
| **设备标识（含精确内网 IP）→ `<redacted>`** | ❌ **未实现**（`192.168.10.10` 原样落盘） |

证据：`redaction-jvm-probe.txt`。代码里 `UrlRedactor` 的 KDoc 已写明「其余 §11 规则随导出包（L8/P2）落地」，与 docs/03 §13 把脱敏完备放到 L8 一致 —— 但 13 报告 §2 的「脱敏 … 出处：§11」写成了已达标，属口径夸大，登记为 **BUG-010**。

### 5.6 其余检查

| 检查 | 实测 |
|---|---|
| 开关 | 设备页「文件日志：开/关」可切（默认开）；关闭时磁盘无新增（作者单测亦覆盖，本轮未重复造） |
| 动态调级 | 设备页切 DEBUG 后 `UI_SCREEN_OPEN`（DEBUG 级）进入文件与内存环，切回 INFO 后不再落 —— 与 docs/03 §3.1/§14 一致 |
| lint | `check` 绿（0 error）；但 `core:log` 自身有 **8 条 warning**（`SdCardPath` 1 + `ButtonStyle` 6 + `Overdraw` 1），全部来自 P1-8 新增文件 → 作者「32 warning 无一条来自新代码」的说法不准确（见 BUG-012） |

### 5.7 环境风险（本轮真实踩到，非代码缺陷）

复验过程中设备上的 `ilab.iptv.player` **被另一个 temp（`worker-dev-b-4`）重新安装**（`dumpsys package` 的 `lastUpdateTime` 前移、`PACKAGE_REPLACED` 广播、`pm path` 拉回的 base.apk 哈希 ≠ 我的构建）。那一窗口截到的设备页（`c3-console-rotation-fail.png` / `c4-console-2.png`）其实是**对方的包**：其 `LogConsoleActivity.deviceReport()` 只输出到「计数」行，正好对应截图里缺掉的「文件…」块，能一一对上。

**处置**：QA 重新安装自己的包并用 `pm path` + 哈希确认后才采信数据（§5 全部结论都出自"哈希已核对为我的构建"的窗口）。**建议 god 立一条规矩：真机 QA 期间设备独占**（一次只允许一个 agent 装机/跑真机），否则 QA 证据随时会被别人的包污染。

**C 段判定：✅ L2/L3 功能全部通过复验**（落盘 / 轮转 / 清理 / pull / 降级自愈 / 调级）；随附 3 条口径类缺陷（BUG-010/011/012），不阻塞 P1-G，但应在文档批收口。

---

## 6. 新缺陷（已登记台账 §3）

| ID | 严重级 | 结论 | 位置 | 证据 |
|---|---|---|---|---|
| BUG-20260922-010 | S4 | `13-P1-8日志落盘验证.md` §2 把脱敏标为「§11」达标，实测只实现 §11 的 2/5 条规则：查询参数与 userinfo 生效，**URL 路径 hash 化、内网 IP 脱敏未实现**（代码 KDoc 声明留 L8/P2，docs/03 §13 亦然） | 13 报告 §2；`core/log/…/UrlRedactor.kt` | `redaction-jvm-probe.txt` |
| BUG-20260922-011 | S5 | 份数上限 **12** 是代码默认值，`docs/03 §6` 只写「2 MB / 7 天 / 50 MB」；且计数上限会**删除当天（非活跃）段文件**（本轮实测删掉当天 3.9 MB 段与当天大段文件）——需在文档写明或明确改成只按时间/总量清理 | `docs/03 §6`；`LogFilePolicy.DEFAULT_MAX_FILE_COUNT` | `c-cleanup.txt` |
| BUG-20260922-012 | S5 | 13 报告 §3 称「32 warning 中无一条来自本次新增代码」不准确：`core:log` 自身 8 条 warning（`SdCardPath`、`ButtonStyle`×6、`Overdraw`）全部来自 P1-8 新增文件 | 13 报告 §3 | `core/log/build/reports/lint-results-debug.xml`（本轮 `check` 产出） |

另：环境风险 1 条（§5.7，共享设备并发装机），建议 god 定为流程规则，不按代码缺陷统计。

---

## 7. 设备状态还原

| 项 | 状态 |
|---|---|
| `logs` 目录 | 已还原为目录（降级反证后 `mv` 还原），当前 **12 份 / 96 KB**，活跃文件正常追加 |
| 我造的临时件 | `files/big-moved.log`、`files/logs-placeholder-file`、`/data/local/tmp/filler.txt` 已删除 |
| 装机包 | 设备上 `base.apk` 哈希 = `8c3bc2e0…`（= QA 本轮构建），未被换回 |
| 代码 | `git status` 干净（反证与 JVM 探针均已撤回并通过快照比对） |

---

## 8. 未关闭项（交下一棒）

1. **BUG-010 / 011 / 012**：都是文档口径与取值留痕，建议并入下一批文档作业（`DOC-*`）；BUG-011 若要改代码（去份数上限或写进文档）需 god 裁决。
2. **BUG-001 / 002 / 004 / 006 / 007**（历轮遗留，转文档批）：本轮未动。
3. **真机 QA 独占**：请 god 定流程（§5.7）。
4. **P1-G 阶段**：本轮只复验三件；P1 出口还差 P1-2 频道列表 658 台 ≥50 fps 的真机长跑与 P1-6 故障转移的真机对照（分别由 `worker-dev-b-4` 等人在跑，不在本卡范围）。
