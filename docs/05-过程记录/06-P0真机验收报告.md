# 06 · P0 真机验收报告（P0-5 装机 + P0-G 部分验收）

> 阶段出口：P0-G（部分，S1–S6 与 P0-6/P0-7 未含）｜ 版本：0.1.0 / 1 / @3d3d33d（QA 自建 APK `b86e8ee1…`）
> 证据人：QA（Angela，独立于开发）｜ 签字人：**god（留空待签）** ｜ 日期：2026-09-21
> 真机：Sony BRAVIA 4K VH21，Android 12 / API 31 / armeabi-v7a，`192.168.x.186:5555`（已脱敏）
> 证据目录（仓库外）：`hive/agents/worker-qa-2/evidence/P0/`

---

## 1. 阶段出口条件（引自 docs/04 §2.3）

| # | 出口条件 | 判据 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | 可重复产出签名 APK 并安装到电视运行 | S4 / 真机启动不崩 | QA 在**自己的 worktree**（`3d3d33d`）独立出包并签名 ✅；`install -r` Success；冷启动 `TotalTime` 227/189/227/401 ms（4 次），进程存活，无 `FATAL EXCEPTION`；**但字节级 SHA256 不可复现**（见 BUG-004） | ⚠️ 通过（附条件） |
| 2 | S1–S6 有明确数据结论并据此更新 docs/02 §7/§8 | S1–S6 数据 | 未测：属 P0-3/Spike（dev-B）范围，本轮未进场 | ⬜ 未测 |
| 3 | 日志骨架可用（验收 L1） | L1：门面 + 4 Sink + 内存环可看 | `core/common` 有 `Logger`/`EventCodes` 门面类型；**`core/log` 目录下只有 `build.gradle.kts`，0 个源码文件**；无 Sink、无内存环、无从 App 内查看 | ❌ 不通过 |
| 4 | CI 绿色 | 首次绿色构建 | `.github/workflows/ci.yml` 存在（assembleDebug / testDebugUnitTest / lint + 纯 Kotlin grep 守护）；GitHub API `repos/cuijfboy/iptv-player/actions/runs` 返回 `total_count = 0` | ❌ 无证据（从未运行） |

## 2. 实测数值（原始数据）

| 指标 | 目标 | 实测 | 证据 |
|---|---|---|---|
| 安装 | `install -r` Success | Success（覆盖安装 2 次均成功） | E2：`adb install -r` stdout |
| 冷启动 `am start -W` TotalTime | ≤5 s（S6 口径，占位页） | 227 ms（首测）/ 189 ms / 227 ms | E2：`P0-am-start.txt`、段日志 |
| 运行稳定性 | 不崩 | logcat 3,680 行扫描，`FATAL EXCEPTION` = 0；`pidof` 三次均返回进程号 | E2：`P0-logcat-launch.txt`、`P0-logcat-relaunch.txt` |
| 画面 | 可确认 UI | **不可确认**：`MainActivity` 无 `setContentView`，窗口 `mHasSurface=false`、`isVisible=false`，电视屏保 `com.sony.dtv.livingfit/DreamActivity` 占屏 | E3/E2：`P0-01-launch.png`、`P0-window-dump.txt` |
| APK（dev-A 产出） | 签名 release | 6,844,635 B，SHA256 `bfe79fc79fe815ba127e8265613c8326c690c8e76a71ce967752675f1f3cb85e` | E1：`P0-app-release-before-rebuild.apk` |
| APK（QA 复建） | 同源重建 | 6,844,624 B，SHA256 `6df457162c1c9d2351fca4c4ad2d221d1929eca42fcd62f367e5139a1b0ca7fb` | E1：`P0-gradle-assembleRelease.log` |
| APK（QA worktree 自建，`3d3d33d`） | 独立出包 | 6,844,567 B，SHA256 `b86e8ee155e0f9774d11bcdc45b561fedd6e41b684c1d74fa87a4be695ca1453`；与 dev-A 产物**解包 payload 逐字节一致**，仅 `META-INF/*` 不同 | E1：`P0-qa-worktree-*` |
| 证书 SHA-256 | `a98e8d4b…` | `a98e8d4bbc7a895888502ceecad742729e476d16aa22f8e5f9f9c1d47b259004`（两次构建一致，CN=IPTV Player，RSA 2048） | E1：`apksigner verify --print-certs` |
| 签名方案 | — | v1 ✅、v2 ✅、v3/v3.1/v3.2/v4 ❌ | 同上 |
| `check`（基线/还原后） | SUCCESS | SUCCESS（3 s / 12 s） | E4：`P0-gradle-check-baseline.log`、`-restored.log` |

## 3. 独立复核结果（逐项）

| # | 复核项 | 命令（原文） | 结果 |
|---|---|---|---|
| R1 | 入口组件 | `aapt2 dump badging app-release.apk \| grep launchable` | 实际 `ilab.iptv.player.MainActivity`（leanback + LAUNCHER 双入口） |
| R2 | 文档入口可用性 | `adb shell am start -n ilab.iptv.player/.ui.MainActivity` | **Error type 3 … `ilab.iptv.player.ui.MainActivity` does not exist** → 见 BUG-001 |
| R3 | 装机启动 | `adb install -r … && adb shell am start -W -n ilab.iptv.player/.MainActivity` | Status ok / LaunchState COLD / 无崩溃 |
| R4 | 签名证书 | `apksigner verify --print-certs --verbose` | 证书 SHA-256 = `a98e8d4b…`，与作者声明一致 ✅ |
| R5 | 口令未入库 | `grep -rF "<口令>" .`（排除 `.git`/`build`/`.gradle`/`.kotlin`）；`git check-ignore -v local.properties`；`git log --all -S "<口令>"` | 命中仅 `./local.properties`；该文件被 `.gitignore:5` 忽略、未被跟踪；全历史 0 命中 ✅ |
| R6 | 纯 Kotlin 守护反证 | 临时加 `core/domain/src/main/kotlin/QaPurityProbe.kt`（`import android.os.Bundle`）→ `./gradlew check`；删除后重跑 | 加探针：**BUILD FAILED (25 s)**，`Purity guard failed: :core:domain is a pure-Kotlin module but imports Android: src/main/kotlin/QaPurityProbe.kt -> android.os.Bundle`；删除后：**BUILD SUCCESSFUL (12 s)**；`git status --short` 前后逐字一致，无残留 ✅ |
| R6b | 守护覆盖面（附加） | 同文件改为**无 import 行的全限定写法** `android.os.Bundle::class.java` → `./gradlew check` | 守护**未命中**（非 import 行）；由 Kotlin 编译器兜住：`e: … Unresolved reference 'android'`，BUILD FAILED (7 s) → 见 BUG-005 |
| R7 | 依赖矩阵 · `:feature:channels` | `./gradlew :feature:channels:dependencies` | 无 `core:player` / `core:database` / `core:network` / `core:source` / `core:epg` ✅（注：该模块目前是空骨架，无 dependencies 块，本条为**空洞通过**） |
| R8 | 依赖矩阵 · `:app` | `./gradlew :app:dependencies` | `project :core:database`、`project :core:player` **各出现 2 处**（debug/releaseRuntimeClasspath 及对应 unitTest classpath），路径为 `:core:data → :core:database`、`:feature:player → :core:player`；`:app` 自身 `build.gradle.kts` 未直接声明二者 → 见 BUG-002 |
| R9 | 可复现性 | 备份原 APK → `./gradlew assembleRelease` → 比对 | 构建 SUCCESS (4 s)，**SHA256 变化**：仅 `META-INF/*`（CERT.RSA/CERT.SF/MANIFEST.MF）与 `META-INF/version-control-info.textproto` 不同，`diff -rq` 其余文件全部一致 → 见 BUG-004 |
| R10 | 独立出包并装机（QA 自建） | `git merge --ff-only main`（→ `3d3d33d`）→ `./gradlew assembleRelease` → `apksigner verify --print-certs` → `adb install -r` → `am start -W` | BUILD SUCCESSFUL 46 s（320 executed / 263 from cache）；签名 v1+v2 ✅、证书 SHA-256 `a98e8d4b…` ✅；入口 `ilab.iptv.player.MainActivity`；安装 Success；冷启动 401 ms；`FATAL EXCEPTION` = 0 → 作者的"可出签名包 + 可装机"结论**独立复现成立** ✅ |

> R9 的副作用：仓库内 `app/build/outputs/apk/release/app-release.apk` 现为 QA 复建产物（SHA `6df45716…`）。原产物（`bfe79fc7…`）完整保存在证据目录 `P0-app-release-before-rebuild.apk`；两者 payload 与证书一致。`build/` 被 `.gitignore` 忽略，工作区状态未变。

## 4. 证据归档

| 编号 | 内容 | 路径 / 文件名 |
|---|---|---|
| E1 | 版本三连 + APK SHA256 + 证书 | `P0-app-release-before-rebuild.apk`（dev-A 原包）、`P0-qa-worktree-app-release.apk`（QA 自建）、`P0-qa-worktree-apk-sha256.txt`、`P0-qa-worktree-apksigner.txt`、`P0-gradle-assembleRelease.log`、`P0-qa-worktree-assembleRelease.log` |
| E2 | 命令原文 + logcat + 进程状态 | `P0-am-start.txt`、`P0-qa-build-am-start.txt`、`P0-logcat-launch.txt`、`P0-logcat-relaunch.txt`、`P0-qa-build-logcat.txt`、`P0-window-dump.txt` |
| E3 | 截图（屏保占屏，仅证"未崩溃"） | `P0-01-launch.png`、`P0-02-launch-2s.png`、`P0-qa-build-launch.png` |
| E4 | Gradle 原始日志 | `P0-gradle-check-{baseline,probe-import,probe-fqn,restored}.log`、`P0-deps-app.log`、`P0-deps-channels.log` |
| E5 | 设备与环境 | Android 12 / API 31 / armeabi-v7a / BRAVIA_VH21；`git status` 前后快照 `P0-git-status-{before,after,after-release}.txt` |

## 5. 缺陷与回归

- 未关闭 S1/S2：0（BUG-002 记为 S3；本轮无阻塞级）
- 未关闭 S3 及排期：BUG-001、BUG-002、BUG-003（详见 `04-缺陷台账.md` §3）
- 回归范围：本轮为 P0 冒烟（安装 → 启动 → 存活 → 日志扫描）；换台/长稳属 P1，本轮不适用
- 未决问题（请 god 裁决）：
  1. `docs/04 §8` 的启动命令与实际类名不一致，是否改文档（推荐）还是改包结构？
  2. `docs/02 §3.2` 规则 3 文字（`:app` 不得依赖 `core:database`/`core:player`）与其矩阵 `app` 行（二者 ✅）自相矛盾，出口判据该按哪一条？
  3. P0 出口条件 3（L1 日志骨架）与 4（CI 绿色）当前不满足，是否随 P0-6/P0-7 补做后再整体签 P0-G？

## 6. 结论与签字

| 角色 | 结论 | 备注 |
|---|---|---|
| QA（证据） | P0-5 装机与启动**通过**；P0-G 出口 1 有条件通过，3/4 不通过，2 未测 | 证据齐全，见 §4 |
| god（签字） | **（留空待签）** | 建议：先派修 BUG-001/002，再随 P0-6/P0-7 收口 P0-G |
