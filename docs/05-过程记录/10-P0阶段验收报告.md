# 10 · P0 阶段验收报告（关卡 P0-G）

> 阶段出口：**P0-G** ｜ 版本：`0.1.0` / `1` / @`04d1407` ｜ 证据人：QA（Angela / `worker-qa-4`）｜ 签字人：god ｜ 日期：2026-09-21
> 基线：主线 tip `04d1407`（含 Spike 夹具与 [07-Spike报告](./07-Spike报告.md)），`git merge --ff-only main` 无需动作
> 纪律：本报告只写 QA **自己跑出来的**数，不采信作者自述；原始证据不入公开仓库，存于 `$AGENT_DIR/evidence/P0G/`（文件清单见 §3，
> `$AGENT_DIR` = `/Users/jeffrey/temp/iptv-player-workspace/hive/agents/worker-qa-4`，报告中只引用文件名与结论）

---

## 1. 阶段出口条件（引自 [docs/04 §2.3](../04-开发计划与验收.md)）

| # | 出口条件（原文） | 判据 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | 可重复产出签名 APK 并安装到电视运行 | 出包 + `apksigner verify` + 真机 `install -r` + 冷启动 | 独立出包 6,869,138 B / SHA256 `6d15b9f7…`，v1+v2 验证通过；`install -r` Success；冷启动 COLD×3 = **290 / 279 / 271 ms**；logcat 561 行 0 条 FATAL/ANR | ✅ 通过 |
| 2 | 日志骨架可用（验收 L1） | L1：门面 + Sink 可用；内存环可在 App 内查看 | App 内可见 `APP_START` / `UI_SCREEN_OPEN` 实时事件；内存环 **375/2000（淘汰 0）**、队列积压 0、丢弃 0；分类日志 `IPTV/APP`、`IPTV/UI` 可捞；环覆盖写由单测 6/6 通过 | ⚠️ 有条件通过（条件见 §2.2，均为文档口径，不涉及代码） |
| 3 | S1–S6 有明确数据结论，并据此更新 docs/02 §7/§8 | S1 ≥90% 且平均 ≤3 s；S3 ≥50 fps；S5 平均 ≤3 s 无内存增长；S6 ≤5 s | S3 复跑 **60 fps**（p50 16.67 ms / p95 16.74 ms）；S1 抽 5 条复跑 **5/5 起播**（481–2180 ms）；artifacts 与报告核心数字一致（成功率/均值/p50/PSS），3 处百分位与 2 处证据缺口不闭合 | ⚠️ 有条件通过（条件见 §2.3，报告口径与证据补录） |
| 4 | CI 绿色 | GitHub Actions 结论 success | run `35615696372` @`528fffb`：`conclusion = success`，失败步骤为空；`assembleRelease` 按设计 skip（仓库未配 Secrets） | ✅ 通过 |

**整体建议：可有条件签 P0-G**——四项出口在功能层面全部达标，无 S1/S2 未关闭缺陷，剩余条件均为文档/报告口径与证据补录（§4 新缺陷 BUG-006…009），不需要代码返工。

---

## 2. 逐条取证

### 2.1 出口 1 · 可装机签名 APK（✅ 通过）

| 项 | 实测 | 证据 |
|---|---|---|
| 独立出包 | `./gradlew assembleRelease`（`JAVA_HOME`=JBR 21）BUILD SUCCESSFUL，589 任务（首跑 322 执行 / 267 命中缓存；复跑 `SUCCESS` 且 SHA 未变，全部 UP-TO-DATE，说明无源码漂移） | `build-release-second.log`、`app-release-build1.apk`、`apk-facts.txt` |
| 产物 | `app-release.apk` **6,869,138 B**，SHA256 `6d15b9f741bb693bba9a8a94bc74feacd98f81287b0061cb23dd14d6137e0d6d` | `apk-facts.txt` |
| 签名 | `apksigner verify`：v1 `true`、v2 `true`、v3/v3.1/v3.2/v4 `false`；单签名者；证书 SHA-256 `a98e8d4b…`（与前两轮同一张） | `apk-facts.txt` |
| 清单/入口 | `package ilab.iptv.player`，`versionName 0.1.0` / `versionCode 1`，入口 `ilab.iptv.player.MainActivity`（LAUNCHER + LEANBACK_LAUNCHER 双意图） | `apk-facts.txt` |
| 装机 | `adb install -r` → **Success**（覆盖安装，同签名） | `install.txt` |
| 冷启动 ×3 | `am start -W -n ilab.iptv.player/.MainActivity`：**290 / 279 / 271 ms**，三次均 `LaunchState: COLD`（另一次复测 320 ms） | `cold-start-3x.txt` |
| 无 FATAL/ANR | 全量 logcat 561 行，`FATAL EXCEPTION` / `AndroidRuntime` / `ANR in` / `Force finishing` **0 条**；pid 存活、`mResumedActivity` = `.MainActivity` | `full-logcat-after-3-coldstarts.txt` |

### 2.2 出口 2 · 日志骨架 L1（⚠️ 有条件通过）

**App 内设备页实测（截图 `01`、`10`）**

| 项 | 实测 |
|---|---|
| 设备/版本行 | `设备：Sony BRAVIA 4K VH21 / Android 12 (API 31) / ABI armeabi-v7a,armeabi`；`应用：0.1.0 (1)` |
| 内存环 | **375/2000（淘汰 0）**，队列积压 0；计数：丢弃 0 / 级别过滤 1 / 信封失败 0；会话 `app-0db6` |
| 实时事件 | `APP_START app started {version=0.1.0 (1), device=…}`（INFO）；`UI_SCREEN_OPEN main screen resumed {screen=Main}`、`UI_SCREEN_OPEN log console opened {screen=LogConsole}`（DEBUG） |
| 调级开关 | 页面按钮「级别：INFO（点击切换）」实测可切到 DEBUG（再切 VERBOSE，循环回 INFO），切换后 DEBUG 事件立即出现在同一屏 |
| 级别口径 | `UI_SCREEN_OPEN` 是 DEBUG（docs/03 §3.3），默认 INFO 下**不下发**（`级别过滤 +1`），切 DEBUG 后出现——与文档一致，非缺陷 |

**logcat 分类日志（`logcat-filter-checks.txt`、`logcat-ui-debug.txt`）**

| 命令 | 结果 |
|---|---|
| `adb logcat -s 'IPTV/*:V'`（**docs/03 §6 的写法**） | **0 行**（同一次运行里分类日志确实存在） |
| `adb logcat -s 'IPTV/APP:V'` | 命中 `I IPTV/APP: seq=1 t=… +285848ms APP_START … session=app-0980 thread=main` |
| 切 DEBUG 后 `adb logcat -s 'IPTV/UI:V'` | 命中 `D IPTV/UI : seq=2 … UI_SCREEN_OPEN log console opened {screen=LogConsole}` |

> 结论：tag 形式是 `IPTV/<category>`，`IPTV/*` 不是合法通配 → docs/03 §6 的命令捞不到东西（**新缺陷 BUG-20260921-006**，S4，文档类）。

**内存环上限（行为 + 代码 + 单测三条腿）**

| 证据 | 结果 |
|---|---|
| 控制台自报分母 | `375/2000`——环的容量上限即 2000，页面直接暴露（截图 `10`） |
| 代码 | `MemoryRingSink.DEFAULT_CAPACITY = 2000`，满则 `removeFirst()` 并累加 `evictedCount`；`tail(200)` 供控制台渲染 → 页面上限 200 行 |
| 独立单测 | `./gradlew :core:log:testDebugUnitTest --tests '*MemoryRingSinkTest*'` → **6 tests / 0 failures**（覆盖「只留最新 capacity 条并计数淘汰」「tail 取最新 n」「clear 后计数保留」「非法容量拒绝」） |
| 未达标的部分 | 本轮实测事件数 375 条，**没触达 2000**，因此没有观测到「淘汰」计数增长（见 §6 限制）；「条数有界」由容量常量 + 覆盖写单测 + 页面分母共同支撑 |

**条件（不阻断功能）**：docs/03 §13 L1 写「日志门面 + **4 个 Sink** 可用」，而 §5 共列 5 个 Sink、docs/04 P0-6 只要求 `LogcatSink` + `MemoryRingSink`，实现是 2 个（`LogcatSink`、`MemoryRingSink`，Hilt `@IntoSet` 注册）→ 三处口径不一致（**BUG-20260921-007**，S5）。按 docs/04 P0-6 的 P0 范围判定功能达标。

### 2.3 出口 3 · S1–S6 数据结论（⚠️ 有条件通过）

**a) S3 复跑（QA 自己的 Spike 构建，SHA256 `84761c12…`；`spike/tools/run-spikes.sh s3`）**

| 指标 | dev-B 原始（`s3_s3virtual.json`） | QA 复跑（`s3_s3qa*.json`） | 结论 |
|---|---|---|---|
| 帧数 / 时长 | 1799 / 30 s | **1800 / 30 s** | 一致 |
| 帧间隔 p50 / p95 / p99 / max | 16.67 / 16.72 / 16.80 / 19.63 ms | **16.67 / 16.74 / 16.89 / 17.19 ms** | 一致（p95 均略高于 16.7 ms 判据，属 vsync 抖动） |
| 等效帧率 | 60 fps | **60 fps** | ✅ ≥50 fps |
| PSS 增量 | +9,540 KB | **+9,849 KB** | 一致 |
| 对照 `naive` | 146 帧 / 30 s → 4.28 fps | 79 帧 / 15 s → p50 216.78 ms ≈ 4.6 fps | 一致（不虚拟化不可行，结论不依赖精确值） |

**b) S1 抽样复跑（同一批 5 条 URL，`mode s1`，release）**

| # | 频道（dev-B 编号） | dev-B costMs | QA 复跑 costMs | 倍数 | 复跑实测格式 |
|---|---|---|---|---|---|
| 1 | #2 CCTV-13 | 575 | 867 | 1.5× | `video/avc` 1920×1080 + `audio/mp4a-latm` |
| 2 | #3 CCTV5+ | 488 | 481 | 1.0× | `video/avc` 1280×720 + `audio/mp4a-latm` |
| 3 | #11 CCTV-13新闻 | 832 | 2180 | 2.6× | `video/avc` 1920×1080 + **`audio/mpeg-L2`** |
| 4 | #14 凤凰中文 | 1372 | 1240 | 0.9× | **`video/mpeg2`** 720×576 + `audio/mpeg-L2` |
| 5 | #19 Zhejiang4K | 398 | 1185 | 3.0× | **`video/hevc`** 3840×2160 + `audio/mp4a-latm` |

5/5 `outcome=ok`、`errors=0`、PSS 增量 +8,137 KB；**全部起播且耗时同量级**（均 <3 s，判据 ≤3 s 达成）。最大的两条偏差（第 3、5 条）出现在设备负载极高时（`uptime` load average ≈ 39），与源端/负载一致，不构成回归。

**c) 原始 artifacts 与 07 报告数字对账（含 `worker-dev-b/evidence/artifacts/*`）**

| 报告数字 | artifacts 直接计算 | 对账 |
|---|---|---|
| S1 成功率 20/20 = 100% | 20 条全 `ok`，`errors=0` | ✅ |
| S1 平均 2138.8 ms | 2138.8 ms | ✅ |
| S1 p50 1182.5 ms | 1182.5 ms | ✅ |
| S1 p90 5244 ms | nearest-rank = **5035 ms** | ❌ 不闭合 |
| S1 p95 6215 ms | nearest-rank = **5244 ms**（6215 是最大值） | ❌ 不闭合 |
| S1 PSS 17341→33318 KB（+15.6 MB） | 一致 | ✅ |
| S5 复用 30 次：平均 946 ms / p50 514 / 30-30 出首帧 / PSS +3.9 MB | 946.0 / 513.5 / 全 `ok` / +3,943 KB | ✅（p95 报告 3313 ms vs 计算 **3282 ms**，❌ 不闭合） |
| S5 重建 10 次：平均 765 / PSS +12.5 MB | 765.4 / +12,533 KB | ✅ |
| S3 全表（帧数/百分位/掉帧/每秒绘制块数/PSS） | 逐项一致 | ✅ |
| S6 5 次冷启动：视频首帧 1546/1459/1434/1394/1523 ms | 设备上只有第 1/4/5 次的 artifact（1546/1394/1523 与报告一致），第 2、3 次**无原始 artifact**；「首页首帧 280–307 ms」「TotalTime 808–849」同样无 artifact | ⚠️ 部分不可对账 |
| S2「到 READY 528–682 ms」等 | `s2_*.json` **没有** `readyAfterMs` 字段（`costMs` 为 384/679/493），只能回到 logcat | ⚠️ 不可对账 |

→ 三条实质结论（S1 成功率与均值、S3 帧率、S5 均值与内存）**完全成立**；不闭合的是百分位定义（报告取了最大值附近）与两类证据缺口，登记为 BUG-008 / BUG-009。

**条件**：① 07 报告按 nearest-rank（或写明百分位取法）重算 S1 p90/p95 与 S5 p95；② 补 S6 第 2、3 次的 artifact（或标注该两行为 logcat 推算）与 S2「到 READY」的原始字段/来源；③ 同时把设备上已存在但未拉回的 `s1_colds6-5.json` 补进 dev-B 的 evidence 目录。

### 2.4 出口 4 · CI 绿色（✅ 通过）

| 项 | 实测 | 证据 |
|---|---|---|
| Run | `35615696372`（`.github/workflows/ci.yml`，event=push，branch=main），`status=completed`，**`conclusion=success`** | `ci-run-35615696372.json` |
| SHA | `528fffb`（= `origin/main` 当时 tip） | `ci-run-35615696372.json`、`git-state.txt` |
| 步骤 | `Set up job` ✅ → `checkout` ✅ → `Set up JDK 21` ✅ → `Install Android SDK components` ✅ → **`check + lint` ✅** → **`assembleDebug` ✅** → `assembleRelease` **skipped** → `assembleRelease skipped (no signing secrets)` ✅ → `Upload APKs` ✅；失败步骤为空 | `ci-run-35615696372-jobs.json` |
| skip 是否按设计 | 是：release 步骤带 `if: ${{ env.RELEASE_STORE_PASSWORD != '' }}`（`ci.yml:99-100`），仓库未配 3 个 `RELEASE_*` Secrets；兜底步骤（`ci.yml:108`）在 Step Summary 写明「跳过不等于签名」，且 CR-07 本地仍会驳回未签名 release | `.github/workflows/ci.yml:99-136` |
| 历史 | 上一个 run `35614747492` @`75e8571` 结论 failure（停在 `setup-android`），已被本次取代 | GitHub API |

> 覆盖范围提醒：绿色针对 `528fffb`。本线 tip `04d1407`（只新增 `spike/` 夹具与 07 报告，`spike/` 是独立 Gradle 构建、不参与根构建）**未推送**，因此没有对应 run；god 推送后会自然触发，P0-G 的 CI 证据以推送后的那次为准。

---

## 3. 证据归档

全部原始文件在 `$AGENT_DIR/evidence/P0G/`（`$AGENT_DIR` = `…/hive/agents/worker-qa-4`），仓库内只留文件名与结论（脱敏规则 docs/03 §11）。

| 编号 | 内容 | 路径 / 文件名 |
|---|---|---|
| E1 | 版本三连 + APK SHA256 + 签名/清单 | `apk-facts.txt`、`install.txt`、`git-state.txt` |
| E2 | 事件码 + logcat | `full-logcat-after-3-coldstarts.txt`、`logcat-IPTV-coldstarts.txt`、`logcat-filter-checks.txt`、`logcat-ui-debug.txt` |
| E3 | 截图 | `01-console-info-level.png`、`03-console-tap-level.png`、`05-console-baseline-debug-cleared.png`、`09-console-fresh-debug.png`、`10-console-ring-after-events.png`（控制台：事件、级别、内存环 375/2000） |
| E4 | 指标原始数据 | `cold-start-3x.txt`、`spike/artifacts/s3_s3virtual.json`、`s3_s3naive.json`、`s1_s1qa.json`、`spike/s1-rerun-logcat.txt`、`TEST-…MemoryRingSinkTest.xml`、`ci-run-35615696372*.json` |
| E5 | 环境与复现 | 设备 `Sony BRAVIA 4K VH21` / Android 12 / API 31 / armeabi-v7a；`adb connect <TV-LAN-IP>:5555`；`JAVA_HOME=/Applications/Android Studio.app/Contents/jbr/Contents/Home`；`ANDROID_HOME=$HOME/Library/Android/sdk`；签名口令从共享树 `local.properties` 以环境变量注入（不落盘）；Spike 构建 `~/.gradle/wrapper/dists/gradle-8.13-bin/*/gradle-8.13/bin/gradle -p spike assembleRelease` + `spike/tools/sign-release.sh` |

---

## 4. 缺陷与回归

**未关闭 S1/S2：0**（放行前置条件满足）。未关闭 S3/S4：1（BUG-001 入口文档）、2（BUG-003 CI 覆盖面已修、首跑已补）、4（BUG-004 转文档口径）；
本轮新增 4 条，全部是文档/报告口径类，**不含代码缺陷**。下列按 [04-缺陷台账](./04-缺陷台账.md) §2 字段格式登记（本波未改 `docs/01–04`，请 god 合并进台账）。

| ID | 版本 | 严重级 | 复现率 | 关联用例 | 证据 | 状态 | 修复人 | 验证人 |
|---|---|---|---|---|---|---|---|---|
| BUG-20260921-006 | `0.1.0 (1)` @`04d1407` | S4 | 必现 | TC-P0-LOG-01 | `docs/03` §6 给的 `adb logcat -s 'IPTV/*:V'` 实测 **0 行**，而同一次运行 `-s 'IPTV/APP:V'` / `-s 'IPTV/UI:V'` 命中；tag 形态是 `IPTV/<category>`，`IPTV/*` 不是合法通配。证据 `evidence/P0G/logcat-filter-checks.txt`、`logcat-ui-debug.txt` | 新建（待确认） | arch（文档）/ god 同步 | QA |
| BUG-20260921-007 | `0.1.0 (1)` @`04d1407` | S5 | 必现 | TC-P0-LOG-01 | `docs/03` §13 L1 写「日志门面 + **4 个 Sink** 可用」，§5 列 5 个 Sink，`docs/04` P0-6 只要求 `LogcatSink`+`MemoryRingSink`；实现为 2 个（`core/log/…/di/LogModule.kt` 的 `@IntoSet`）。证据 `evidence/P0G/10-console-ring-after-events.png`、`LogModule.kt` | 新建（待确认） | arch（文档口径）/ god 裁定 | QA |
| BUG-20260921-008 | `0.1.0 (1)` @`04d1407` | S4 | 必现 | TC-P0-SPK-01 | `07-Spike报告` §2 的 S1 p90=5244 / p95=6215、§5 的 S5 p95=3313，与原始 artifacts 按 nearest-rank 计算（5035 / 5244 / 3282）不符（报告的 p95 取了最大值）；均值、p50、成功率、PSS 全部一致。证据 `…/worker-dev-b/evidence/artifacts/s1_s1a.json`、`s5_s5reuse.json`，复算脚本见 §6 | 新建（待确认） | dev-B（报告作者） | QA |
| BUG-20260921-009 | `0.1.0 (1)` @`04d1407` | S5 | 必现 | TC-P0-SPK-01 | S6 报 5 次冷启动，原始 artifact 只有第 1/4/5 次（设备上第 2、3 次也没有），「首页首帧 280–307 ms」「TotalTime 808–849 ms」无 artifact；S2「到 READY 528–682 ms」在 `s2_*.json` 里没有对应字段（只有 `costMs` 384/679/493）→ 这些数字无法从证据复核。另：设备上已有 `s1_colds6-5.json` 但未拉回 dev-B 的 evidence 目录。证据 `evidence/P0G/s1_colds6-5.json`、设备 `…/files/s1_colds6-*.json` | 新建（待确认） | dev-B | QA |

**回归范围**：出口 1 真机冒烟（装机/冷启动 ×3/无崩溃）+ 出口 2 日志链（事件 → 队列 → 双 Sink → 页面）+ 出口 3 抽样复跑（S3 全量口径复跑、S1 5/20 抽样）+ 出口 4 CI 复核。未跑长稳（L5）与换台 30 次真机冒烟——属 P1 出口。

---

## 5. 结论与签字

| 角色 | 结论 | 备注 |
|---|---|---|
| QA（证据） | **四项出口功能层面全部达标；证据齐全但带 4 条口径/证据类新缺陷** | S1/S2 未关闭 = 0；BUG-006…009 均为文档或报告修正，不涉及代码改动 |
| god（签字） | 未签 | 建议：**有条件通过** —— 条件为 BUG-006/008 随文档批 `DOC-2` 修正，BUG-007 由 god 裁定口径，BUG-009 补录 S6/S2 原始 artifact |

---

## 6. 限制与未覆盖（诚实清单）

1. **设备在 QA 压力测试中自行重启一次**：为取证「内存环有上限」，QA 用 `am start` 快速切换屏幕累计约 2200 次，设备随后重启（`uptime` 归零、`getprop sys.boot.reason=reboot`、重启后 App 与本期证据均正常，logcat 无 FATAL/ANR）。这是合成负载（远高于任何真实使用），**不作为产品缺陷**，但在 P1 长稳/换台冒烟时应按正常节奏跑并观察。
2. **内存环 2000 上限未在实测中触达**：本轮实测 375 条（未观测到「淘汰」增长），上限依据是源码常量 + `MemoryRingSinkTest` 6/6 + 控制台分母；受第 1 条影响未再叠加到 2000+ 条。
3. **S1 复跑是与 dev-B 不是同一次会话**：结果数量级一致但绝对值受源端与设备负载影响（本次负载 load≈39，第 3/5 条偏慢 2.6–3.0×）；抽样只覆盖 5/20 条，不能替代全量 658 条复测（W-S1-3）。
4. **S2 未复测**：P0 判据只看矩阵结论；AC3/EAC3 直播源当天已失效，dev-B 用公开测试向量 + 本机 HTTP 替代（其报告 §8 已标注非直播封装）。
5. **未做**：长稳内存（docs/02 §8.5 播放 10 min）、EPG 数据层与 Room、HDMI ARC/AVR 透传听感——均为 P1/P3 范围。
6. **复算方法**：artifacts 对账用 Python 直接读 `entries[].costMs`，nearest-rank 取 `sorted[ceil(p·n)-1]`；S3/S5 的 PSS、帧数以 artifact 字段为准，不做二次加工。
