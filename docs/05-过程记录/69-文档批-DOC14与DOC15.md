# 69 · 文档批 · DOC-14 + DOC-15（把已批准/已落地口径补进基线文档）

> 卡：`DOC-14` + `DOC-15`（**文档批，只改文档、不用电视**）｜ 执行：temp `worker-toby2-doc-batch`（Toby2，codex）｜ 日期：2026-09-23
> 基线：worktree `worker-toby2-doc-batch`（分支 `agent/worker-toby2-doc-batch`）
> 边界：**只改文档**；未动代码；**未 commit / 未 push**；**未用电视**；脱敏（需要设备地址处写 `<TV-LAN-IP>`）
> 验收：**归 god 复核**（文档类不走 QA 真机轮）

---

## 0. 结论

`D13`（长稳验收）、`CI-FLAKE-1`（CI 竞态）与两处 EPG / 事件码口径，已按**已批准 / 已落地**的结论补进基线文档。每条改动标注来源（卡号 / 记录号 + 批准日期）；**不改既有结论**、**改动最小化**（能加一段 / 一条就不改正文）；`tools/ci/sensitive-info-guard.sh` 绿。

## 1. 改动清单（逐条）

| # | 文件 · 位置 | 改动 | 来源（卡 / 记录 + 批准日期） |
|---|---|---|---|
| 1 | `docs/01-需求与可行性.md` §5 决策记录 | **新增 `D13` 行**：长稳验收 = **~15 min 采样 + 以日志可排查为替代保障**；P1/P2 出口不再要求连续播放 2 h；长期体验回归真实使用观察 | 人工决策 `D13`（2026-09-22，决策人 god）；依据 `05-过程记录/25-P1阶段验收报告.md`（v2 按此口径签 `P1-G`）、`28-关键路径事件码.md`（`D13` 配套补 4 码） |
| 2 | `docs/04-开发计划与验收.md` §8 | **新增「CI 竞态说明」段**：`CI-FLAKE-1`（lint × Hilt 任务图竞态）**已修**（Hilt 插件任务接线，修复提交 `da1761e`）；**判据 = 同一提交重跑一次为准**；历史假红记录指向对应提交 / 记录 | `CI-FLAKE-1`（god 回写 2026-09-23）；依据 `49-CI竞态修复.md`、`16-P1-4播放界面验证.md` §8 |
| 3 | `docs/02-技术架构设计.md` §7.7 | **新增一行注**：`PLAY_PREPARE_FAIL.origin`（`ENV-1`）与 `PLAY_FAILOVER.stage`（`SWITCH-P95-1`）为**纯增量字段**；取值 / 语义**以 `03 §3.3` 为准，本文不复制** | `DOC-14c`（补 / 核对与事件码相关的口径）；依据 `66-环境闸门治理.md`、`65-换台p95修复.md`（god 批准 2026-09-23） |
| 4 | `docs/02-技术架构设计.md` §6.3 | **新增一条判读规则**：网格 ⟷ 面板**四种情形**，**补第四种** = 同一页里多个频道共用同一 `epg_channel_id`（guide id 多对一）时的折叠判读；修复后同页每台都出块 | `DOC-15`；依据 `52-BUG018取证与NEW2.md` §3.3（三分支）、`54-BUG018对账修复.md` §2.3 / §8.6（第四情形 + 修复） |

## 2. 为什么最小化

每条都是**追加**（新表行 / 新段 / 新注 / 新 bullet），**不重写任何既有结论**：

1. `D13` 只是补上决策表里**缺失的那一行**（原表 `D12` 直接跳到 `D14`）。文档别处（`docs/03 §3.3.1`、`05-过程记录/03-测试计划与用例.md`、`25`、`28`）早已在按此口径执行——本次是把口径**落回 `01 §5` 的单一出处**，不是新推导。
2. `docs/04 §8` 原有的「CI 稳定性」注（记现象 + 三条缓解建议）**逐字保留**；新段紧跟其后，只把「**已修 + 判据**」补成结论。修复记录 `49` §7 第 2 点已明确建议回写这句；**不用**「CI 里重跑一次」掩盖（49 号的选择是从任务图消除竞态）。
3. `docs/02 §7.7` 的注**只指路、不复制**——字段取值 / 语义的唯一定义仍在 `03 §3.3`（同 §4.0 F1「03 为事件码唯一定义来源」）。本表只登记「事件 → 触发者 → 关键字段」，不重复定义。
4. `docs/02 §6.3` 的判读规则**此前不在基线文档**（只在 `52` / `54` 号过程记录里）；本次按 **`54` §8.6 的明示「交文档批（`docs/02`）补齐第四种情形」**落一条，不含任何新推导。

## 3. 与基线文档的交叉引用

- `D13`：`docs/03 §3.3.1`（「以日志可排查为替代保障」落到四条关键路径的必打码）、`docs/03` §13 L5、`05-过程记录/03-测试计划与用例.md`（L5 用例）。
- CI 竞态：`docs/04 §8` 原有「CI 稳定性」注（同页，逐字保留）。
- 事件码增量字段：`docs/03 §3.3`（`PLAY_PREPARE_FAIL` 行、`PLAY_FAILOVER` 行）。
- 判读规则：`docs/02 §6.3`「覆盖率三口径」「绑定择优」，`docs/02 §8.3`（网格分页 32 行）。

## 4. 质量门（实跑留痕）

| 命令 | 结果 |
|---|---|
| `bash tools/ci/sensitive-info-guard.sh` | **OK — 0 unallowlisted hit(s)**（706 tracked files；37 条既有 allowlist 命中，均与本批无关） |

## 5. 未做项

- 未改 `docs/05` **既有**记录（只新增本文件 + 索引一行）。
- 未动代码；未 commit / 未 push；未用电视。
- 未新增 / 删除任何事件码（`docs/03 §3.3` 仍 **50** 条）。
- 未改 `docs/02` 的冻结接口签名（`docs/02 §4` 未动）。
