# 04 · 缺陷台账

> 关卡：G5/G6（用例评审 → 提测与缺陷闭环）｜ 作者：QA（Angela）｜ 日期：2026-09-21
> 配套：[03 测试计划与用例](./03-测试计划与用例.md) §7（分级、模板、关闭规则）
> 纪律：缺陷由**作者**修，QA 只做验证关闭；作者 ≠ 修复人 ≠ 验证人。

---

## 1. 用途

一张表回答三个问题：**现在有多少缺陷、卡在谁手里、能不能放行出口**。每条缺陷的字段与证据要求见 `03-测试计划与用例.md` §7；缺证据的单子由 QA 打回，不进修复队列。

---

## 2. 字段说明

| 字段 | 取值 / 格式 |
|---|---|
| ID | `BUG-<yyyymmdd>-<nnn>`，按发现日递增 |
| 版本 | `versionName` + `versionCode` + `git rev-parse --short HEAD`（+ APK SHA256 前 8 位） |
| 严重级 | S1 阻塞 / S2 严重 / S3 一般 / S4 轻微 / S5 建议（判据见 03 §7.1） |
| 复现率 | 必现 / 高（≥7/10）/ 中（3–6/10）/ 低（≤2/10）/ 单次 |
| 关联用例 | `TC-xxx-xx`，并写清对应需求 F* |
| 证据 | 事件码 + logcat 片段 / JSONL 会话、截图或录屏、指标数值；**四项必填** |
| 状态 | 新建 / 已确认 / 修复中 / 待验证 / 已关闭 / 重开 / 挂起 |
| 修复人 | 提交修复的作者（不是发现人） |
| 验证人 | QA（只有 QA 能写「已关闭」） |

---

## 3. 台账（当前）

| ID | 版本 | 严重级 | 复现率 | 关联用例 | 证据 | 状态 | 修复人 | 验证人 |
|---|---|---|---|---|---|---|---|---|
| BUG-20260921-001 | 0.1.0 (1) @5ad2dfa（APK `bfe79fc7`） | S3 | 必现 | TC-P0-DEV-03 | `adb shell am start -n ilab.iptv.player/.ui.MainActivity` → `Error type 3 … Activity class {ilab.iptv.player/ilab.iptv.player.ui.MainActivity} does not exist`；`aapt2 dump badging` 实际入口为 `ilab.iptv.player.MainActivity`；`docs/04 §8` 的发布流程写的是 `.ui.MainActivity`；证据 `evidence/P0/P0-am-start.txt` | 新建 | dev-A（文档由 god 同步） | QA |
| BUG-20260921-002 | 0.1.0 (1) @5ad2dfa | S3 | 必现 | TC-P0-CI-02 | `./gradlew :app:dependencies` 中 `project :core:database`、`project :core:player` 各出现 2 处（debug/releaseRuntimeClasspath + unitTest），路径 `:core:data → :core:database`、`:feature:player → :core:player`；`:app` 未直接声明二者。`docs/02 §3.2` 规则 3 文字禁止 `:app` 依赖二者，同节矩阵 `app` 行却标 ✅ → 文档自相矛盾，出口判据待裁决；证据 `evidence/P0/P0-deps-app.log` | 已确认（待 god 裁决口径） | arch（文档口径）/ god | QA |
| BUG-20260921-003 | 0.1.0 (1) @5ad2dfa | S3 | 必现 | TC-P0-CI-01、TC-P0-CI-02 | GitHub API `repos/cuijfboy/iptv-player/actions/runs` → `total_count = 0`（CI 从未运行，"首次绿色构建"无证据）；`ci.yml` 只跑 `assembleDebug`/`testDebugUnitTest`/`lint` + 纯 Kotlin grep 守护，**未跑 `check`、未跑 `assembleRelease`、无依赖规则守护任务**，与 `docs/02 §3.2` 规则 5"CI 强制"不符 | 新建 | dev-A / god（Push 权限） | QA |
| BUG-20260921-004 | 0.1.0 (1) @5ad2dfa | S4 | 必现 | TC-P0-BLD-03 | 同源重建（`./gradlew assembleRelease`，BUILD SUCCESSFUL 4 s）后 SHA256 由 `bfe79fc7…` 变为 `6df45716…`；`diff -rq` 解包对比仅 `META-INF/CERT.RSA`、`CERT.SF`、`MANIFEST.MF`、`META-INF/version-control-info.textproto` 不同，payload 与证书一致。影响：报告里的 APK SHA256 只能标识单次产物 | 新建 | dev-A（可选：开启可复现构建） | QA |
| BUG-20260921-005 | 0.1.0 (1) @5ad2dfa | S4 | 必现 | TC-P0-CI-02 | 纯 Kotlin 守护只匹配 `import` 行：全限定写法 `android.os.Bundle::class.java` 未触发 `verifyPureKotlin`，由 Kotlin 编译器 `e: Unresolved reference 'android'` 兜住（BUILD FAILED 7 s）。证据 `evidence/P0/P0-gradle-check-probe-fqn.log` | 新建 | dev-A | QA |

> 首轮登记（2026-09-21，P0-5 真机验收）。缺陷详情与命令原文见 [06-P0真机验收报告](./06-P0真机验收报告.md)。

### 3.1 示例（**格式示例，不是真实缺陷**）

| ID | 版本 | 严重级 | 复现率 | 关联用例 | 证据 | 状态 | 修复人 | 验证人 |
|---|---|---|---|---|---|---|---|---|
| BUG-20260921-001 | 0.1.0 (1) @ abc1234 | S2 | 必现 | TC-P1-PLAY-03 | `PLAY_FAILOVER` 缺失；logcat 原文；录屏 `failover-01.mp4`；主源 403 时未切备胎 | 待验证 | dev-B | QA |

---

## 4. 统计口径（每阶段出口填写）

| 阶段 | 新增 | 已关闭 | S1/S2 未关闭 | 平均修复时长 | 结论 |
|---|---|---|---|---|---|
| P0 | 5 | 0 | 0 | —（修复未开始） | 出口 1 有条件通过；出口 3（L1）/4（CI）不通过，见 06 报告 §1 |
| P1 |  |  |  |  |  |
| P2 |  |  |  |  |  |
| P3 |  |  |  |  |  |

---

## 5. 状态流转与规则

```
新建 ──确认──▶ 已确认 ──▶ 修复中 ──提交修复──▶ 待验证 ──QA 复测通过──▶ 已关闭
                                  ▲                 │
                                  └── 复测不通过 ── 重开（计数 +1）
                （S1/S2 停线优先修；挂起需 god 记录理由）
```

- **关闭权**：只有 QA 能关闭；关闭必须附新证据（重跑用例 + 版本三连）。
- **重开**：同一用例重开 ≥2 次 → 严重级升一级，并回 G4 做代码评审复盘。
- **挂起**：`wontfix` 或需人工决策时由 god 记录理由；S1/S2 不允许挂起。
- **脱敏泄露**：一律 S1，当轮修，且要复检导出包与历史日志。
- **与出口的关系**：G6/G7 放行要求 S1/S2 未关闭数为 0；S3 必须有明确排期（见 03 §10）。
