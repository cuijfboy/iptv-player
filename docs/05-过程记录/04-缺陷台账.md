# 04 · 缺陷台账

> 关卡：G5/G6（用例评审 → 提测与缺陷闭环）｜ 作者：QA（Angela）｜ 日期：2026-09-21
> 配套：[03 测试计划与用例](./03-测试计划与用例.md) §7（分级、模板、关闭规则）
> 纪律：缺陷由**作者**修，QA 只做验证关闭；作者 ≠ 修复人 ≠ 验证人。

---

## 1. 用途

一张表回答三个问题：**现在有多少缺陷、卡在谁手里、能不能放行出口**。每条缺陷的字段与证据要求见 `03-测试计划与用例.md` §7；缺证据的单子由 QA 打回，不进修复队列。

---

## 2. 字段说明

| 字段 | 取值 / 格式 |
|---|---|
| ID | `BUG-<yyyymmdd>-<nnn>`，按发现日递增 |
| 版本 | `versionName` + `versionCode` + `git rev-parse --short HEAD`（+ APK SHA256 前 8 位） |
| 严重级 | S1 阻塞 / S2 严重 / S3 一般 / S4 轻微 / S5 建议（判据见 03 §7.1） |
| 复现率 | 必现 / 高（≥7/10）/ 中（3–6/10）/ 低（≤2/10）/ 单次 |
| 关联用例 | `TC-xxx-xx`，并写清对应需求 F* |
| 证据 | 事件码 + logcat 片段 / JSONL 会话、截图或录屏、指标数值；**四项必填** |
| 状态 | 新建 / 已确认 / 修复中 / 待验证 / 已关闭 / 重开 / 挂起 |
| 修复人 | 提交修复的作者（不是发现人） |
| 验证人 | QA（只有 QA 能写「已关闭」） |

---

## 3. 台账（当前）

| ID | 版本 | 严重级 | 复现率 | 关联用例 | 证据 | 状态 | 修复人 | 验证人 |
|---|---|---|---|---|---|---|---|---|
| BUG-20260921-001 | 0.1.0 (1) @02e84f8 | S3 | 必现 | TC-P0-DEV-03 | 原证据 `evidence/P0/P0-am-start.txt`（`Error type 3 … .ui.MainActivity does not exist`）；**复验（2026-09-21）**：`docs/04-开发计划与验收.md:134` 仍写 `am start -n ilab.iptv.player/.ui.MainActivity`，而 APK 实际入口为 `ilab.iptv.player.MainActivity`（`aapt2` 清单 + 真机启动一致）→ **文字未改**；证据 `evidence/P0FIX/aapt2-manifest-xmltree.txt`、`evidence/P0FIX/cold-start.txt` | 仍开放（转文档批 `DOC-2`） | arch（文档）/ god 同步 | QA |
| BUG-20260921-002 | 0.1.0 (1) @02e84f8 | S3 | 必现 | TC-P0-CI-02 | 原证据 `evidence/P0/P0-deps-app.log`；**复验**：`docs/02 §3.2` 矩阵 `app` 行仍标 ✅ `database`/`player`，与同节规则 3 文字（禁止 `:app` 直接声明）自相矛盾，**文档未改**；新增构建期守护按**规则文字**实现，反证 A（`:app` + `:core:player` → `rule 3 … FAILED`）证明守护不会放过；证据 `evidence/P0FIX/02-guard-counterexample-A-app-player.log` | 仍开放（转文档批 `DOC-2`，矩阵定稿后可改白名单） | arch（文档口径）/ god | QA |
| BUG-20260921-003 | 0.1.0 (1) @02e84f8 | S3 | 必现 | TC-P0-CI-01、TC-P0-CI-02 | 原证据 `evidence/P0/P0-ci-runs.json`；**复验·覆盖面已修**：`ci.yml:42/45/49-55/30-34` 已跑 `check lint` + `assembleDebug` + `assembleRelease`（口令走 Secrets）+ 显式 `sdkmanager "platforms;android-36" "build-tools;35.0.0" "platform-tools"`，旧裸 `grep` 守护已删。**仍开放的一半**：GitHub API `actions/runs` 与 `actions/workflows` 均 `total_count = 0`；根因 `origin/main`=`039f747`，本机主线 `02e84f8` 领先 **7 个未推送提交** → CI 从未被触发。证据 `evidence/P0FIX/ci-github-runs.json`、`ci-github-workflows.json` | 仍开放（覆盖面已修复；首跑待 god 推送触发） | dev-A（已完成）/ god（推送权限） | QA |
| BUG-20260921-004 | 0.1.0 (1) @02e84f8 | S4 | 必现 | TC-P0-BLD-03 | 原证据 `evidence/P0/P0-*.apk`（`bfe79fc7…`→`6df45716…`）；**复验**：QA 本轮独立出包 SHA256 `1f19ac85…`（6,845,227 B），与作者产物 `bcc40475…` **体积一致、哈希不同**，差异仍只在签名/元数据块。god 已裁决"不追求可复现构建，文档写明 SHA 只标识单次产物"→ 处置为文档口径，不再追代码；证据 `evidence/P0FIX/apk-sha256.txt`、`apksigner-verify.txt` | 转文档批（`DOC-2`；god 裁决不追可复现） | dev-A（无代码改动） | QA |
| BUG-20260921-005 | 0.1.0 (1) @02e84f8 | S4 | 必现 | TC-P0-CI-02 | 原证据 `evidence/P0/P0-gradle-check-probe-fqn.log`；**复验通过**：在 `:core:model` 的 `Channel.kt` 加**无 import** 的 `android.os.Build.VERSION.SDK_INT` → `:core:model:verifyPureKotlin` FAILED，报 `Channel.kt:75 -> Android reference: …`；改为注释 + 字符串字面量则不误报（OK）；探针已撤回，`check` 恢复 SUCCESS。证据 `evidence/P0FIX/08-purity-counterexample-BUG005.log`、`09-purity-negative-comment-string.log`、`10-check-final-restored.log` | 已关闭（QA 复验通过） | dev-A（已完成） | QA |
| BUG-20260921-006 | `0.1.0 (1)` @`04d1407` | S4 | 必现 | TC-P0-LOG-01 | `docs/03` §6 给的 `adb logcat -s 'IPTV/*:V'` 实测 **0 行**，而同一次运行 `-s 'IPTV/APP:V'` / `-s 'IPTV/UI:V'` 命中；tag 形态是 `IPTV/<category>`，`IPTV/*` 不是合法通配。证据 `evidence/P0G/logcat-filter-checks.txt`、`logcat-ui-debug.txt` | 新建（待确认） | arch（文档）/ god 同步 | QA |
| BUG-20260921-007 | `0.1.0 (1)` @`04d1407` | S5 | 必现 | TC-P0-LOG-01 | `docs/03` §13 L1 写「日志门面 + **4 个 Sink** 可用」，§5 列 5 个 Sink，`docs/04` P0-6 只要求 `LogcatSink`+`MemoryRingSink`；实现为 2 个（`core/log/…/di/LogModule.kt` 的 `@IntoSet`）。证据 `evidence/P0G/10-console-ring-after-events.png`、`LogModule.kt` | 新建（待确认） | arch（文档口径）/ god 裁定 | QA |
| BUG-20260921-008 | `0.1.0 (1)` @`04d1407` | S4 | 必现 | TC-P0-SPK-01 | `07-Spike报告` §2 的 S1 p90=5244 / p95=6215、§5 的 S5 p95=3313，与原始 artifacts 按 nearest-rank 计算（5035 / 5244 / **3313**）不符（报告的 p95 取了最大值）；均值、p50、成功率、PSS 全部一致。证据 `…/worker-dev-b/evidence/artifacts/s1_s1a.json`、`s5_s5reuse.json`，复算脚本见 §6。**QA 勘误（第 5 轮）**：本格原文写 `3282`，那是 `floor(p/100×N)` 的取法；按报告 §1.2 定义的 `ceil(p/100×N)`，S5 p95 = **3313**，即报告值本身就对 | **已关闭（QA 第 5 轮复验通过，2026-09-22）**：dev-B 第 3 轮已修（百分位统一并写进 `07-Spike报告` §1.2 nearest-rank；S1 p90/p95 改为 5035/5244、p50 1112，S5-A p50 513、S5-B p50 389，逐条来自 artifact；复算脚本 `spike/tools/recompute-percentiles.py`，输出 `evidence/percentiles-nearest-rank.txt`）。QA 第 5 轮**不用作者脚本**、直接读 artifact 复算：S1 p50/p90/p95 = 1112/5035/5244、平均 2138.8、max 6215，S5 p50 513/389、p95 3313/3764 —— 与报告 §0/§2/§5 逐条一致。见 `18-QA批次复验报告` §3.1 | dev-B（第 3 轮 `worker-dev-b-3`） | QA（第 5 轮 `worker-qa-5`） |
| BUG-20260921-009 | `0.1.0 (1)` @`04d1407` | S5 | 必现 | TC-P0-SPK-01 | S6 报 5 次冷启动，原始 artifact 只有第 1/4/5 次（设备上第 2、3 次也没有），「首页首帧 280–307 ms」「TotalTime 808–849 ms」无 artifact；S2「到 READY 528–682 ms」在 `s2_*.json` 里没有对应字段（只有 `costMs` 384/679/493）→ 这些数字无法从证据复核。另：设备上已有 `s1_colds6-5.json` 但未拉回 dev-B 的 evidence 目录。证据 `evidence/P0G/s1_colds6-5.json`、设备 `…/files/s1_colds6-*.json` | **已关闭 = 部分误报（QA 第 5 轮复验，2026-09-22）**：dev-B 第 3 轮已修（设备残留件 `s1_colds6-5.json` 已补回；S6 **重跑 5 次**并逐次留证 `evidence/colds6/run-1..5.txt` + `artifacts/s1_colds6-1..5.json`（首页 291–316 ms、视频 1487–1588 ms、TotalTime 866–898 ms），旧表数字作废；S2「到 READY」按 artifact 复核——该字段一直存在，逐条在 `s2_s2aac2.json` 的 `entries[].s2.readyAfterMs`（528/591/682），QA 当时看的 `s2_s2aac_sw.json` 是同日 22:25 已被取代的首次试跑文件，§3.3 已标出每行 artifact 文件名）。**QA 第 5 轮逐条复核**：① S6 五次 artifact（`colds6/run-1..5.txt` + `s1_colds6-1..5.json`）与报告 §6 表逐条一致（首页 316/291/301/299/308，视频 1500/1511/1588/1487/1545，TotalTime 898/866/872/879/879）；② S2 反驳**成立**（`s2_s2aac2.json` 确有 `s2.readyAfterMs` = 682/591/528，被取代的 `s2_s2aac_sw.json` 没有该判定块）⇒ **S2 这一半是误报**，S6 那一半是真缺陷且已修复。见 `18-QA批次复验报告` §3.2/§3.3 | dev-B（第 3 轮 `worker-dev-b-3`） | QA（第 5 轮 `worker-qa-5`） |
| BUG-20260922-010 | `0.1.0 (1)` @`5f3cf3d` | S4 | 必现 | TC-P0-LOG-01 | `13-P1-8日志落盘验证.md` §2 把脱敏标为按 `docs/03 §11` 达标，实测只实现 §11 的 2/5 条规则：查询参数（`token/key/pwd/pass/sign/auth/secret/expire`）→ `***` ✅、URL userinfo → `***@` ✅，但**「URL 路径 → `scheme://host` + 8 位 hash」与「精确内网 IP → `<redacted>`」未实现**（`/iptv/live/secret-channel.m3u8`、`192.168.10.10` 原样落盘）。`UrlRedactor` KDoc 与 docs/03 §13 都把完备脱敏放在 L8/P2 ⇒ 属**文档口径夸大**，非 L3 功能缺口。证据 `evidence/BATCH1/redaction-jvm-probe.txt`（QA 临时 JVM 探针：真实 `LogBus`+`FileSink` 落盘后读文件的原始字节；探针已删、`git status` 复原为干净） | 新建（转文档批：13 报告 §2 按实测改写，并注明路径/IP 脱敏归 L8） | arch（文档）/ dev-A（L8 时实现） | QA（第 5 轮 `worker-qa-5`） |
| BUG-20260922-011 | `0.1.0 (1)` @`5f3cf3d` | S5 | 必现 | TC-P0-LOG-01 | **份数上限 12 未进文档**：`docs/03 §6` 只写「2 MB / 7 天 / 50 MB」，实现另有 `DEFAULT_MAX_FILE_COUNT = 12`（作者已在 13 报告 §6.1 自查声明的"未写死的默认值"）。且计数上限的清理会**删除当天非活跃段文件**：QA 实测在目录里放 24 份（含 1 份 21 天前的过期件 + 10 份当天段文件 + 当天 3.9 MB 段）→ 冷启动后剩 12 份，过期件删除（符合预期），**当天 3.9 MB 段与当天段文件也被删**（先删最旧）。证据 `evidence/BATCH1/c-cleanup.txt` | 新建（转文档批：§6 写死份数上限与"最旧先删"的顺序；若要保护当天数据则需改策略，等 god 裁决） | arch（文档口径）/ god（若改代码） | QA（第 5 轮 `worker-qa-5`） |
| BUG-20260922-012 | `0.1.0 (1)` @`5f3cf3d` | S5 | 必现 | TC-P0-LOG-01 | `13-P1-8日志落盘验证.md` §3 称「32 warning 中**没有一条来自本次新增代码**」不准确：QA 本轮 `check` 产出 `core/log/build/reports/lint-results-debug.xml` 有 **8 条 warning 全部落在 P1-8 新增文件**——`SdCardPath`（`file/LogFileStorage.kt` 里硬编码 `/sdcard/`）、`ButtonStyle` ×6 与 `Overdraw` ×1（新布局 `activity_log_console.xml`）。全仓 lint 仍是 **0 error / 46 warning**，不阻塞出口。证据 `evidence/BATCH1/`（本轮 `check` 的 lint XML，命令与结果见 `18-QA批次复验报告` §2） | 新建（转文档批：13 报告 §3 改为"警告类别为既有项、其中 8 条来自新增文件但不含 error"；或顺手消掉 6 条 ButtonStyle） | dev-A（可选清理）/ arch（文档） | QA（第 5 轮 `worker-qa-5`） |

| BUG-20260922-013 | `0.1.0 (1)` @`7a5b1b2` | S2 | 必现 | TC-P1-IMP-01 | **导入播放列表弹窗在电视上列不出候选文件**——`/sdcard/Android/data/ilab.iptv.player/files/playlists/real-list.m3u` 已 `adb push`（151,265 B）且 App 能列到（否则会走 `browse_import_empty` 分支），但弹窗只有「标题 + 提示文案 + 取消」，**没有任何文件行**，用户无法选中 → 导入走不下去。复现 3 次（夹具包 2 次 + as-shipped 包 1 次）。**根因**：`feature/channels/…/BrowseActivity.openImportPicker()` 用 `AlertDialog.Builder(...).setMessage(hint).setItems(labels)`；AOSP `AlertController.setupContent()`（`$ANDROID_HOME/sources/android-36.1/com/android/internal/app/AlertController.java` 第 772–801 行）只在 `mMessage == null` 的分支把 `mListView` 挂进 `contentPanel`，**message 与 items 同时用时 list 被静默丢弃**。**影响放大**：生产启动唯一播种路径是 `RoomCatalogSeeder`（表空才播内置夹具），会恢复「上次导入」的 `ChannelCatalogLoader` 没有 Hilt 注解、只给测试 —— 导入 UI 一坏，产品在电视上再没有任何途径装入真频道（内置夹具是 `p12.demo.invalid`，出不了画面）。证据：`evidence/P1G/03-after-ok.png`、`11-current.png`、`evidence/P1G/as-shipped-import-picker.png`、`uiautomator` 层级（只有 title/message/button，无 `ListView`）。**修复（dev-A 第 21 轮 `worker-dev-a-21`，`release` APK sha256 `1bd6c112…`）**：两处坏弹窗（浏览页 `openImportPicker` + 源管理页 `showImportDialog`）统一走 `:core:ui` 的 `ImportPickerDialog`——提示改走 `setCustomTitle`，列表仍走框架 `setItems`，**message 与 items 不再同用**；候选渲染抽成 `ImportPickerModel`（可单测）。真机五步复验（空目录分支、变焦到候选行、导入替换 658→8、重启仍在、源管理页同样可点）见 [30-BUG013导入弹窗修复](./30-BUG013导入弹窗修复.md) | **已关闭（QA 第 7 轮真机复验通过，2026-09-22）**：QA 独立出包（`0.1.0 (1)` @ `1437eea`，8,512,560 B）后**只用正式入口**（频道页 → 导入播放列表 → 从投放目录选择）实测：一级弹窗渲染「标题 + 提示 + 两行入口 + 取消」（v1 缺陷形态是只有标题/提示/取消、无任何行），二级候选列出 `validated.m3u · 147.7 KB · 09-22 08:34`，选中后导入成功；头行 658/670 → **571/658**，冷启动后仍 571/658；落盘日志 `DB_UPSERT{channels:571,streams:658,removed:585,streamsRemoved:82}` + `imports/last-import.json`。见 [25-P1阶段验收报告](./25-P1阶段验收报告.md) §2.2/§2.8 | dev-A（第 21 轮 `worker-dev-a-21`；待 god 集成） | QA（第 6 轮 `worker-qa-p1`） |
| BUG-20260922-014 | `0.1.0 (1)` @`7a5b1b2` | S2 | 必现 | TC-P1-PLAY-01 | **同一 URL、同一台电视、同一分钟：裸 HTTP 200，App 605**。20 条抽样里 4 条（行 183 `东丰`/`stream5.jlntv.cn`、行 273 `嘉峪关综合`/`play.kankanlive.com`、行 393 `泸县新闻综合`/`lxfile.lxxcb.cn`、行 573 `RBC`/`92.50.128.180`）在真机上稳定报 `PLAY_PREPARE_FAIL{failure=HTTP_SERVER, httpStatus=605}`（重试 2 次仍 605），而：① 电视上 `nc <host> 80` 手发同样请求 → `HTTP/1.1 200 OK`；② QA 机器 `curl` 同一 URL → 200/206；③ 从电视换各种 header（`Accept-Encoding: gzip`、`ExoPlayerLib` / `Dalvik…BRAVIA` UA、`keep-alive`、同连接连发两次、`HTTP/1.0`、无 header）→ **全部 200**。即请求内容不是变量，变量在 App 的 HTTP 栈。**未定因**：该网段所有域名解析到 198.18.0.0/15（fake-IP），HTTP 经 `iLabRouter` 中转，不排除中转件对 App 侧连接与裸 socket 处理不同 → 需在正常网络复跑这 4 条定性。证据：`evidence/P1G/item1-row{183,273,393,573}.log`、`item1-retry.csv`、`nc-probe.txt`、`net-probe.txt` | **已关闭（QA 第 7 轮复验，2026-09-22）＝ 非应用缺陷**：dev-B 定性报告（[31-BUG014-HTTP605定性](./31-BUG014-HTTP605定性.md)）证明 605 是本网段路径上真实存在的返回码、网段按 fake-IP 路由而非 Host、请求内容不是变量；人工已裁决起播率改用「方式 B」并把这条网络线升格为 `NET-1` 专项。**QA 第 7 轮独立复核**：本轮 43 条抽样里 7 条 `HTTP_SERVER 605` **全部来自同一台主机 `hlsztemgsplive.miguvideo.com:8080`**，而 QA 机器**裸 `curl` 同一 URL 也 605（TTFB 41–57 ms、0 字节）**；上轮同组 4 条里的 `play.kankanlive.com`、`92.50.128.180` 本轮在 App 内**起播成功（2,420 / 2,393 ms）**，与「概率性反盗链闸门」一致。见 [25-P1阶段验收报告](./25-P1阶段验收报告.md) §2.1/§2.9 | 无（环境/源侧，转 `NET-1` 与源清单质量） | **QA（第 7 轮 `worker-qa-p1b`）** |
| BUG-20260922-015 | `0.1.0 (1)` @`1437eea` | S3 | 必现 | TC-P1-IMP-01 | **导入不是整体替换：同名频道的旧清单流被保留**（界面「流」计数大于清单流数、旧源继续当故障转移备胎）。原报告场景：8 频道/10 流清单叠在内置夹具（658 频道/670 流）上 → 界面显示「流 22」、`CCTV1 · 3 路流`。根因：`RoomCatalogWriter.write()` 频道按 `(name_key,group_key)` upsert **保留原行**（为保住收藏/隐藏/排序/频道号），流按 `(channel_id,url_hash)` upsert **只增不删**，只有「本次目录里不存在的频道」被删并靠外键级联带走其流 → 同名频道的旧流全部留下。**修复（dev-A 第 24 轮 `worker-dev-a-24`，卡 `BUG-STALE-STREAM`）**：`write()` 收集 `dbChannelId → 新 url_hash 集合`，在 `pruneChannelsNotIn` **之后**调 `pruneStreamsNotIn()`（`StreamDao.allIdentities()` 一次读全表 + Kotlin 侧 diff + 400/批删除），日志新增 `streamsRemoved`；8 条单测走真实导入链路。口径：**本地导入/夹具播种 = 整体替换**（流集合以新目录为准、用户字段保留），订阅刷新仍是甄选语义不变。**QA 第 7 轮真机复验**：正式入口导入 `validated.m3u` → 658 频道/670 流 → **571/658（`removed:585, streamsRemoved:82`）**；再导 43 频道/44 流清单 → **43/44（`removed:528, streamsRemoved:10`）**；界面「流」计数与清单逐次一致（不再出现「导入 10 流、列表 22 流」），全表无 `p12.demo.invalid` 残留（流集合整体替换后条数恰好等于清单）。证据：[25-P1阶段验收报告](./25-P1阶段验收报告.md) §2.2/§2.3、[35-导入替换语义修复](./35-导入替换语义修复.md) | **已关闭（QA 第 7 轮真机复验通过，2026-09-22）** | dev-A（第 24 轮 `worker-dev-a-24`） | **QA（第 7 轮 `worker-qa-p1b`）** |
| BUG-20260922-016 | `0.1.0 (1)` @`b533c3e` | S2 | 必现 | TC-V1-EPG-02 | 新装冷启动 EPG 触发**早于频道表播种**（EPG_COVERAGE matched=0/total=0，白跑 26.3 s），且 6 h 新鲜度闸门**只看时间不看「上次跑出 0」** → 之后每次冷启动 SKIP(fresh)，**新装 6 小时内电视没有任何节目单**；手动刷新后正常（matched 140/571、主流 91/107）。证据 `evidence/VERIFY1/`（EPG_COVERAGE 事件序列 + 时间戳） | 新建 | dev-A | QA（VERIFY-1） |
| BUG-20260922-017 | `0.1.0 (1)` @`b533c3e` | S2 | 必现 | TC-V1-UI-01 | 信息条左侧文本列被 P3-3 的四个控件挤到 **209 px**（uiautomator bounds 证据）：频道名/清晰度/now-next 全部截断；且同一会话内该行 10:02 渲染后 10:07 变 `View.GONE` | 新建 | dev-B | QA（VERIFY-1） |
| BUG-20260922-018 | `0.1.0 (1)` @`b533c3e` | S2 | 必现 | TC-V1-EPG-03 | **「有节目」口径与网格不一致**：`LoadEpgUseCase` 用 `[now−6h, now+48h]` 计数，网格只画 6 h（代码注释却称两者一致）→ 抽查 CCTV1/CCTV-12/CGTN俄语/CCTV-11 网格全空、`EPG_MATCH_HIT` 却报 programmesInWindow=84/71/10/42 | 新建 | dev-A | QA（VERIFY-1） |
| NEW-20260922-001 | `0.1.0 (1)` @`88d5336` | **S1** | 必现 | TC-P2-WIZ-01 | **首启向导第 1 步把 17 个内置源清单铺满，4 个按钮被父容器裁到屏外且无 ScrollView** → 遥控 DOWN/OK 无效、只剩 BACK 退出 → **全新安装没有任何路径进频道表**，且完成标记不写 → 每次启动都回向导。复现：`pm clear` + `am start MainActivity` | **待验证（dev-A 第 31 轮 `worker-dev-a-31`，待 god 集成）**：采用派单推荐方案 ②——第 1 步的 17 行内置源收成**一行摘要行**（「已启用 N 个内置源 · 查看清单」，可聚焦、按 OK 进二级页），清单本身移到只读二级页 `wizard_step_sources`（17 个可聚焦行 + 独立滚动容器）；每个面板改成「`ScrollView`(weight 1) + `wrap_content` 按钮页脚」，按钮**不在滚动区内** ⇒ 任何高度都先被布局、始终在首屏（720p=640×360dp 也成立，`WizardPanels` 用常数预算、`WizardPanelLayoutTest` 走 DOM 逐条钉住）。另按派单项 3 让向导**记住上次步骤**（`FirstRunStore.savedProgress/saveProgress`，走完三步仍才写完成标记）。见 [51-NEW1首启向导修复](./51-NEW1首启向导修复.md) | dev-A（第 31 轮 `worker-dev-a-31`） | QA |
| NEW-20260922-002 | `0.1.0 (1)` @`88d5336` | S3 | 必现 | TC-P2-EPG-01 | 全新安装后**首次 EPG 最早就发生在 30 min WorkManager backoff 到期**（实测 12:17:47→12:51:08），与 46 号文档「若期间又冷启动…通常几秒」不符 | 新建 | dev-A | QA（第 9 轮） |
| NEW-20260922-003 | `0.1.0 (1)` @`cd3d466` | **S1** | 必现 | TC-P2-REF-01 | **任何一次刷新跑起来就会把 App 进程打崩**：刷新走 WorkManager 长任务前台路径（`RefreshNotifications.CHANNEL_ID = "refresh"`，通知 id 12023），但 `RefreshNotifications.ensureChannel()` **全仓无调用点**（`rg -n ensureChannel` 只命中定义与播放侧 `PlaybackService`），**渠道 `refresh` 从未被创建** → `SystemForegroundService` 的 `startForeground` 抛 `RemoteServiceException: Bad notification for startForeground`，异常在主线程抛出、`RefreshWorker.postForeground` 的 try/catch 拦不到 → 进程崩溃。系统侧证据：`NotificationService: No Channel found for pkg=ilab.iptv.player, channelId=refresh, id=12023` + `FATAL EXCEPTION: main … RemoteServiceException: Bad notification for startForeground` + `Force finishing activity …WizardActivity`。**影响**：① 首启向导主按钮「使用内置源并继续」= 进第 2 步并自动起更新 ⇒ **按下即闪回桌面**，正常路径走不完向导；② 每次 06:00 定时刷新、以及任何手动刷新都会崩；③ 崩后 WorkManager 把该作业按 30 min 线性退避重排（`dumpsys jobscheduler`：`Minimum latency: +29m2s981ms`）→ 每次重试再崩一次；④ **刷新管线在真机上从未跑完**（全天日志 `SRC_REFRESH_DONE` = 0）。复现：`pm clear` → `am start MainActivity` → `input keyevent 23`（或诊断面板「立即更新」以外的任一刷新入口） | **待验证（dev-A 第 32 轮 `worker-dev-a-32`，待 god 集成）**：原证据 `evidence/P2Gv3/crash-notification-channel.txt`（26 号文档 §6.3 把「通知渠道 refresh」列为待验项，真机从未验过，QA 第 10 轮首次命中）。**修法与理由**：渠道创建**收敛到刷新任务起跑处**——`RefreshWorker.doWork` 在第一次 `setForeground` 之前调用 `RefreshNotifications.ensureChannel`（三个入口全经此 worker 收口、时序即证明、与播放侧 `PlaybackService.onCreate` 同形；**不只在 catch 里兜**），并在渠道拿不到时**降级不崩**（`SERVICE_REFRESH_START{result=failed, reason=channel_unavailable}`，整轮不发通知照跑）。事件码**未新增**：复用已注册的 `SERVICE_REFRESH_START`，补 `result=ok/failed` + `reason`（与 `SERVICE_PLAYBACK_START` 同形）。**同类排查**：全仓仅 2 个渠道构造点、2 个前台调用点，逐条核对无第三处（无 `NotificationManagerCompat` / `.notify(`）；新增静态守护 `tools/ci/notification-channel-audit.sh`（三条规则 + 自测；负向对照在修复前的树上报出 `ensurer-never-called` + `foreground-without-ensure`）并接入 CI。**本轮数字**：`check` BUILD SUCCESSFUL（1281 tasks）、`:app:lintDebug` 0 error / 7 warning（既有类别）、单测 debug/release 992/0/2（新增 4 条）、脱敏守护与焦点审计均 OK。真机复跑步骤见 [53-NEW3通知渠道崩溃修复](./53-NEW3通知渠道崩溃修复.md) §6 | dev-A（第 32 轮 `worker-dev-a-32`） | QA |

> 首轮登记（2026-09-21，P0-5 真机验收）。缺陷详情与命令原文见 [06-P0真机验收报告](./06-P0真机验收报告.md)。
> 复验轮更新（2026-09-21，`02e84f8` 修复包，卡 `QA-P0FIX`）：BUG-005 已关闭；001/002 转文档批 `DOC-2`（文字未改）；003 覆盖面已修复、首跑待推送；004 转文档口径。逐条结论与命令原文见 [09-P0修复复验报告](./09-P0修复复验报告.md)。
> 批次复验轮更新（2026-09-22，`5f3cf3d`，卡 `QA-BATCH-1`）：**BUG-008 已关闭**（QA 独立复算百分位，与报告逐条一致；并勘误本表原来的 `3282` → `3313`）；**BUG-009 已关闭 = 部分误报**（S2「到 READY」字段一直存在，是 QA 看了被取代的 artifact；S6 那半是真缺陷且已修复，5 次 artifact 与数字逐条吻合）。新增 BUG-010/011/012（均为口径/范围类，S4–S5）。逐条结论与证据见 [18-QA批次复验报告](./18-QA批次复验报告.md)。
> 阶段验收轮更新（2026-09-22，`7a5b1b2`，卡 `P1-G`）：**新增 BUG-013（S2，导入弹窗不显示候选文件 → 电视上无法装入真频道）与 BUG-014（S2，已验证源以 HTTP 605 起播失败，而同设备同 URL 裸请求 200，根因待定性）**。逐条判据、数值与证据见 [25-P1阶段验收报告](./25-P1阶段验收报告.md)。
> 第三次复跑轮更新（2026-09-22，`cd3d466`，卡 `QA-P2G-3`，temp `worker-qa-p2g-3`）：
> - **NEW-20260922-001（首启向导 S1）→ 已关闭**。真机 `pm clear` 复验：第 1 步 **4 个按钮全部在屏内**（1080p `[96,824]–[1824,1032]`，720p `[96,464]–[1184,672]`）、焦点落主按钮；摘要行 OK 进只读二级页、BACK 回第 1 步；`am force-stop` 后重启**落回离开的那一步**（第 2 步）；走完三步（第 2 步用「跳过更新」绕开 NEW-003 的崩溃）后冷启动直接进浏览页、向导不再出现；空表分支（隐藏全部频道后）主按钮 `enabled=false`、焦点落「导入本地清单」。
> - **NEW-20260922-002（首次 EPG 30 min）→ 已关闭**。首启冷启动时间线：`WORK_SCHEDULE epg DEFER(catalog_empty)` → `WORK_RUN` 内**有界等待**（`catalogWaitMs=2000`）→ 播种后 **+3.7 s** 即 `EPG_FETCH_OK`×4 → `EPG_COVERAGE matched=209/withProgrammes=203`（不再等 30 min）。
> - **BUG-20260922-018 → 保持打开**。用 52 号新入口（诊断面板「EPG 绑定与窗口」）逐台读：`#1 CCTV1 id=CCTV-1.hk 窗口内 9 条 NAME_FUZZY`、`#30 CCTV-11戏曲 id=545943 6 条`、`#31 CCTV-12社会与法 id=545944 9 条`、`#41 CGTN俄语 id=540038 13 条`（与 52 号本机采样逐条一致）；**同一时刻同一 6 h 窗（13:00–19:00）的 EPG 网格里，CCTV1 与 CCTV-12社会与法整行仍空**，而相邻的 #11/#21/#41 有块 ⇒ 52 号 §3.3 的「网格没载入该页」分支被排除（同页有块），口径与所见仍不一致。
> - **新增 NEW-20260922-003（S1，刷新前台服务崩溃）**，见上表。
> 修复轮更新（2026-09-22，dev-A 第 21 轮 `worker-dev-a-21`，卡 `BUG013-FIX`）：**BUG-013 已修，状态 `新建` → `待验证`（验证权仍归 QA，未关闭）**——`feature/channels` 与 `feature/settings` 两个导入入口统一改走 `:core:ui` 的 `ImportPickerDialog`（提示进自定义标题、列表仍用框架 `setItems`），候选渲染抽成 `ImportPickerModel` 并补 7 条单测；真机五步（空目录分支 / 候选多行 / 导入后 658 → 8 且移除 650 / 重启仍在 / 源管理页同一弹窗可点）见 [30-BUG013导入弹窗修复](./30-BUG013导入弹窗修复.md)。**同轮发现一条候选缺陷（编号待 QA 登记）**：导入后界面「流」计数大于本条清单的流数（8 频道清单导入后显示 22 条流、单行 3–4 路流），根因是 `RoomCatalogWriter` 按 `(name_key, group_key)` upsert 频道、`(channel_id, url_hash)` upsert 流且**只为已删频道级联删流**，同名旧频道留下的流会继续挂在同一行上（刷新语义如此设计，导入场景是否要清掉待 god/arch 裁决）；证据与排除实验见 30 号文件 §6。
> **第 7 轮复跑更新（2026-09-22，基线 `1437eea`，卡 `P1-G` 复跑 · QA `worker-qa-p1b`）**：**BUG-013 已关闭**（正式入口实测：一级弹窗渲染出候选行、二级列表可选，导入后 658 频道/670 流 → 571/658，`am force-stop` 冷启动后仍 571/658）；**新增 BUG-20260922-015（S3，导入替换语义：同名频道旧流不清理）并已关闭**（真机两次导入的「流」计数与清单逐次一致、`streamsRemoved` 命中旧流、无 `p12.demo.invalid` 残留）；**BUG-014 已关闭＝非应用缺陷**（本轮 43 条抽样里 7 条 `HTTP_SERVER 605` 全部来自 `hlsztemgsplive.miguvideo.com:8080`，QA 机器裸 `curl` 同一 URL 同样 605；上轮同组 4 条里 `play.kankanlive.com`、`92.50.128.180` 本轮在 App 内起播成功）。→ **P1 未关闭 S1/S2 = 0**。逐条数值与口径见 [25-P1阶段验收报告](./25-P1阶段验收报告.md) v2。
> **修复轮更新（2026-09-22，dev-A 第 32 轮 `worker-dev-a-32`，卡 `NEW-3`）**：**NEW-20260922-003 已修，状态 `新建` → `待验证`（验证权仍归 QA，未关闭）**——刷新前台通知的渠道创建从「定义了但没人调」改为**在刷新任务起跑处调用**（`RefreshWorker.doWork` 第一次 `setForeground` 之前 `RefreshNotifications.ensureChannel`，与播放侧 `PlaybackService.onCreate` 同形），并新增**渠道不可用时的降级**（`SERVICE_REFRESH_START{result=failed, reason=channel_unavailable}`，发不出通知也不崩进程）；全仓同类排查（2 个渠道构造点 / 2 个前台调用点，无第三处）见 [53-NEW3通知渠道崩溃修复](./53-NEW3通知渠道崩溃修复.md)；配套静态守护 `tools/ci/notification-channel-audit.sh` 已接入 CI，负向对照能在修复前的树上报出本缺陷。真机复跑清单见 53 号 §6（QA）。

### 3.1 示例（**格式示例，不是真实缺陷**）

| ID | 版本 | 严重级 | 复现率 | 关联用例 | 证据 | 状态 | 修复人 | 验证人 |
|---|---|---|---|---|---|---|---|---|
| BUG-20260921-001 | 0.1.0 (1) @ abc1234 | S2 | 必现 | TC-P1-PLAY-03 | `PLAY_FAILOVER` 缺失；logcat 原文；录屏 `failover-01.mp4`；主源 403 时未切备胎 | 待验证 | dev-B | QA |

---

## 4. 统计口径（每阶段出口填写）

| 阶段 | 新增 | 已关闭 | S1/S2 未关闭 | 平均修复时长 | 结论 |
|---|---|---|---|---|---|
| P0 | 5 | 1（BUG-005） | 0 | —（同轮修复，未计时长） | 复验轮见 09 报告：8 项修复中 7 项实测生效、CI 静态达标但运行未判；出口 3（L1 日志）仍不通过（`core/log` 0 源码），出口 4（CI 首绿）待 god 推送触发 |
| P1 | 5（第 5 轮 BUG-010/011/012 为口径/范围类；**第 6 轮 +BUG-013 S2 导入入口不可用、BUG-014 S2 已验证源 HTTP 605**） | 2（BUG-008、BUG-009 于第 5 轮批次复验关闭） | **2（BUG-013、BUG-014）** | —（同轮登记，未计时长） | QA 第 5 轮（卡 `QA-BATCH-1`）：批次三项全部通过复验；`check` 绿（单测 395 条 0 失败、lint 0 error）。**QA 第 6 轮（卡 `P1-G` 取证，报告 `25-P1阶段验收报告.md`）**：7 项必测 **4 通过 / 1 有条件 / 2 不通过** —— 起播成功率 60%（剔除域名不可解析的 4 条源后 75%）< 90%、导入入口不可用；换台 30/30（avg 1,220.5 / p95 2,197 / max 2,326 ms）、故障转移失败→画面 487 ms、30 次换台 PSS −1,990 KB、系统集成回归全过；长稳只做了 45 min 采样。**按 §5 放行规则当前不满足签 `P1-G`。** |
| P2 |  |  |  |  |  |
| P3 |  |  |  |  |  |

---

## 5. 状态流转与规则

```
新建 ──确认──▶ 已确认 ──▶ 修复中 ──提交修复──▶ 待验证 ──QA 复测通过──▶ 已关闭
                                  ▲                 │
                                  └── 复测不通过 ── 重开（计数 +1）
                （S1/S2 停线优先修；挂起需 god 记录理由）
```

- **关闭权**：只有 QA 能关闭；关闭必须附新证据（重跑用例 + 版本三连）。
- **重开**：同一用例重开 ≥2 次 → 严重级升一级，并回 G4 做代码评审复盘。
- **挂起**：`wontfix` 或需人工决策时由 god 记录理由；S1/S2 不允许挂起。
- **脱敏泄露**：一律 S1，当轮修，且要复检导出包与历史日志。
- **与出口的关系**：G6/G7 放行要求 S1/S2 未关闭数为 0；S3 必须有明确排期（见 03 §10）。
