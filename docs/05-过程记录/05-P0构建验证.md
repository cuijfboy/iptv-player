# 05 · P0 构建验证记录

> 任务：P0-1 工程基座（Gradle wrapper + 多模块 + Hilt/Room/Media3 + 签名 + CI）
> 作者：dev-A（Jim）。**第 2 轮（2026-09-21，沙箱已联网）**：第 1 轮只交付了骨架文件、没有构建证据；
> 本轮把骨架跑成**可安装的签名 APK**，下面每条命令、结果、耗时都是**实测**。
> 相关：[04 开发计划与验收](../04-开发计划与验收.md) · [02 技术架构设计](../02-技术架构设计.md)

---

## 1. 结论（先看这段）

**`tools/bootstrap-gradle.sh` + `assembleDebug` / `testDebugUnitTest` / `lint` / `assembleRelease` / `check`
五条命令全部跑通，签名 APK 已产出。**

| 产物 | 路径 | 大小 | SHA256 |
|---|---|---|---|
| Release（已签名） | `app/build/outputs/apk/release/app-release.apk` | 6,844,635 B | `bfe79fc79fe815ba127e8265613c8326c690c8e76a71ce967752675f1f3cb85e` |
| Debug | `app/build/outputs/apk/debug/app-debug.apk` | 9,951,726 B | `25f2edc27eda5328a3fd5736418f27a8e2256e0a5de5d35649d55ac2bf2e2de0` |

- 签名自检：`apksigner verify --print-certs` → **v1(JAR) + v2 均通过**，证书 `CN=IPTV Player, OU=Android, O=ilab, L=Shanghai, ST=Shanghai, C=CN`，RSA 2048，证书 SHA-256 `a98e8d4bbc7a895888502ceecad742729e476d16aa22f8e5f9f9c1d47b259004`。
- 版本组合（AGP 8.11.1 / Kotlin 2.1.21 / KSP 2.1.21-2.0.1 / Hilt 2.56.2 / Room 2.7.1 / Media3 1.6.1）**按实际解析全部可用，一个版本都没改**。
- APK 清单：`minSdk 21 / targetSdk 35 / compileSdk 36`，含 `LEANBACK_LAUNCHER` 电视入口 + 手机 `LAUNCHER`，`android.software.leanback` 必须、触摸屏非必须，APK 内**无 `.so`**。
- 本轮修了 **3 个真实构建缺陷**（§5）：build-logic 编译不过、`:app` 缺 Hilt 运行时依赖、lint `MissingTvBanner` 报错。
- 纯 Kotlin 守护**实测有效**（§4.3）：临时加 `import android.os.Bundle` → `check` 失败；撤销后 `check` 通过。

第 1 轮"骨架已落地但构建证据缺失"的结论**作废**，以本文件为准。

---

## 2. 环境实查（第 2 轮实测，与第 1 轮对比）

| 项 | 第 1 轮 | 第 2 轮（本轮） |
|---|---|---|
| 网络 | 沙箱禁网（DNS syscall 被拒）❌ | **可用**：`services.gradle.org` / `dl.google.com` / `repo1.maven.org` 均 200 ✅（下载速率约 0.3–1.5 MB/s） |
| Gradle | 无（`which gradle` 空） | 由 `tools/bootstrap-gradle.sh` 下载 8.13 发行包生成 wrapper ✅ |
| 依赖缓存 | `~/.gradle` 不存在 | 首次构建后约 **1.5 GB**（AGP + Kotlin + KSP + Hilt + AndroidX 全家桶） |
| JDK | JBR 21 可用；`/usr/bin/java` 是 stub | 同上。**注意：`./gradlew` 自己也要求 `JAVA_HOME`**（启动 JVM 用），`gradle.properties` 的 `org.gradle.java.home` 只管构建 JVM |
| Android SDK | 只有 `platforms/android-36.1`、`build-tools/36.1.0+37.0.0` | 首次构建时 AGP **自动补装** `platforms/android-36` 与 `build-tools/35.0.0`（`compileSdk = 36` 需要） |

本轮所有命令都先执行：

```bash
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
```

---

## 3. 已交付（第 1 轮骨架，本轮验证有效）

| 交付点 | 文件 |
|---|---|
| 版本目录（唯一版本来源） | `gradle/libs.versions.toml` |
| 根构建 | `settings.gradle.kts`（`includeBuild("build-logic")` + 20 个模块）、`build.gradle.kts` |
| JDK 指向 JBR | `gradle.properties` → `org.gradle.java.home=/Applications/Android Studio.app/Contents/jbr/Contents/Home` |
| 约定插件（included build） | `build-logic/`：`iptv.kotlin.library`、`iptv.android.library`、`iptv.android.feature`、`iptv.hilt`、`iptv.room` |
| 纯 Kotlin 禁 Android 的构建期守护 | `verifyPureKotlin`（挂在 `check`）+ CI grep 前置守卫 |
| 多模块骨架 | `:app` + `core/*`×13 + `feature/*`×5（共 20 个模块，全部参与构建） |
| 依赖接入 | Hilt + KSP、Room、Media3/ExoPlayer（均实测可解析、可编译） |
| 可启动入口 | `MainActivity`（`@AndroidEntryPoint`）+ `IptvApplication`（`@HiltAndroidApp`）+ 电视 banner/入口 |
| 签名 | `app/build.gradle.kts`：密钥文件入库、口令只从 `local.properties`/环境读（ADR-003） |
| wrapper 引导脚本 | `tools/bootstrap-gradle.sh`（本轮实跑通过） |
| CI | `.github/workflows/ci.yml`（**本轮未在 CI 上验证**，见 §6） |

---

## 4. 构建验证（本轮实测）

### 4.1 命令 / 结果 / 耗时

| # | 命令 | 结果 | 耗时 | 产物 / 证据 |
|---|---|---|---|---|
| 1 | `bash tools/bootstrap-gradle.sh`（第 1 次） | **失败** ❌ | 4 min 48 s | `build-logic` 脚本编译报 4 个 `Unresolved reference: findLibrary`，wrapper 未生成（§5-1） |
| 2 | 同上（修 build-logic 后重跑） | **成功** ✅ | 1 min 33 s | 下载 gradle-8.13-bin.zip → `:wrapper UP-TO-DATE` → `./gradlew --version` 打印 `Gradle 8.13` |
| 3 | `./gradlew assembleDebug`（冷） | **成功** ✅ | 4 min 30 s | `app/build/outputs/apk/debug/app-debug.apk`（433 tasks） |
| 4 | `./gradlew testDebugUnitTest` | **成功** ✅ | 8 s | `app/build/test-results/testDebugUnitTest/TEST-ilab.iptv.player.PlaceholderTest.xml` → `tests=1 failures=0 errors=0` |
| 5 | `./gradlew lint`（第 1 次） | **失败** ❌ | 45 s | 1 error：`MissingTvBanner`（§5-3）；27 warning |
| 6 | `./gradlew lint`（补 banner 后） | **成功** ✅ | 8 s | `app/build/reports/lint-results-debug.html`；**0 error** / 27 warning（全是依赖有新版、targetSdk 35、锁定横屏、ChromeOS ABI 之类的提示） |
| 7 | `./gradlew assembleRelease` | **成功** ✅ | 1 min 33 s | `app/build/outputs/apk/release/app-release.apk`，**已用 release 密钥签名** |
| 8 | `apksigner verify --print-certs app-release.apk` | **通过** ✅ | <1 s | v1 + v2 通过，1 个签名者，证书见 §1 |
| 9 | `aapt2 dump badging app-release.apk` | **通过** ✅ | <1 s | `ilab.iptv.player` / minSdk 21 / targetSdk 35 / `leanback-launchable-activity` / `banner` 存在 / 无 native code |
| 10 | 终验：`assembleDebug`+`testDebugUnitTest`+`lint`+`assembleRelease`+`check`（热缓存） | **全部成功** ✅ | 4 / 4 / 4 / 4 / 4 s | 全部 `BUILD SUCCESSFUL`，无 FAILED 任务 |

### 4.2 构建期守护与依赖矩阵（实测）

| 检查 | 命令 | 结果 |
|---|---|---|
| 纯 Kotlin 模块的 Android 导入 | `./gradlew :core:common:verifyPureKotlin` 等 | `verifyPureKotlin: OK (:core:common / :core:domain / :core:model)` ✅ |
| 依赖守护规则 3（`:app` 不直接依赖 database/network/source/epg/player） | `./gradlew -q :app:dependencies --configuration debugRuntimeClasspath` | `:app` 直接依赖只有 `core:common/log/model/domain/data/ui/design` + `feature:*`；`core:database`、`core:network`、`core:source`、`core:epg`、`core:player` **只作为 `:core:data` 的传递依赖出现** ✅ |
| 依赖守护规则 4（只有 `:feature:player` 可见 `:core:player`） | `./gradlew -q :feature:channels:dependencies --configuration debugRuntimeClasspath` | `:feature:channels` 可见的工程只有 `core:common/design/domain/log/model/ui`，**看不到 `:core:player`** ✅ |

### 4.3 纯 Kotlin 守护的反证实验（本任务第 5 项）

1. 临时新增 `core/domain/src/main/kotlin/ilab/iptv/player/core/domain/PurityGuardProbe.kt`，内容含 `import android.os.Bundle`。
2. `./gradlew :core:domain:verifyPureKotlin` → **失败**，输出：

   ```
   > Purity guard failed: :core:domain is a pure-Kotlin module but imports Android:
       src/main/kotlin/ilab/iptv/player/core/domain/PurityGuardProbe.kt  ->  android.os.Bundle
   ```

3. `./gradlew check` → **失败**（2 个失败：`verifyPureKotlin` 与 `:core:domain:compileKotlin`，后者因为纯 Kotlin 模块没有 Android 类路径，`android.os.Bundle` 本来就解析不到 —— 守卫生效时编译也会同时失败）。
4. **删除探针文件**，`./gradlew check` → **成功**（`verifyPureKotlin: OK` ×3，`BUILD SUCCESSFUL in 7s`）。工作区已确认无残留（`find core/domain -name '*.kt'` = 0）。

---

## 5. 失败与修复（本轮真实修掉的构建缺陷）

| # | 失败 | 根因 | 修复 | 状态 |
|---|---|---|---|---|
| 1 | `build-logic/build.gradle.kts`：4 × `Unresolved reference: findLibrary`，导致 `tools/bootstrap-gradle.sh` 连 wrapper 都生成不出来 | Gradle 8.13 为版本目录生成的是**只含类型化别名**的访问器，`libs.findLibrary(...)` 在 included build 的根构建脚本和预编译约定插件里都不可用 | 6 个文件改为显式取目录对象：`extensions.getByType<VersionCatalogsExtension>().named("libs")`，再 `catalog.findLibrary(...)` | 已修（实测通过） |
| 2 | `:app` 里 `@HiltAndroidApp` / `@AndroidEntryPoint` 无法解析（编译期） | Hilt 的 Gradle 插件**不会**自动添加 `hilt-android`（实现）与 `hilt-compiler`（KSP）依赖，只套 `alias(libs.plugins.ksp/hilt)` 不够 | `:app` 改套约定插件 `id("iptv.hilt")`（它同时给实现依赖 + KSP 处理器） | 已修（实测通过） |
| 3 | `./gradlew lint` 失败：`AndroidManifest.xml:15 Error: Expecting android:banner ... [MissingTvBanner]` | 带 `LEANBACK_LAUNCHER` 的电视应用必须有首页 banner，`abortOnError = true` 下这是 error | 新增占位 banner `app/src/main/res/drawable/app_banner.xml`，并在 `<application>` 上挂 `android:banner`（正式 320×180 美术资源留到 P1 设计系统） | 已修（lint 0 error） |
| 4 | `verifyPureKotlin` 的报错文案写成 `:core:domain:verifyPureKotlin is a pure-Kotlin module` | 任务动作里 `path` 取到的是**任务路径**而不是模块路径 | 在工程作用域先取 `guardedModulePath` 再传进任务 | 已修（现在输出 `:core:domain is a pure-Kotlin module`） |
| 5 | 上一轮的 3 条环境阻塞（无 Gradle / 无网络 / 无缓存） | 沙箱禁网 | 本轮沙箱放开后自动消除；`tools/bootstrap-gradle.sh` 一次跑通 | 已闭环 |

**未做（按 dispatch 边界）**：不装到电视、不 push、不 git commit —— 提交由 god 统一做。

---

## 6. 未解决 / 需要 god 或 QA 接手

1. **CI 未验证**：`.github/workflows/ci.yml` 本轮没有跑（本机没有 GitHub runner）。风险点已排除一半：CI 用 JDK 17 + `-Dorg.gradle.java.home="$JAVA_HOME"` 覆盖 macOS 路径；但 `setup-android` 给的 SDK 是否含 `platforms/android-36` 与 `build-tools/35.0.0` 仍需一次真实 CI 运行确认。
2. **lint 的 27 条 warning** 未处理（按 `abortOnError = true` 不阻断）：以"依赖有更新版本"为主（Room 2.8.5、Media3 1.11.1、AGP 9.4.1、Kotlin 2.4.20 等），另有 `OldTargetApi`（targetSdk 35 vs 36，按 docs/02 决定）、`DiscouragedApi`（锁定横屏，Android 16 起失效）、`ChromeOsAbiSupport`（只出 armeabi-v7a）。是否升版本/改配置请 god 拍定，本轮**不改版本**。
3. **banner 是占位图形**：正式 320×180 美术资源随 P1 设计系统（`:core:design`）补。
4. **本机跑 `./gradlew` 必须先 `export JAVA_HOME=...`**（`/usr/bin/java` 是 stub），否则报 `Unable to locate a Java Runtime`。建议后续把这行写进团队 README 或包一层 `./run` 脚本。
5. **真机安装/启动属 QA**：`adb connect <电视 IP>:5555` → `install -r app-release.apk` → `am start -n ilab.iptv.player/.MainActivity`。本轮未执行，也未验证启动后无崩溃。
6. **纯 Kotlin 模块包名**：按 dispatch 已批准口径（完整模块路径，如 `ilab.iptv.player.core.common`）定稿，约定插件从模块路径推导，本轮构建实测通过，无需再改。

---

## 7. 本轮改动文件（工作区改动，未提交）

| 文件 | 改动 |
|---|---|
| `build-logic/build.gradle.kts` | 版本目录改用 `VersionCatalogsExtension` 取 `catalog`（修缺陷 1） |
| `build-logic/src/main/kotlin/iptv.{android.feature,android.library,hilt,kotlin.library,room}.gradle.kts` | 同上（5 个约定插件） |
| `build-logic/src/main/kotlin/iptv.kotlin.library.gradle.kts` | 另外修 `verifyPureKotlin` 的模块名文案（缺陷 4） |
| `app/build.gradle.kts` | `:app` 改套 `id("iptv.hilt")`（修缺陷 2） |
| `app/src/main/AndroidManifest.xml` | `<application>` 增加 `android:banner`（修缺陷 3） |
| `app/src/main/res/drawable/app_banner.xml` | 新增占位 banner |
| `gradlew`、`gradlew.bat`、`gradle/wrapper/gradle-wrapper.jar` | 由 `tools/bootstrap-gradle.sh` 生成（wrapper jar 已在 `.gitignore` 白名单内） |
| `docs/05-过程记录/05-P0构建验证.md` | 本文件：把第 1 轮的"未完成"改成实测结果 |

---

## 8. 修复包验证（第 3 轮，2026-09-21）

> 输入：CodeReview `08-CodeReview-P0.md`（CR-01/04/05/06/07/11/13）+ QA `06-P0真机验收报告.md` / `04-缺陷台账.md`（BUG-003/005）。
> 基线：`3d3d33d`（worktree 分支 `agent/worker-dev-a-3`，已 `git merge --ff-only main`）。
> 环境：JBR 21（`JAVA_HOME` 显式指定）+ Android SDK（`ANDROID_HOME`）+ `~/.gradle` 暖缓存（8.13 / 依赖已就绪）。
> 本节的每条命令都是**实跑**；反证实验改完即撤回，`git status` 与改动前一致（除本节列出的正式改动）。

### 8.1 逐条修复与验证

| # | 项 | 状态 | 改动点（文件:行） | 验证命令 → 结果 |
|---|---|---|---|---|
| 1 | **CR-01** 依赖规则 2–5 构建期守护 | ✅ 已修 | 新增 `build-logic/src/main/kotlin/iptv.module.dependencies.gradle.kts`（规则表 `:35–43`、只读"声明桶"的采集 `:55–66`、环检测 `:68–94`、守护任务 `:96–166`、挂 `check` `:168–170`）；`build.gradle.kts:14` 应用 `id("iptv.module.dependencies")` | `./gradlew verifyModuleDependencies` → `OK (19 modules; no Android dependency from a pure-Kotlin module, no forbidden direct dependency, no api exposure of [core:database/network/source/epg/player], no cycles)`；`./gradlew check` → SUCCESS。**反证**：见 §8.2（A–E 五组） |
| 2 | **CR-04** `AppError` 补 4 个工厂 | ✅ 已修 | `core/common/src/main/kotlin/ilab/iptv/player/core/common/AppResult.kt:73–102`（`tls` / `emptyMedia` / `playlistGone` / `cancelled`，`retryable` 按 docs/02 §4.6 全为 `false`） | `./gradlew :core:common:compileKotlin` → SUCCESS（随 `check` 一起跑）；映射逐条对齐 §4.6 |
| 3 | **CR-05** `Logger` 便捷方法不再伪造事件 | ✅ 已修 | `core/common/.../Logger.kt:32–73`：`v/d/i/w/e` **去掉默认实现改为抽象**，包层 KDoc 写清信封契约（`seq`/`ts`/`elapsedMs`/`thread`/`sessionId` 必须由实现从 `Clock`/`SessionIdFactory`/当前线程取真值，取不到就不发事件）；顺手修掉同文件把日志骨架写成 P0-2 的过期注释 | `./gradlew check` → SUCCESS。语义理由见 §8.4 |
| 4 | **CR-06** 明文 HTTP 放行 | ✅ 已修 | `app/src/main/AndroidManifest.xml:23` 增 `android:usesCleartextTraffic="true"` | `aapt2 dump xmltree …AndroidManifest.xml` → `usesCleartextTraffic(0x010104ec)=true`（在**打包后的 APK** 里核对，不是只看源码） |
| 5 | **CR-07** 口令缺失不再静默出未签名包 | ✅ 已修 | `app/build.gradle.kts:28–36`（口令/密钥文件齐全才算"可签名"）、`:69–71`（可签名才挂 `signingConfig`）、`:87–122`（`requireReleaseSigning` + 只闸 `assembleRelease` / `bundleRelease` / `packageRelease` / `packageReleaseBundle` / `packageReleaseUniversalApk` / `signReleaseBundle`）；`keystore/README.md:4–19` 口径改为"只读仓库根目录 `local.properties`"并写明 release 必签名 | 有口令：`assembleRelease` → SUCCESS、`apksigner verify` v1+v2 通过、证书 `a98e8d4b…`；**无口令**：`assembleRelease` → `FAILED (:app:requireReleaseSigning)`，提示逐项列出缺哪 4 个、放哪；`bundleRelease` 同样 2 s 内失败；`assembleDebug` **无口令照常 SUCCESS**（debug 不签名不受影响） |
| 6 | **CR-11** `allowBackup` 与图标 | ✅ 已修 | `app/src/main/AndroidManifest.xml:17`（`allowBackup="false"`）、`:19`（`android:icon="@drawable/app_icon"`，不再用系统 drawable）；新增 `app/src/main/res/drawable/app_icon.xml`（矢量占位图标） | `aapt2 dump xmltree` → `allowBackup=false`、`icon=@0x7f080055`（**应用自己的资源**，不是 `@android:`）；`aapt2 dump badging` → `application-icon-*: res/1A.xml` |
| 7 | **CR-13 / BUG-003** CI 覆盖面 | ✅ 已修（CI 仍未真跑） | `.github/workflows/ci.yml:13`（任务名）、`:30–34`（显式 `sdkmanager` 装 `platforms;android-36` + `build-tools;35.0.0` + `platform-tools`）、`:41–42`（`check lint` 取代原来的裸 `grep` 步骤）、`:44–48`（`assembleDebug`）、`:49–55`（`assembleRelease`，口令走 GitHub Secrets） | 本机复现 CI 的命令序列：`check lint` → SUCCESS（lint 0 error）；`assembleDebug` / `assembleRelease` → SUCCESS。**真实 CI 运行仍需 god 推送后确认**（本机无 runner） |
| 8 | **BUG-005** 守护漏检全限定写法 | ✅ 已修 | `build-logic/src/main/kotlin/iptv.kotlin.library.gradle.kts:39–134`：在原 import 匹配之外增加"去注释/去字符串后的裸 `android.`/`androidx.` 引用"检测（正则 `:53–54`，前置边界 `(?<![\w.])` 避开 `com.x.android.Y` 这类长包名），注释、普通字符串、raw 字符串、块注释全部先剥离 | 见 §8.3 三组反证：注释/字符串**不误报**，全限定写法**命中**（旧守护漏检），import 写法**回归仍命中** |

### 8.2 反证实验 · CR-01 依赖守护（每条都改完即撤回）

守护任务 `verifyModuleDependencies` 的报告原文（一次跑出三类违规，说明它不是只查一条）：

```
> Dependency guard failed (docs/02 §3.2):
    - rule 5 (api exposure): :core:data exposes :core:database through `api` — declare it with implementation() …
    - rule 2: :feature:channels declares :core:database directly (see docs/02 §3.2)
    - rule 5 (no cycles): :core:common -> :core:log -> :core:common
```

| 实验 | 注入 | 期望 | 实测 |
|---|---|---|---|
| A（dispatch 指定的那条） | `app/build.gradle.kts` 加 `implementation(project(":core:player"))` | `check` 失败 | **FAILED**：`rule 3: :app declares :core:player directly`（`./gradlew check`，1 m 19 s）；撤回后 `check` → SUCCESS |
| B（规则 2） | `feature/channels/build.gradle.kts` 加 `implementation(project(":core:database"))` | 失败 | **FAILED**：`rule 2: :feature:channels declares :core:database directly` |
| C（api 暴露） | `core/data/build.gradle.kts` 的 `:core:database` 由 `implementation` 改 `api` | 失败 | **FAILED**：`rule 5 (api exposure): :core:data exposes :core:database through api` |
| D（环） | `core/common/build.gradle.kts` 加 `implementation(project(":core:log"))` | 失败 | **FAILED**：`rule 5 (no cycles): :core:common -> :core:log -> :core:common` |
| E（规则 1b，无环的纯 Kotlin 越界） | `core/model/build.gradle.kts` 加 `implementation(project(":core:log"))` | 失败 | **FAILED**：`rule 1b: :core:model is a pure-Kotlin module but declares the Android module :core:log directly`（旧守护对"纯 Kotlin 模块在构建脚本里挂 Android 依赖"无信号，即 CR-02） |

- B/C/D 三条实验一次跑完（同一个失败报告里同时列出），E 单独跑（要排除环的干扰，证明是规则 1b 命中）；随后**全部撤回**；撤回后 `verifyModuleDependencies` → `OK (19 modules…)`，`:core:data:check` 也会带上该守护。
- 规则口径按 god 裁决实现：**只看本模块的直接声明**（`build.gradle.kts` 或它套用的约定插件），传递依赖不算违规；但受限模块（`core:{database,network,source,epg,player}`）**不得用 `api` 暴露**，中间模块必须用 `implementation`，这样它进不了消费者的编译类路径。
- 实现细节（踩过的坑）：只能读"声明桶"配置（`!canBeResolved && !canBeConsumed`）。AGP 自己的 `debugUnitTestCompileClasspath` / `debugAndroidTestCompileClasspath` 这类**可解析**配置里含一条指向**模块自身**的工程依赖和整条传递闭包，照单全收会报出假环 `:app -> :app`（第一次实现就是被它绊住的）。
- 顺手补了 CR-02 的另一半（规则 1b）：`verifyPureKotlin` 管的是纯 Kotlin 模块的**源码**，守护再管它的**依赖声明**——纯 Kotlin 模块直接声明 Android 模块（`:core:log`/`:core:ui` 等）会被点名，判据是"目标模块是否套了 Android 插件"。

### 8.3 反证实验 · BUG-005 纯 Kotlin 守护（探针文件改完即删）

探针：`core/domain/src/main/kotlin/ilab/iptv/player/core/domain/PurityGuardProbe.kt`（纯 Kotlin 模块，本不该碰 Android）。

| 探针内容 | 命令 | 实测 |
|---|---|---|
| 只有注释提到 `android.os.Bundle`、只有字符串字面量含 `android.os.Bundle` / `androidx.lifecycle.ViewModel` | `./gradlew :core:domain:verifyPureKotlin` | **SUCCESS**（`verifyPureKotlin: OK (:core:domain)`）→ 不误报 |
| 无 import 行的全限定写法 `val bundle: Any = android.os.Bundle::class.java` | 同上 | **FAILED**：`PurityGuardProbe.kt:9 -> Android reference: val bundle: Any = android.os.Bundle::class.java`（旧守护此例漏检，见 BUG-005） |
| 回归：`import android.os.Bundle`（原口径） | 同上 | **FAILED**：`PurityGuardProbe.kt:3 -> import android.os.Bundle` → 原有能力未丢 |

探针删除后 `./gradlew :core:domain:verifyPureKotlin` → SUCCESS，`find core/domain -name '*.kt'` 无残留。

### 8.4 两个需要说明的设计取舍

1. **CR-05 为什么把 5 个快捷方法改成抽象**（而不是"注入填充器再保留默认体"）：默认体无法拿到 `seq`/`ts`/`thread`/`sessionId`，要么伪造（旧行为，直接污染 `sessionId` 折叠与 `seq` 排序），要么把接口改成有状态。改成抽象**签名一字未动**（不破坏 docs/02 §4.1 的 v1 冻结面），并把"必须用真值、取不到就不发事件"写进 KDoc；实现落 P0-6 `:core:log`。
2. **BUG-005 为什么选"加宽检测"而不是"只写边界说明"**：编译器（`Unresolved reference 'android'`）确实兜底，但那是**编译期**报错、文案指向 Kotlin 解析失败，且只在被编译的变体上出现；加宽后的守护给出的是"哪个纯 Kotlin 模块、哪一行、什么写法"，代价是几十行字符串/注释剥离逻辑。因此两条都留：守护负责说清问题，编译器负责兜底。

### 8.5 最终状态

| 命令 | 结果 |
|---|---|
| `./gradlew verifyModuleDependencies` | SUCCESS — `OK (19 modules; no Android dependency from a pure-Kotlin module, no forbidden direct dependency, no api exposure of [:core:database, :core:network, :core:source, :core:epg, :core:player], no cycles)` |
| `./gradlew check` | **SUCCESS**（含 `verifyPureKotlin` ×3、`verifyModuleDependencies`、单测、lint） |
| `./gradlew assembleDebug` | SUCCESS — `app-debug.apk` 9,098,896 B，SHA256 `24cadf42…`（无口令也可出包） |
| `./gradlew assembleRelease` | SUCCESS（带口令）— `app-release.apk` 6,845,227 B，SHA256 `bcc40475f482472b06fd985aea27d3f2b1f558a528d694a0a71454692658891c`；`apksigner verify` v1 ✅ v2 ✅，证书 SHA-256 `a98e8d4bbc7a895888502ceecad742729e476d16aa22f8e5f9f9c1d47b259004`（与第 2 轮同一把密钥） |
| `./gradlew lint` | SUCCESS — **0 error / 29 warning**（24 条是"依赖有新版"类提示；`OldTargetApi` / `DiscouragedApi` / `ChromeOsAbiSupport` 为既有项；新增 2 条是本轮修复的直接后果，见下） |
| 合并后 APK 关键位 | `minSdk 21 / targetSdk 35 / compileSdk 36`；入口 `ilab.iptv.player.MainActivity`（leanback + LAUNCHER 双入口）；图标/ banner 均为应用资源 |

**本轮新增的 2 条 lint warning（都不是 error，`abortOnError = true` 仍为 0 error）**：

- `UnusedAttribute`：`usesCleartextTraffic` 仅 API 23+ 生效，而 `minSdk = 21` —— API 21/22 上明文本来就是放行的，属性冗余但无害（CR-06 要求加）。
- `DataExtractionRules`：`allowBackup=false` 后 lint 建议再补 `android:dataExtractionRules`（Android 12+ 的备份/迁移规则）。属 P1 数据策略的活，未在本轮加。

### 8.6 仍未闭环（不属本包或需他人接手）

1. **真实 CI 从未运行**（BUG-003 的"无运行记录"部分）：`ci.yml` 已补 `check`/`assembleRelease`/SDK 组件，但本机无 runner —— 需 god 推送后跑一次并把结果补进本文件（P0 出口条件 4）。
2. **文档口径 CR-03 / BUG-002**：`docs/02 §3.2` 矩阵 `app` 行与规则 3 自相矛盾、且缺 `:core:testing` 列 —— 属 arch 的写回清单（我不改 docs/01–04）；本守护按 god 裁决的**规则文字**实现（`:app` 不得直接依赖 `core:{database,network,source,epg,player}`），矩阵修订后若口径变化，改规则表（`iptv.module.dependencies.gradle.kts:33–41`）即可。
3. **BUG-001**（`docs/04 §8` 写 `.ui.MainActivity`，实际 `ilab.iptv.player.MainActivity`）未动：属文档/入口命名口径，归 arch+god。
4. **BUG-004**（APK 字节级不可复现，仅 `META-INF/*` 不同）未动：本轮未开可复现构建（未开 `org.gradle.configuration-cache`，也未固定 zip 时间戳），列为 P1 可选。
5. **守护只覆盖"负向清单"，不是 docs/02 §3.2 整张矩阵**：规则 1b/2/3/4 是"这些边不许有"，没有实现"只有矩阵允许的边才许有"。理由：矩阵 `app` 行与规则 3 目前自相矛盾且缺 `:core:testing` 列（CR-03 / BUG-002，arch 待回写），照矩阵做正向校验会先把矛盾固化进构建。矩阵修订后若要正向校验，把规则表（`iptv.module.dependencies.gradle.kts:35–43`）换成完整白名单即可。

---

## 9. P0-6 日志骨架验证（第 4 轮，2026-09-21）

> 目标：按 `docs/03 §3/§4/§5` 把日志骨架做到 **L1**——`Logger` 门面 + `LogcatSink` + `MemoryRingSink`，
> 并在设备页看见实时日志。作者：dev-A（Jim，第 4 轮 temp）。实现未提交，由 god 集成。

### 9.1 交付物（模块归属按 docs/02 §3.1 / §4.1）

| 模块 | 文件 | 说明 |
|---|---|---|
| `core:common`（纯 Kotlin） | `src/main/kotlin/.../core/common/EventCodes.kt` | 事件码注册表（45 个，逐行对照 docs/03 §3.3）+ `ALL` 集合 + `isRegistered()`，供 P0-7 CI 守护使用 |
| `core:common`（纯 Kotlin） | `src/test/kotlin/.../core/common/EventCodesTest.kt` | 注册表格式 / 去重 / 条目数守护 |
| `core:log` | `LogSink.kt` | 冻结接口 `write/flush/id`（docs/02 §4.1，扩展点 E8） |
| `core:log` | `LogBus.kt` | `Logger` 实现：级别过滤 → 真值信封 → 脱敏 → 有界队列（1024）→ 单后台线程分发；`flush()` 带上限；队列满时按 docs/03 §5 先丢 VERBOSE/DEBUG |
| `core:log` | `MemoryRingSink.kt` | 内存环 2000 条（可配），覆盖写 O(1)；`snapshot/tail/size/evictedCount/clear` |
| `core:log` | `LogcatSink.kt` | `TAG=IPTV/<category>`，FATAL→ASSERT，异常堆栈同 tag 输出 |
| `core:log` | `AndroidClock.kt` / `UuidSessionIdFactory.kt` / `UrlRedactor.kt` / `DeviceInfo.kt` | 真值信封源（墙钟 + `SystemClock.elapsedRealtime`）、`play-7f3a` 形状的会话 id、脱敏（§11 凭据部分）、设备与版本信息 |
| `core:log` | `di/LogModule.kt` | Hilt：`Clock`/`UptimeMs`/`SessionIdFactory`/`Redactor` + 两个 `@IntoSet LogSink` + `LogBus`/`Logger` 单例。内存环单例与集合元素是**同一个对象**（否则设备页读到的是另一个环） |
| `core:log` | `ui/LogConsoleActivity.kt` + `res/layout/activity_log_console.xml` + `res/values/strings.xml` + `AndroidManifest.xml` | 设备页：设备/应用/日志计数 + 实时日志（每秒刷新、最多渲染 200 行）+ 级别切换 + 清空内存环 |
| `core:log` | `src/test/.../LogBusTest.kt`、`MemoryRingSinkTest.kt`、`UrlRedactorTest.kt`、`LogcatSinkTest.kt` | 26 条纯 JVM 单测（不碰 Android API） |
| `app` | `IptvApplication.kt` | 启动发 `APP_START`（INFO，字段：版本 / 设备 / 内存上限）+ `flush()` |
| `app` | `MainActivity.kt` + `res/layout/activity_main.xml` + `strings.xml` | 占位首页加一个"日志与设备信息"入口；`onResume` 发 `UI_SCREEN_OPEN`（DEBUG） |
| `core:log` | `build.gradle.kts` | 加 `iptv.hilt` 约定插件 + `androidx.activity`（`@AndroidEntryPoint` 需要 `ComponentActivity`） |

### 9.2 命令与结果（实测；`JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`，`ANDROID_HOME=~/Library/Android/sdk`）

| 命令 | 结果 |
|---|---|
| `./gradlew :core:common:testDebugUnitTest testDebugUnitTest check assembleRelease`（dispatch 给的原文） | **FAILED**：`Cannot locate tasks that match ':core:common:testDebugUnitTest' as task 'testDebugUnitTest' not found in project ':core:common'`。原因：`core:common` 是 `java-library`（纯 Kotlin，docs/02 §3.1），单测任务是 `:core:common:test`，**没有** `testDebugUnitTest`——命令名要按模块类型区分，文档口径不变 |
| `./gradlew :core:common:test testDebugUnitTest check assembleRelease`（修正后实跑） | **SUCCESS**，1 m 09 s（254 executed / 1019 up-to-date） |
| 单测明细 | `:core:common:test` 4 条、`:core:log:testDebugUnitTest` 26 条（LogBus 14 / MemoryRing 6 / Redactor 5 / Logcat 1），**0 失败** |
| `./gradlew verifyModuleDependencies` | SUCCESS — `OK (19 modules; no Android dependency from a pure-Kotlin module, no forbidden direct dependency, no api exposure of [:core:database, :core:network, :core:source, :core:epg, :core:player], no cycles)` |
| `./gradlew lint` | SUCCESS — **0 error**（`abortOnError = true` 下不红）。警告：`:app` 报告 30 条（其中 24 条是"依赖/AGP 有新版本"类提示，其余 6 条为既有的 `OldTargetApi`/`ChromeOsAbiSupport`/`UnusedAttribute`/`DataExtractionRules`/`DiscouragedApi` + 本轮新增 `Overdraw`）；`:core:log` 报告 4 条（本轮新增：`ButtonStyle`×2、`DiscouragedApi`×1（manifest 的 `screenOrientation`）、`Overdraw`×1（console 布局根背景））。**没有 error，也没有新增 error 类问题** |
| `./gradlew assembleRelease`（带口令） | SUCCESS — `app/build/outputs/apk/release/app-release.apk`，6,869,138 B，SHA256 `e445eca3bc4592e20772a7055fa7a6400ec93a8c4ef0b9d405d356b4ad46ac93` |
| `apksigner verify --print-certs` | v1 ✅ v2 ✅，证书 SHA-256 `a98e8d4bbc7a895888502ceecad742729e476d16aa22f8e5f9f9c1d47b259004`（与前两轮同一把密钥） |

**本轮自己引入并修掉的 3 个缺陷（写下来给下一位）**：

1. Kotlin **块注释可嵌套**：KDoc 里写 `IPTV/` 紧跟 `*:V`、以及 `scheme://` 紧跟 `***@host`，都会打开一个嵌套注释，报"Unclosed comment"并连带一片 "Unresolved reference"。注释里"/ 紧跟 *"的写法必须避开。
2. `MutableIterator.remove()` 在 Kotlin 里返回 `Unit`，`if (!it.remove())` 编不过；改为判 `queue.remove(candidate)` 的返回值（顺带正确覆盖"分发线程刚取走该条"的竞态）。
3. `PackageInfo.longVersionCode` 需要 API 28，而 `minSdk = 21` → lint **error**（`NewApi`）。改为 `SDK_INT >= P` 分支 + `@Suppress("DEPRECATION")`，并抽到 `DeviceInfo.appVersion()` 供 app 与设备页共用。

### 9.3 事件码注册表（最小集 = docs/03 §3.3 全表，45 个）

`APP_START` `APP_STOP` `UI_SCREEN_OPEN` `UI_FRAME_JANK` ·
`NET_REQ_OK` `NET_REQ_FAIL` `NET_CHARSET_FALLBACK` ·
`SRC_FETCH_OK` `SRC_FETCH_FAIL` `SRC_PARSE_OK` `SRC_DEDUPE` `SRC_REFRESH_START` `SRC_REFRESH_DONE` `SRC_REFRESH_SKIP` ·
`VAL_SHALLOW_OK` `VAL_SHALLOW_FAIL` `VAL_DEEP_OK` `VAL_DEEP_FAIL` `SRC_SCORE` `SRC_SELECT` ·
`EPG_FETCH_OK` `EPG_FETCH_FAIL` `EPG_PARSE_OK` `EPG_MATCH_HIT` `EPG_MATCH_MISS` `EPG_COVERAGE` ·
`DB_UPSERT` `DB_FAIL` · `PLAY_PREPARE_START` `PLAY_FIRST_FRAME` `PLAY_PREPARE_FAIL` `PLAY_FAILOVER` `PLAY_STALL`
`PLAY_SWITCH_CHANNEL` `PLAY_END` `PLAY_ENGINE_INIT` `PLAY_ENGINE_RELEASE` ·
`SERVICE_REFRESH_START` `SERVICE_REFRESH_STOP` · `WORK_SCHEDULE` `WORK_RUN` · `PERF_STARTUP` `PERF_EPG_GRID` ·
`CRASH` `ANR`

- 位置：`core/common/src/main/kotlin/ilab/iptv/player/core/common/EventCodes.kt`；`EventCodes.ALL` 与 `EventCodes.isRegistered(code)` 供 P0-7 CI 守护断言"日志里的码必须已注册"。
- 只按 docs/03 §3.3 抄录，**没有新增自定义码**；本轮真正发出去的事件只有两个：`APP_START`（INFO）、`UI_SCREEN_OPEN`（DEBUG）。

### 9.4 电视上怎么看日志（实测步骤）

```bash
adb connect 192.168.10.186:5555
adb install -r app/build/outputs/apk/release/app-release.apk
adb shell am start -n ilab.iptv.player/.MainActivity          # 遥控选中"日志与设备信息"回车
adb shell am start -n ilab.iptv.player/ilab.iptv.player.core.log.ui.LogConsoleActivity   # 或直接拉起设备页
adb logcat -d | grep 'IPTV/'                                   # LogcatSink 出口（TAG=IPTV/<category>）
```

设备页从上到下：设备/应用信息、日志计数、"级别：INFO（点击切换）"（INFO→DEBUG→VERBOSE 循环，docs/03 §14 的动态调级在 P0 先做在页面上）、实时日志区（每秒刷新，最多渲染 200 行；环本体仍是 2000 条）。

**实测结果（Sony BRAVIA 4K VH21 / Android 12 / API 31 / armeabi-v7a）**：

| 检查 | 实测 |
|---|---|
| 装机 | `adb install -r` → `Success`；`dumpsys package` = `versionCode=1 / versionName=0.1.0 / minSdk=21 / targetSdk=35` |
| 启动即出日志 | `adb logcat -d \| grep IPTV/` → `I IPTV/APP: seq=1 t=1790002260742 +26614153ms APP_START app started {version=0.1.0 (1), device=Sony BRAVIA 4K VH21 / Android 12 (API 31) / ABI armeabi-v7a,armeabi, maxMemoryMb=192} session=app-8b4c thread=main`——`seq/ts/elapsedMs/sessionId/thread` **全为真值** |
| 设备页出日志 | 屏幕实测：`内存环 1/2000（淘汰 0）`、`队列积压 0`、`级别过滤 2`，日志区显示上面那条 `APP_START` |
| 动态调级生效 | 页面切到 DEBUG（页面按钮获得焦点后回车，或遥控点按）→ `级别：DEBUG`；返回首页再进设备页 → 环内新增 `UI_SCREEN_OPEN main screen resumed screen=Main`、`UI_SCREEN_OPEN log console opened screen=LogConsole`，`内存环 3/2000` |
| 多路分发 | 同一时刻 `adb logcat` 出现同样两条 `D IPTV/UI` 记录 → 内存环与 logcat 收到的是同一条事件 |
| 级别过滤 | INFO 时 `UI_SCREEN_OPEN`（DEBUG）既不进环也不进 logcat，只累加"级别过滤"；切 DEBUG 后才出现 |
| 证据（仓库外，不入公开库） | `$AGENT_DIR/evidence/P0-6/`：`console-info-level.png`、`console-after-tap.png`、`console-live-debug-events.png`、`logcat-after-launch.txt`、`logcat-debug-events.txt`、`app-release-P0-6.apk` |

### 9.5 边界与未做项（不属 L1，避免被读成"已完成"）

1. **信封契约（CR-05）落实方式**：`LogBus` 从注入的 `Clock`/`UptimeMs`/`SessionIdFactory` 与当前线程取真值；取不到（抛异常，或 `ts ≤ 0`、`elapsedMs < 0`、线程名/会话 id 为空）就**丢事件并计数**（`envelopeFailureCount`）。单测 `drops the event instead of faking a missing envelope value`、`a failing envelope source never reaches the caller` 覆盖。
2. **只做了 L1 要求的两个 Sink**：`FileSink`（L3）、`CrashSink`（L4）、`MetricBridge` 未做；`LogBus` 已按 `Set<LogSink>` 注入，新增出口只需再绑一个 `@IntoSet`（扩展点 E8 在真机上已验证不变量成立）。
3. **P2-8 / L5 诊断面板未提前做**：设备页只有"设备信息 + 实时日志 + 级别切换 + 清空"，没有过滤/搜索/按会话折叠/导出；`LogEvent.screen` 字段本轮只放进 `fields`，待 UI 层接入。
4. **脱敏只做到 docs/03 §11 的一部分**：查询串凭据（`token/key/pwd/pass/sign/auth/secret/expire`，大小写不敏感）与 URL 用户信息已打码，且发生在 `LogBus` 入口（调用方绕不过去）；"路径替换为 host + 8 位 hash"与"导出管线二次 `Redactor`"属 L8/P2，未做。
5. **级别与容量还没接 DataStore**（docs/03 §14 的持久化开关属 P2）：级别是进程内状态，重启回 INFO；内存环容量固定 2000。
6. **`core:log` 顺带绑定了 `Clock`/`Redactor`/`SessionIdFactory`**：P0 只有 `:app` 依赖 `:core:log`，放这里能凑齐图；后续若 `:core:data` 等模块需要 `Clock`，应把这些基础类型迁到独立模块，避免"基础类型绑在日志实现里"。
## 10. CI 修复（第 4 轮，2026-09-21）

> 注：本轮与 P0-6 那轮并行，两轮各自把新章节编为 §9；集成时 god 把 CI 这节顺延为 §10，内容未改。

> 输入证据：GitHub Actions **首次真跑**失败 —— run `35614747492`（commit `75e8571`，https://github.com/cuijfboy/iptv-player/actions/runs/35614747492）。
> 失败步骤：`Set up Android SDK`（`android-actions/setup-android@v3`）→ failure；其后 `Install Android SDK components` / `check + lint` / `assembleDebug` 全部 **skipped** —— 还没跑到 Gradle 就死在 SDK 准备上。
> 基线：`75e8571`（worktree 分支 `agent/worker-dev-a-5`，已 `git merge --ff-only main`）。
> 改动文件：**只有** `.github/workflows/ci.yml` + 本文档。不改应用代码、不改依赖规则、不动 secrets、不 commit / 不 push。

### 9.1 改动点（file:line）

| # | 项 | 改动 |
|---|---|---|
| 1 | 去掉 `android-actions/setup-android@v3` | `ci.yml:36–40` 删除该 action；改为 `ci.yml:40–77` 直接用运行器预装的 SDK：解析 `$ANDROID_SDK_ROOT`/`$ANDROID_HOME`（缺失即 `::error` 退出）→ 把 `cmdline-tools/latest/bin`、`cmdline-tools/bin`、`tools/bin` 补进 `$GITHUB_PATH`（外加一次 `find` 兜底）→ `yes \| sdkmanager --licenses` 接受许可 → 显式装 `platforms;android-36` `build-tools;35.0.0` `platform-tools` → 把 `ANDROID_HOME`/`ANDROID_SDK_ROOT` 写进 `$GITHUB_ENV` |
| 2 | `assembleRelease` 无 secrets 时优雅降级 | `ci.yml:93–129`：secrets 映射成 step 级 `env`，用 `if: ${{ env.RELEASE_STORE_PASSWORD != '' }}` 判断（`secrets` 上下文不能在 `if` 里读，`env` 可以）——有 secrets 走真构建；没有时走 `assembleRelease skipped` 步（`::notice` + `$GITHUB_STEP_SUMMARY` 列出三个口令的 set/empty），作业仍然绿。`check` / `assembleDebug` 无条件跑 |
| 3 | JDK | `ci.yml:25–34`：temurin **17 → 21**，并加 `cache: gradle`（依据见 §9.4-1） |
| 4 | 三个 Gradle 调用 | `ci.yml:88/91/106`：保留 `--no-daemon` 与 `-Dorg.gradle.java.home="$JAVA_HOME"`（覆盖依据见 §9.4-3），新增 `--stacktrace` |
| 5 | 跑得住 | `ci.yml:11–13` `concurrency` + `cancel-in-progress`（同一 ref 只留最后一次，避免排队堆积）；`ci.yml:19–21` `timeout-minutes: 45`（对齐 docs/02 §6.1 的 45 min 预算，防止卡死占满默认 6 h） |

### 9.2 本地能验证的结论（本节全部实跑）

| 验证项 | 命令 | 结果 |
|---|---|---|
| YAML 语法 | `python3 -c "import yaml,sys;yaml.safe_load(open('.github/workflows/ci.yml'))"` | **OK**。本机 python3 原本**没装 pyyaml**（`ModuleNotFoundError`），已 `pip3 install --user pyyaml` 后通过；另用 macOS 自带 `ruby -ryaml` 二次解析，同样 OK |
| 每个 `run:` 块的 shell 语法 | `bash -n` 逐块（把 `run` 抽出来喂给 `bash -n`） | **5/5 OK**（SDK 步骤、check、assembleDebug、assembleRelease、skip 步骤） |
| 无 secrets 时的跳过分支 | 直接执行 skip 步骤脚本，`GITHUB_STEP_SUMMARY` 指向临时文件、三个口令置空 | **exit 0**；summary 写入"assembleRelease skipped"标题 + 三行 `empty` + 开启方式；另发一条 `::notice` |
| 跳过分支的边界（口令其实存在） | 同上，但把 `RELEASE_STORE_PASSWORD` 置非空 | **exit 0** + `::warning …RELEASE_STORE_PASSWORD is set but the release step did not run` —— 万一哪天 `if` 没读到 env，日志里会喊出来，不会静默永不出发行包 |
| SDK 发现与守卫分支 | 以本机 `ANDROID_SDK_ROOT` 跑该步骤脚本 | 打印 `Android SDK: /Users/jeffrey/Library/Android/sdk` 后按预期 `::error::sdkmanager not found` —— 本机 **没有 cmdline-tools**（所以 `sdkmanager` 那两行本地无法验证），GitHub 镜像自带 |
| 本地构建（`JAVA_HOME`=JBR 21.0.10、`ANDROID_HOME`、worktree 无 `local.properties`） | `./gradlew --no-daemon --stacktrace -Dorg.gradle.java.home="$JAVA_HOME" check assembleDebug` | **BUILD SUCCESSFUL in 1m 59s**（968 tasks：484 executed / 484 from cache）；产出 `app-debug.apk` **9,098,896 B**（与第 3 轮记录一致） |
| 无口令的 release（对照） | `./gradlew … assembleRelease`（不带任何 `RELEASE_*`） | **FAILED** `:app:requireReleaseSigning`，`missing: RELEASE_STORE_FILE, RELEASE_STORE_PASSWORD, RELEASE_KEY_ALIAS, RELEASE_KEY_PASSWORD`，且 `app/build/outputs/apk/release/` **目录不存在** —— 这正是 CI 必须 skip 该步的证据：不 skip 的话，secrets 配齐之前 CI 必红 |

### 9.3 仍需 CI 真跑才能确认

1. runner 镜像上 `ANDROID_HOME` / `cmdline-tools` 的**实际路径与可写性**（脚本已对三种常见布局 + `find` 兜底，但只有真机能证明命中）。
2. `sdkmanager` 在 **JDK 21** 下能跑（本机无 cmdline-tools，无法本地验证；若真机报 Java 版本相关错，回退方案是把 JDK 设回 17 —— 见 §9.4-1，两条路 AGP 都接受）。
3. Gradle 8.13 + JDK 21 + AGP 8.11.1 在 Linux 上**冷缓存**首跑（本地是 1.5 GB 暖缓存，只相差下载时长）。
4. secrets 配齐后的 `assembleRelease`（当前被 skip，因此 CR-07 的 CI 分支还没被真跑验证过）。

**下一次 run 要看**：① `Install Android SDK components` 通过（不再出现 setup-android）；② `check + lint` 绿；③ `assembleDebug` 绿；④ `assembleRelease` 显示 **skipped**（若直接显示 success，说明已有人配好 secrets，属正常）。

### 9.4 顺手排查的坑（含判断依据）

1. **JDK 17 够不够 → 够，但改用 21**。依据：反编译 AGP 8.11.1 jar 里的 `com/android/build/gradle/internal/plugins/AndroidPluginBaseServices.class`，跑的是 `JavaVersion.current().isCompatibleWith(JavaVersion.VERSION_17)`，不满足才报 `AGP_USED_JAVA_VERSION_TOO_LOW` —— 所以 17 是它写死的**下限**，17 能跑。换 21 的理由是**与已验证环境一致**：本机五条命令的绿灯全在 JBR **21.0.10** 上跑出来，中间没有任何一步是在 17 上验证过的。
2. **AGP 8.11.1 要求的最低 Gradle = 8.13**。依据：同一个 jar 的 `VersionCheckPlugin.class` 里常量 `"8.13"`（不满足会报 `Minimum supported Gradle version is 8.13`）；而 wrapper 正是 `gradle-8.13-bin.zip` —— 正好达标，wrapper 一行都不用改。
3. **`-Dorg.gradle.java.home="$JAVA_HOME"` 覆盖仍有效**。依据：`./gradlew -Dorg.gradle.java.home=/nonexistent/jdk-xyz help` → `Value '/nonexistent/jdk-xyz' given for org.gradle.java.home Gradle property is invalid (Java home supplied is invalid)`，即命令行值**压过** `gradle.properties`（否则那个不存在的路径不会生效）。所以 `gradle.properties` 里写死的 macOS JBR 路径在 Linux 上由这条覆盖兜住 —— **前提是每个 Gradle 调用都带这个参数**，漏一个就会去找 `/Applications/Android Studio.app/...` 而失败（本轮三个调用全部保留）。
4. **该装哪几个 SDK 组件**。依据是 AGP 自己的解析结果：本机首次构建（22:04）时 AGP **自动补装**了 `platforms/android-36` 与 `build-tools/35.0.0`（此前 SDK 里只有 36.1.0 / 37.0.0），说明 `compileSdk = 36` + AGP 8.11.1 默认 build-tools = **35.0.0**，正是 CI 里显式安装的那两个包（外加 `platform-tools`）。
5. **daemon 与缓存**。保留 `--no-daemon`（一次性 runner 上留 daemon 只多占内存）；新增 `actions/setup-java` 的 `cache: gradle`，它缓存 `~/.gradle`，同时省掉每次约 130 MB 的 gradle-8.13 发行包下载。`--stacktrace` 为首次真跑提供可定位堆栈。
