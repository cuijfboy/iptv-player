# 63 · 内置快照「判据收紧重生成 + 工具化 + 文案」独立复验收（卡 SNAP-REFRESH-QA-1）

> 卡：**SNAP-REFRESH-QA-1**（独立验收「开箱可播更可靠 + 文案与现状一致 + 工具可复跑」；**作者 Creed 不自签，本轮由独立 QA 当裁判**）
> 验收对象：主线集成提交 **`30779ec`**（`feat(snapshot): 快照判据收紧重生成（189→154）+ 工具化 + 文案校正`；作者交付说明见 [62](./62-快照修订与工具化.md)）
> 执行：QA temp `worker-angela-snap-qa`（Angela2）｜ 日期：2026-09-23 ｜ 基线 worktree `worktrees/worker-angela-snap-qa`，`HEAD 30779ec`
> 设备：Sony BRAVIA 4K VH21 / Android 12（API 31）/ `<TV-LAN-IP>:5555` —— **独占**（开工探活 `device`；全程未掉线）
> 边界：**不改功能代码**、未 commit / 未 push、未改 `docs/01–04`、未动网络/路由/DNS。发现缺陷只记录 + 报 god。
> 脱敏：电视/本机/网关地址一律 `<TV-LAN-IP>` / `<MAC-LAN-IP>` / `<LAN-GATEWAY>`；原始 JSONL、截图、日志不入库，只引用路径与结论。
> 证据：`$AGENT_DIR/evidence/`（`app-release-snap-refresh-qa.apk`、原始/剪辑截图、`app-log.jsonl`、`channels.m3u` 复核命令输出；见各判据「证据」行）

---

## 0. 结论（先给答案）

**卡 SNAP-REFRESH-QA-1：通过（PASS）。** 7 条判据全部达标：主线自建包开箱即播、默认第 1 台长稳可播、文案与现状一致、工具可静态复跑、刷新不冲掉快照、导入与脱敏回归无副作用。**未新立 S0 / S1 / S2 缺陷**；只记 1 条 S3 观察（在播放器页用「上/下」换台到本组末位后不再前进，**不在本卡判据内**，见 §9）。

| # | 判据 | 结果 |
|---|---|---|
| 1 | 主线自建 release 包 | ✅ `assembleRelease` 成功；**8,754,961 B** sha256 `bcc22b15…9294`；包内 `assets/snapshot/channels.m3u` 与源码树逐字节一致（`a34e9226…23c475`） |
| 2 | 开箱（`pm clear` + 零导入） | ✅ `DB_UPSERT{154/154}` + `SRC_PARSE_OK{provider=snapshot-1, entries=154, channels=154, streams=154}`；浏览页 **`频道 154 / 分组 4 / 流 154`**、焦点 **`1 CCTV-11戏曲`**、`p12.demo.invalid` **0 命中**、`tvg-chno` **1..154 连续无缺口** |
| 3 | 默认台长稳 + 抽样（本卡核心） | ✅ 默认第 1 台连续播放 **≥100 s**，`PLAY_STALL` **0**（首帧 1079 ms）；**再抽 6 台**（跨 4 分组）首帧 **479–1968 ms 全 ≤3 s**、各播 **≥30 s** 无 stall；全会话 `PLAY_STALL` **0** / `PLAY_PREPARE_FAIL` **0** / `PLAY_FAILOVER` **0** |
| 4 | 文案 | ✅ `「内置示例表（无法出画面）」` **全仓库 0 命中**；导入弹窗 = **`内置快照（电视已验证可播放）`**、设置页与诊断面板 = **`没有订阅（当前是内置快照）`** |
| 5 | 工具复跑（静态） | ✅ `tools/snapshot/verify.sh` **全 ok**（15 项）；**独立重数** `channels.m3u` 分组成 央视 25 · 卫视 22 · 港澳台 4 · 地方/其他 103（=154），与 `PROVENANCE.md` **逐项一致** |
| 6 | 回归 | ✅ 向导触发的 MANUAL 刷新 `SRC_REFRESH_DONE{channels:0}`（18 源全 TIMEOUT）→ **快照未被冲掉**（浏览页仍 154）；手动导入 `validated.m3u` → **571 频道 / 658 流**；导出诊断包**脱敏 0 命中** |
| 7 | 收尾 | ✅ 代理三项复核为空、`pm clear`、重装主线 release 包、`svc power stayon false`、回桌面、本机无 `http.server` |

---

## 1. 判据 1 · 构建（主线自建）

`local.properties` 从主仓 `iptv-player/local.properties` 拷入（gitignore，不入库）。`JAVA_HOME` = Android Studio JBR，`ANDROID_HOME` = `~/Library/Android/sdk`。

```bash
./gradlew --offline assembleRelease     # BUILD SUCCESSFUL（637 actionable tasks）
```

| 项 | 值 |
|---|---|
| 产物 | `app/build/outputs/apk/release/app-release.apk` |
| 字节数 | **8,754,961 B** |
| sha256 | `bcc22b1513eda1cfea622b02781701d726bcb7368cd0d4b2cdd1b90154e9294a` |
| 包内资产 | `assets/snapshot/channels.m3u`（26,998 B，sha256 `a34e92261d9cc93615b868634f82d7831edabd7f0bf4e74a725690c40f23c475`）、`assets/snapshot/PROVENANCE.md`（4,631 B）——`unzip -l` 实测 |
| 与 62 号对照 | 字节数与作者记录一致（8,754,961 B）；sha256 与作者记录不同（`bcc22b15…` vs `8281d8cf…`）＝重打包的时间戳/签名块差异，属已知口径；**源码资产 sha256 逐字节相同** |

装机：`adb install -r` 成功 → `pm clear ilab.iptv.player` 成功。

## 2. 判据 2 · 开箱（`pm clear` + 零导入）

冷启动（`monkey` 起 LAUNCHER 进向导；另一次直接进浏览页，两路一致）：

| 判据 | 实测（事件码 / 截图） |
|---|---|
| 快照播种 | `DB_UPSERT{channels:154, streams:154, removed:0}` + `SRC_PARSE_OK{message="fixture seeded into Room", provider=snapshot-1, format=m3u, entries=154, channels=154, streams=154, ms:127}`（冷启动即完成，早于向导） |
| 频道表 | 浏览页头行 **`频道 154 / 分组 4 / 流 154`**；分组 = 央视 25 / 卫视 22 / 港澳台 4 / 地方·其他 103 |
| 默认焦点 | **`焦点：1 CCTV-11戏曲`**（浏览页第一行 = `1 CCTV-11戏曲`，分组「央视」） |
| 夹具残留 | 整份会话日志 `grep -c p12.demo.invalid` = **0**；`tvg-chno` 独立重数 **1..154 连续、无重复、无缺口** |
| 向导第 3 步 | 「**当前有 154 个频道，其中 154 个可播放。**进入频道表后焦点会落在第一个可播放频道上，按 OK 播放。」 |

证据：截图 `~/evidence/ob-browse.png`、`ob-wizard3.png`、日志 `~/evidence/app-log.jsonl`。

## 3. 判据 3 · 默认台长稳 + 抽样起播（本卡核心）

方法：浏览页焦点在 `1 CCTV-11戏曲` → 按 OK 起播；判据取应用 JSONL 的 `PLAY_*` 事件。换台用**浏览页导航 + OK**（在播放器页内用「上/下」换台见 §9 观察）。

### 3.1 默认第 1 台（`channelId=1`）

| 项 | 实测 |
|---|---|
| 起播 | `PLAY_PREPARE_START{channelId:1, streamId:1, engine:media3, timeoutMs:12000}` → `PLAY_FIRST_FRAME{costMs:**1079**, vcodec:video/avc, acodec:audio/mpeg-L2, 1920x1080}` |
| 长稳 | 连续播放 **≥100 s**（截取第 ~55 s 与第 ~100 s 画面，均为动图/实景，非黑屏/静止）；窗口内 **`PLAY_STALL` = 0** |

### 3.2 抽样（≥5 台，跨 4 分组）

| 序 | channelId | 分组 | 首帧 costMs | 观察窗 | 该台是否 stall |
|---|---|---|---|---|---|
| 抽 1 | 25 | 央视 | 872 | ≥30 s | 否 |
| 抽 2 | 26 | 卫视 | 479 | ≥30 s | 否 |
| 抽 3 | 48 | 港澳台 | 1291 | ≥30 s | 否 |
| 抽 4 | 60 | 地方/其他 | 1872 | ≥30 s | 否 |
| 抽 5 | 90 | 地方/其他 | 575 | ≥30 s | 否 |
| 抽 6 | 120 | 地方/其他 | 1968 | ≥30 s | 否 |

**首帧全部 ≤3 s（479–1968 ms）、各台 hold ≥30 s 无 stall。** 会话内首帧样本 34 条，`max(costMs)=1968 ms`，**无一条 >3000 ms**。

### 3.3 全会话计数

| 事件码 | 计数 |
|---|---|
| `PLAY_STALL` | **0** |
| `PLAY_PREPARE_FAIL` | **0** |
| `PLAY_FAILOVER` | **0**（`give_up`/`resolve_fresh` 均未出现） |
| `PLAY_FIRST_FRAME` | 34（costMs 最大 1968） |
| `PLAY_SWITCH_CHANNEL` | 26 |

证据：`~/evidence/app-log.jsonl`、截图 `play-ch1-55s.png` / `play-ch1-100s.png`。

## 4. 判据 4 · 文案

| 位置 | 实际字符串（截图为准） |
|---|---|
| 浏览页「导入播放列表」弹窗 · 当前源 | **`当前：内置快照（电视已验证可播放）`** |
| 设置页「最近刷新」 | **`没有订阅（当前是内置快照）`** |
| 诊断面板「运行概览 · 最近刷新」 | **`没有订阅（当前是内置快照）`** |
| 代码侧 | `rg "示例表"` 在 `docs/` 之外 **0 命中**；`browse_import_none`=「内置快照（电视已验证可播放）」、`source_import_none`=「当前：内置快照」、`SettingsViewModel`/`DiagnosticsViewModel` 均「没有订阅（当前是内置快照）」 |

**「内置示例表（无法出画面）」在本版任何界面均未出现。** 证据：`~/evidence/copy-import.png`、`copy-settings.png`、`copy-diag.png`。

## 5. 判据 5 · 工具复跑（静态）

```bash
tools/snapshot/verify.sh app/src/main/assets/snapshot/channels.m3u   # 退出码 0
```

```
ok  extm3u-header / one-stream-per-channel extinf=154 url=154 / channels>=150 channels=154
ok  row-metadata / display-name-present / tvg-chno-1..N-contiguous
ok  group-covered:央视 / 卫视 / 地方/其他
ok  no-private-address / no-synthetic-invalid-host / no-credential-query-key / name-key-unique
ok  provenance-channel-count declared=154 file=154 / provenance-group-counts mismatched=0
verify.sh: OK — app/src/main/assets/snapshot/channels.m3u (154 channels)
```

**独立重数**（不复用脚本，直接对文件统计 `group-title`，并与 `PROVENANCE.md` 的 `<!-- snapshot-groups -->` 头比对）：

| 分组 | 文件实测 | PROVENANCE 声明 | 一致 |
|---|---|---|---|
| 央视 | 25 | 央视=25 | ✅ |
| 卫视 | 22 | 卫视=22 | ✅ |
| 港澳台 | 4 | 港澳台=4 | ✅ |
| 地方/其他 | 103 | 地方/其他=103 | ✅ |
| 合计 | **154** | **154** | ✅ |

另：`tvg-chno` 1..154 连续（`min=1 max=154`，重复 0、缺口 0）；`.invalid` / `p12.demo.invalid` 0 命中。

## 6. 判据 6 · 回归

| 项 | 实测 |
|---|---|
| **刷新不冲掉快照** | 向导第 2 步自动起的一轮 MANUAL 刷新（18 源，`SRC_REFRESH_START{sources:18}`）本网段对 `raw.githubusercontent.com` **全部 TIMEOUT**（`SRC_FETCH_FAIL ×17`，各 ~30 s）⇒ `SRC_REFRESH_DONE{phase:DONE, channels:0, unavailable:0, elapsedMs:151580}` ⇒ **0 频道不落库**；刷新后浏览页仍 **`频道 154 / 分组 4 / 流 154`**（截图），诊断面板「数据」仍 154/4/154 |
| **手动导入真清单** | 把 `iptv-player/tools/fixtures/large/validated.m3u`（658 条）`adb push` 到 `…/files/playlists/` → 浏览页「导入播放列表」→「从投放目录选择」→ 选 `validated.m3u` ⇒ 头行 **`频道 571 / 分组 4 / 流 658`**（截图），即真实清单整体替换频道表（导入即替换语义成立） |
| **导出诊断包脱敏** | 诊断面板「导出诊断包」→ `…/files/export/iptv-diag-20260923-030749.zip`（10,261 B）；解包 `overview.txt`/`logs/iptv-*.log`/`README.txt`/`stats.json`/`meta.json`；**扫描 0 命中**：无 `token`/`msisdn`/`secret`/`password`/`apikey` 键名，无 `192.168.*`/`10.*`/`172.16–31.*` 私网地址，无 `p12.demo.invalid`、无 `genglei.8866.org` 等原始宿主、无 `key=`/`authid`；日志内 URL 只剩 `https://raw.githubusercontent.com/<8 位 hash>` |

## 7. 判据 7 · 设备收尾（已实测）

| 项 | 实测 |
|---|---|
| 代理三项 | `http_proxy=:0`、`global_http_proxy_host=`（空）、`global_http_proxy_port=0`（开工与收尾两次均空，见 §8） |
| `pm clear` | `Success`（应用外部目录一并清空，`…/files` 已不存在） |
| 重装主线 release 包 | `adb install -r app/build/outputs/apk/release/app-release.apk` → `Success` |
| 常亮 | `svc power stayon false` |
| 回桌面 | `mCurrentFocus = me.efesser.flauncher/.MainActivity` |
| 本机服务 | `lsof -iTCP -sTCP:LISTEN` 无 `python`/`http` ⇒ **无遗留 `http.server`** |
| 备注 | 电视上仍存有上一卡 Creed 留下的探针夹具包 `ilab.iptv.player.spike`（非本卡产物，未动） |

## 8. 全局代理复核（开工 / 收尾各一次）

| 时刻 | `http_proxy` | `global_http_proxy_host` | `global_http_proxy_port` | 判定 |
|---|---|---|---|---|
| 开工 | `:0` | （空） | `0` | 空/`:0` ✅ |
| 收尾 | `:0` | （空） | `0` | 空/`:0` ✅ |

## 9. 观察项（不在本卡判据内）

- **S3（观察，非本卡引入）**：在**播放器页**用遥控「上」键（`KEYCODE_DPAD_UP`）逐台前进时，可正常 `PLAY_SWITCH_CHANNEL 1→2→…→25`；到**本组（央视）末位 `25 CCTV-9纪录`**后，再按「上」**不再前进**（3 s 间隔连按两次仍停在 25）；「下」键则为后退（`25→24…`）。用**浏览页导航 + OK**换台无此限制（本轮抽样 26/48/60/90/120 均正常到达）。未深入定位（可能在设计上是「组内 zap」），仅记录，供 god 判断是否另立卡。本卡判据 2/3 使用的是浏览页路径，不受影响。

## 10. 设备还原清单（对照卡要求）

1. 代理三项复核为空 —— ✅（§8）
2. `pm clear` —— ✅
3. 重装主线 release 包 —— ✅
4. `svc power stayon false` —— ✅
5. 回桌面 —— ✅
6. 停本机 `http.server` —— ✅（本卡全程未起，复核无监听）

---

_执行人：QA temp `worker-angela-snap-qa`（Angela2）。未 commit / 未 push，交 god 集成。_
