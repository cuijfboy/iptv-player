# 02 · 设计评审记录（关卡 G1）

> 评审对象：`docs/01-需求与可行性.md` … `docs/04-开发计划与验收.md`（已确认基线）
> 评审人：技术总监（arch）｜评审日期：2026-09-21｜评审方式：文档逐章评审 + `dsh/` 实测数据抽样核对
> 关联产出：[02 技术架构设计](../02-技术架构设计.md) 的 §4 / §7 / §8 已按本次结论**冻结为接口 v1**
> 结论：**有条件通过（Conditional Pass）** —— 6 项阻断问题已在本轮直接修入 `docs/02`；1 项需 god 拍板；其余为软件缺陷回写清单（§4）。

---

## 1. 评审范围与方法

| 项 | 内容 |
|---|---|
| 文档 | `01`（需求/决策 D1–D10）、`02`（架构）、`03`（日志诊断）、`04`（计划/验收/Spike S1–S6） |
| 代码/数据证据 | `/Users/jeffrey/temp/dsh/`（只读）：`iptv-repo/out/mac-pipeline/selected.json`、`out/validated.m3u`、`out/validated-lite.m3u`、`iptv-repo/README.md`、`tv-audit/` |
| 未做 | 不写产品代码、不跑构建、不装真机（保持评审独立性） |
| 数据抽样结论 | 579 频道 / 741 流（`selected.json`）。音频：AAC 695、**MP2 30**、缺元数据 14、**AC3 2**；视频：H264 725、**HEVC 5**、MPEG-2 2；分辨率：1080p 351、720p 154、480p 113、**2160p 6**。**每频道流数：1 条 = 417 个频道（72%）、2 条 = 162 个** |

---

## 2. 结论摘要

| 严重级 | 数量 | 处置 |
|---|---|---|
| 阻断（必须修后才能开工） | 6 | R-01…R-06：**已直接修入 `docs/02`（接口 v1）** |
| 重要（不阻断 P0，但必须在对应阶段前闭环） | 15 | R-07…R-21：写入回写清单（§4），指派到具体文档与章节 |
| 建议 | 8 | R-22…R-29：可择期采纳 |

**放行判断**：`02` 的模块边界、端口/策略签名、错误模型、并发模型在本轮已无歧义，可施工；`docs/04` 的 S1–S6 判据已与 §7/§8 的待定项逐条挂钩（见 `02` §4.7 / §7.8 / §8.6）。

---

## 3. 逐条评审意见

### 3.1 阻断级

| # | 章节 | 问题 / 风险 | 结论 |
|---|---|---|---|
| R-01 | `02` §4.3 / §4.4 | 契约用到但**从未定义**的类型达 20+ 个：`StreamOutcome` `ChannelWithStreams` `ChannelFilter` `SourceConfig` `AppSettings` `Metric(sSummary)` `EpgCoverage` `RefreshOptions/Progress` `FailoverInput` `PlaybackRequest/PreparedMedia/PlaybackState/PlaybackEvent/AudioTrackInfo/AspectRatioMode` `StreamTarget` `ProbeContext` `XmltvStream` `EpgChannelIndex` `ScoreInput/Breakdown` `SearchHit` `EpgLoadReport` `RawEntry` `SourceKind`。**风险**：签名不可编译、各模块各自造类型，冻结失效 | 阻断 → 已修：`02` §4.2 补齐全部模型；`02` §4.3/§4.4 给出完整可施工签名 |
| R-02 | `02` §6.2 vs §4.4 vs §4.3 | 播放状态有**三个来源**（§6.2 状态机图、`PlayerEngine.state`、`WatchChannelUseCase` 的 `PlaybackUiState`），未规定谁是唯一写者。**风险**：引擎与领域层双写状态 → 竞态与 UI 抖动 | 阻断 → 已修：§4 冻结「单一写者」规则（`PlaybackController` 写业务态，引擎只发原始 `EngineState`/`PlaybackEvent`），并给出 `EngineState→PlaybackPhase` 映射表 |
| R-03 | `02` §4.1 / §6.2 | `AppError` 是 8 个 data class 的 sealed 结构，但 §6.2 的失败分类（4xx / 超时 / 解码 / 停滞）与它**不对应**，`FailoverPolicy` 拿不到可判定的类别。**风险**：故障转移策略无法实现，或退化为 `when (error is …)` 的散乱判断 | 阻断 → 已修：§4.1 改冻结为 `AppError(code, failure: FailureClass, retryable, …)` + 现象→`FailureClass`→策略动作映射表 |
| R-04 | `02` §10 + §4.4 | `PlayerEngine` 同时暴露 `suspend prepare()` 与同步 `play()/stop()/attach(Surface?, LifecycleOwner)`，§10 只说「单线程 confined」，**未规定谁在哪个调度器调用、能否并发、释放后可否复用**。**风险**：Surface 早期释放、Media3 线程校验崩溃 | 阻断 → 已修：§4.5 冻结并发与线程契约（引擎调用串行化、单实例、prepare 互斥、surface 归属与 detach 语义） |
| R-05 | `02` §6.2 / `04` S1·S5 | F4（自动故障转移）与 S1/S5 判据隐含「每频道有备胎」，但实测 **417/579（72%）频道只有 1 条流**。**风险**：多数频道无源可切，F4 验收与「失败 5 s 内切备胎」在真机上不可达 | 阻断 → 已修：§4.3 增加 `FailoverAction.ResolveFresh` 与「无备胎」路径（RetrySame → 单频道按需复检 → GiveUp 并提示），`FailoverLimits` 冻结默认阈值；S1/S5 判据按「有备胎 / 无备胎」分组统计（§4.7） |
| R-06 | `03` §3.3/§4 vs `02` §4.1 | **同一类型两处定义且不一致**：`LogEvent` 在 `03` 有 `seq/elapsedMs/thread/screen/sessionId`，`02` 只有 7 个字段；`EventCodes`/`LogCategory` 亦然。**风险**：两个模块各写一份，日志字段缺失导致排查手册（`03` §12）失效 | 阻断 → 已修：`03` 为唯一定义来源，`02` §4.1 改为引用 + 只列 `02` 上特有的类型 |

### 3.2 重要级

| # | 章节 | 问题 / 风险 | 结论 / 处置 |
|---|---|---|---|
| R-07 | `02` §3.2 | 依赖矩阵允许 `:app → :core:database`，与「表现层不直接触碰 Room」自相矛盾；`feature:* → :core:player` 的例外（括号标注）未写成规则 | **已修入 §3.2 守护规则**（新增第 4 条：`:app` 不得依赖 `:core:database`；只有 `:feature:player` 可依赖 `:core:player`） |
| R-08 | `02` §6.1 | **时间预算与并发/上限算术不自洽**：4000 候选 × 浅校验 6 s ÷ 并发 12 ≈ 33 min > 预算 20 min，冷启动全量必然中断 | 回写 `02` §6.1（预算改为 45 min 且**截止时间驱动的准入**；默认候选上限降为 3000）→ 数值待 god 拍板（§5 开放问题 Q3） |
| R-09 | `02` §5.1 | 索引与真实数据冲突：`stream.url_hash` **唯一**会挡住「同一 URL 服务多频道」（常见于同源多台）；`channel.name_key` 唯一会挡住跨分组同名台（不同源、内容不同） | 回写 `02` §5.1：`url_hash` 唯一索引改为 `UNIQUE(channel_id, url_hash)`；`name_key` 唯一改为 `UNIQUE(name_key, group_key)` |
| R-10 | `02` §5.1 | `programme` 表**无唯一约束**、无级联；EPG 重复拉取会翻倍插入，§6.3 的「Room 事务写入」不可幂等 | 回写 `02` §5.1：`UNIQUE(epg_channel_id, start_ms)` + `INSERT OR REPLACE`；programme 按窗口删除时按 `epg_channel_id` 批量 |
| R-11 | `02` §5.1 / §10 vs `03` §5 | 指标有**两条写入路径**（`MetricBridge` 日志旁路 vs `MetricsRepository` 端口），`03` §5 又让 `:core:log` 延迟依赖 Room，与 §3.2 矩阵（log 不依赖 database）冲突 | 回写：唯一写者为 `MetricsRepository`（`:core:data`）；`MetricBridge` 只把事件转成 `Metric` 交给端口，Room 绑定留在 `:core:data` |
| R-12 | `02` §7 / `04` S1·S2 | 现有 658/579 频道的「可解码」结论来自 **Android `MediaPlayer` 深探**（`dsh/iptv-repo/README.md` §2），不是 Media3；而 100 分模型的「设备兼容 20 分」正是用该探测结果校准的 | 回写 `04` S1/S2：判据须写明「以 Media3 复测为准」，`dsh` 结果仅作候选集与对照基线；S1 需给 `Media3 成功率 − MediaPlayer 成功率` 的差值 |
| R-13 | `02` §7 / `04` S2 | S2 只验「AC3/EAC3 是否出声」，但基线里 AC3 仅 **2/741（0.3%）**，而 **MP2 有 30 条（4%）**、`mpeg2video` 2 条、HEVC 5 条、2160p 6 条 | 回写 `04` S2：扩为音频×编码矩阵 {AAC / MP2 / AC3} × {透传 / 软解}；`02` §7 冻结「透传判定 + 降级顺序」（探测 → 直通 → 关透传重试 → 切源） |
| R-14 | `02` §7 / `03` §3.3 | 「首帧」「起播耗时」无冻结定义，`PLAY_FIRST_FRAME` 由谁发出未定（引擎 / 控制器 / UI 三处都可能发），S1/S5/S6 的指标会口径不一 | **已修入 §7.3**：首帧 = 引擎 `FirstFrame` 事件；`PLAY_FIRST_FRAME` 由 `PlaybackController` 唯一发射；`costMs = firstFrameUptime − prepareStartUptime` |
| R-15 | `02` §7 / §8.1 | Surface/播放视图归属未定：引擎 `attach(Surface?, LifecycleOwner)` 但谁创建 `SurfaceView`、切后台/离开播放页时是否销毁引擎、信息条与视频层叠顺序均未写 | **已修入 §7.4/§4.5**：Activity 提供并持有 Surface，引擎只渲染；会话由控制器管理，退到浏览页不释放引擎；`detach(null)` = 停渲染不停音频 |
| R-16 | `02` §8.4 / `04` S3·S6 | 性能预算只有目标值，**无测量方法**（debug/release、时长、p50/p95、采样次数），落不了 CI 或真机验收 | **已修入 §8.5**：冻结测量口径（release + 冷启动 5 次取 max、滚动 30 s 取 p95、帧耗时阈值 16.7 ms）；S3/S6 按此复现 |
| R-17 | `02` §8.1/§8.2 | 导航契约缺失：`PlayerActivity` 入参键、返回结果、返回后焦点恢复、向导退栈与「返回键层级一致」的可判定规则均未定义 | **已修入 §8.2**：冻结 `PlayerContract`（EXTRA_CHANNEL_ID / EXTRA_STREAM_ID / EXTRA_AUTOPLAY）与返回语义（RESULT_OK + 返回 channelId，浏览页按 channelId 恢复焦点）；向导用 `FLAG_ACTIVITY_CLEAR_TASK` 收尾 |
| R-18 | `02` §8.3 / §4.3 | EPG 网格的数据来源未约束（有直接查 Room 的诱因），窗口查询 API 也没有上限 → 658×多日查询可能卡主线程 | **已修入 §8.3/§4.3**：网格只经 `EpgRepository.window(EpgWindowQuery)`；冻结上限（单次 ≤64 频道、窗口 ≤24 h、分页游标） |
| R-19 | `04` §2.2 | S3（EPG 网格 Canvas 原型，658 台×6 h）被排在 P0，而 EPG 网格属 P3，P0 里做等于提前生产化 | 回写 `04` §2.2：S3 在 P0 只交**原型与数据结论**（可丢弃代码 + 帧耗时数据），产品级网格仍在 P3-1 |
| R-20 | `02` §6.1 | 评分模型的扣分项（H.265 −8、AC3 −5、>1080p −3）与设备矩阵（仅 armeabi-v7a、3 GB、MediaTek MT5895）未写死判定阈值；2160p/HEVC 在真机是否可解未定 | 回写 `02` §6.1 评分表脚注：设备兼容分由 `DeviceProfile` + S1/S2 实测结论计算，禁止硬编码机型字符串 |
| R-21 | `02` §4.3 / `03` §11 | `ExportDiagnosticsUseCase` 返回 `File`（domain 依赖 `java.io`）且脱敏只在 `LogBus` 生效，而 `play_history`/`stream` 表存 URL 全文 → 导出包与库内数据的脱敏边界不清 | **已修入 §4.3**：返回值改为 `DiagnosticPackage(path, sizeBytes, createdAtMs)`；回写 `03` §8：导出器必须经 `Redactor` 二次过滤（库内保留原文、导出必脱敏） |

### 3.3 建议级

| # | 章节 | 建议 |
|---|---|---|
| R-22 | `02` §3.1 | 在模块清单里显式标注 Hilt 绑定归属（`:core:data` 提供端口绑定、`:app` 只组装），避免多处 `@Module` |
| R-23 | `02` §7 | `SimpleCache` 默认关闭的判断保持，但建议在 P1 前不引入，避免直播场景无收益的磁盘写放大 |
| R-24 | `03` §4 | `AppError.Permission` 缺 `cause`；建议与 §4.1 新模型统一（`FailureClass.PERMISSION`） |
| R-25 | `03` §13 L2 / `04` §2.1 | 「无裸 `Log.d`」「事件码必须在 `EventCodes.kt` 注册」建议合并成一条 CI 规则，作为 P0-7 的验收子项 |
| R-26 | `01` F5 / `02` §4.2 | 数字键跳台的「频道号」语义需冻结优先级（用户编辑 > 源 `tvg-chno` > 自动编号），否则换台行为不可预测 |
| R-27 | `01` F1 / `02` §4.4 | 「导入本地文件」需 SAF 持久化 URI 授权（`takePersistableUriPermission`），建议在 §4.4 `SourceKind.LOCAL_FILE` 处补一句 |
| R-28 | `03` §6 | 日志路径两处写法不同（`/sdcard/Android/data/.../files/logs/` 与 `files/logs/`），建议统一为「应用 files/logs，外部路径仅供 adb pull」 |
| R-29 | `04` §1 / §2 | P0 估时 1–2 天含 8 项任务 + 6 个 Spike，偏乐观；以 PM 的 WBS（`05-过程记录/01-WBS与排期.md`）为准并保持与 `04` 一致 |

---

## 4. 设计缺陷回写清单（须落回 `docs/`）

> **执行状态更新（2026-09-21，第 2 轮，arch）**：god 已答复全部开放问题（§5 Q1–Q5 全部批准，见下）。W1–W10 逐条状态如下：**✅ = 已办（文档已落地）/ ⏳ = 转下阶段（写明理由）**。文档级回写由 arch 完成，"实现"（代码落库 / CI 规则 / 导出器）在对应阶段由责任方执行——本清单的"已办"指文档口径已定稿，不等于代码已实现。

| # | 目标文档 · 章节 | 回写内容 | 责任方 | 状态 |
|---|---|---|---|---|
| W1 | `02` §3.2 | 依赖守护规则第 4 条（app 不依赖 database；feature 侧 player 例外） | arch | ✅ 已办（第 1 轮已改，本轮复核在册） |
| W2 | `02` §4 / §7 / §8 | 接口冻结 v1（模块边界 / 端口签名 / 错误模型 / 并发模型 / Spike 挂钩） | arch | ✅ 已办（第 1 轮已改，接口 v1 在册） |
| W3 | `02` §6.1 | 刷新预算与上限改为截止时间驱动（默认 45 min / 3000 候选） | arch（god 已批准） | ✅ 已办：§6.1 定稿 45 min / 3000 / 截止时间驱动，标注「2026-09-21 god 批准」 |
| W4 | `02` §5.1 | 索引修正：`stream(channel_id,url_hash)` 唯一；`channel(name_key,group_key)` 唯一；`programme(epg_channel_id,start_ms)` 唯一 + 幂等 upsert/按 `epg_channel_id` 修剪 | arch（文档）/ dev-A（P0-3 实现） | ✅ 文档已办（§5.1 索引与幂等说明已改）；**实现转 P0-3** |
| W5 | `02` §5.1 / §10 | 指标唯一写者 = `MetricsRepository`；`MetricBridge` 仅转发事件 | arch（文档）/ dev-A（实现） | ✅ 文档已办（§10 冻结「指标写入」）；**实现转 P0-3** |
| W6 | `02` §6.1 评分表 | 设备兼容分由 `DeviceProfile` + S1/S2 实测驱动，禁止机型硬编码 | arch（文档）/ dev-A（实现） | ✅ 文档已办（§6.1 评分表脚注）；**实现转 S1/S2 结论回写** |
| W7 | `03` §8 / §13 | 导出包必须经 `Redactor` 二次过滤（库内原文、导出脱敏） | arch（文档）/ dev-A（P2-8 / P3-6 实现） | ✅ 文档已办（§8 / §11 / §13 L8）；**实现转 P2-8 / P3-6** |
| W8 | `04` §2.2 | S1/S2 判据口径（Media3 复测为准、音频×编码矩阵）、S3 限定为原型 | arch + god | ✅ 已办：§2.2 修订 + 文件头 v1.1 + 附录 A 存旧口径（god 2026-09-21 批准） |
| W9 | `04` §2.1 | CI 规则并入 P0-7 验收子项（事件码注册表 + 禁止裸 Log） | arch（文档）/ dev-A（P0-7 实现） | ✅ 文档已办（§2.1 P0-7 验收子项）；**实现转 P0-7** |
| W10 | `01` F5 附注 | 频道号优先级冻结（用户编辑 > tvg-chno > 自动） | god | ⏳ 转下阶段：**待 god 决定**频道号优先级（不在本轮 5 项批准决策内，`01` F5 未改） |

> 边界声明：D1–D10（`docs/01` §5）本轮**未推翻、未修改**。W8/W10 涉及已确认文档，仅作为建议提交 god 裁决。

---

## 5. 需 god 拍板的开放问题（详见同批 `propose` 消息）

| # | 问题 | 推荐 |
|---|---|---|
| Q1 | 冻结接口 v1 后，`docs/02` 的变更流程（谁批、是否升版本号） | 冻结后签名变更须 arch 提案 + god 批准 + 文档版本 v1.x 递增 |
| Q2 | 72% 频道无备胎时 F4 的验收口径 | 按「有备胎/无备胎」两组分别验收；无备胎组以「重试 + 按需复检 + 明确提示」为准 |
| Q3 | 刷新预算与候选上限默认值 | 预算 45 min、候选上限 3000、截止时间驱动准入 |
| Q4 | S2 若判定 AC3 不可透传，是否引入 libVLC 模块 | 以比例决策：AC3/EAC3 占比 ≥5% 才引入，否则 AAC 优先 + 明确提示 |
| Q5 | `04` 中 S1/S2/S3 判据的修订是否立即执行 | 立即修订（走 W8），避免 P0 Spike 用错口径 |

> **决议（2026-09-21，god 批准，Q1–Q5 全部按推荐采纳）**：
> 1. **冻结流程（Q1）**：签名/契约变更走「arch 提案 → god 批准 → `docs` 版本 v1.x 递增」。
> 2. **F4 口径（Q2）**：分组验收——有备胎组 5 s 内切换；无备胎组「重试 → 按需单频道复检 → 明确提示」（依据基线 417/579 频道无备胎）。
> 3. **刷新预算（Q3）**：45 min、候选上限 3000、截止时间驱动准入（`02` §6.1 定稿）。
> 4. **libVLC（Q4）**：按比例决策——AC3/EAC3 占比 **≥5% 且不可透传**才引入；当前 0.3% 倾向不打包，终判等 S2 矩阵。
> 5. **`04` 判据（Q5）**：S1/S2/S3 立即修订（已执行，见 W8 / `04` v1.1）。

---

## 6. 签字

| 角色 | 结论 | 备注 |
|---|---|---|
| 技术总监（arch） | **有条件通过** | 阻断项已修入 `docs/02` 接口 v1；W3–W10 按 §4 清单在对应阶段前闭环 |
| god | **已批准 Q1–Q5（2026-09-21）** | W1–W9 文档口径已闭环；W10（频道号优先级）待 god 决定；集成提交由 god 执行 |
