# 02 · 技术架构设计

> 本文档定义 **分层、模块边界、接口契约、扩展点与并发/错误模型**。
> 目标：**分层合理 · 模块解耦 · 易扩展 · 可测试 · 可观测**。
> 相关：[01 需求与可行性](./01-需求与可行性.md) · [03 日志与诊断设计](./03-日志与诊断设计.md) · [04 开发计划与验收](./04-开发计划与验收.md)
>
> **冻结状态：接口冻结 v1（2026-09-21，关卡 G1 评审通过）**。冻结范围为 §3.2 依赖守护规则、§4 接口契约、§7 播放引擎、§8 UI 架构。
> 冻结后签名变更必须走：arch 提案 → god 批准 → 本文档版本号 v1.x 递增（同时更新 [设计评审记录](./05-过程记录/02-设计评审记录.md)）。
> 评审意见与缺陷回写清单见 [05-过程记录/02-设计评审记录.md](./05-过程记录/02-设计评审记录.md)。
> **2026-09-22 文档对齐批（卡 `DOC-3`，v1.x 口径澄清）**：§5.1 补 `group_key` 推导规则；§6.1 Parse 行事件码统一为 `SRC_PARSE_OK/FAIL`（原 `SRC_PARSE`）、补 `#EXTVLCOPT` 容错与采集口径。均为口径澄清与文本对齐，**冻结范围内的接口签名未变**。
> **2026-09-22 文档对齐批（卡 `DOC-5`，P2-6 本地导入回写）**：§4.2 补 `SourceKind.LOCAL_FILE` 与「本地文件」来源标识（导入清单 `local:<文件名>` vs 内置夹具 `p1-2-fixture`）；§8.1 把频道表页新增的「导入播放列表」入口写进 UI 契约（层级、返回键）。均为口径回写，**冻结范围内的接口签名未变**（未改 §8.1 既有导航契约，只新增一条）。
> **2026-09-22 文档对齐批（卡 `DOC-6`，v1.x 口径澄清）**：§3.1/§3.2 明确「**引擎=机制（`:core:player`）、策略=domain（`:core:domain`）、协调=feature（`:feature:player`）**」的分工，并把故障转移协调器落位 `:feature:player`；§7.2 P2 原先写的「实现位于 `:core:player`」与 §3.2 矩阵矛盾，按此改写；§8.1 补真机验证的本地导入入口；§8.2 补左右键与两级返回的键位契约。均为口径澄清与文本对齐，**冻结范围内的接口签名未变**。
> **2026-09-22 文档对齐批（卡 `DOC-7`，P2-4b/P1-7 回写）**：§6.1 评分表写明「**六维权重和为 97，加权后归一化到 0–100，`byRule` 保留原始分**」并补「未知编码取中性 12」脚注；§4.2 `ScoreInput` 的评分主体由 `entry: RawEntry` 改为 `stream: Stream`；§4.3 `StreamRepository` 补起播耗时中位数端口；§4.4 `PipelineLimits` 补 `shallowFirstBytes`/`deepFirstBytes` 与「深探只取首片字节、不做长播放」语义；§5.1 写明频道级「不可用」的当前口径（不加列，由流派生）；§4.2 写明 `DeviceProfile` 的收敛目标。均为口径回写与实现对齐，**冻结范围内的接口签名仅 §4.2 评分入参按实现改一处**（`ScoreInput.entry` → `ScoreInput.stream`）。

---

## 1. 设计目标与原则

| 原则 | 落地手段 |
|---|---|
| **单向依赖** | 依赖只允许由外向内：`feature → domain ← data → platform`；domain/model 不依赖 Android |
| **依赖倒置** | domain 定义**端口（interface）**，data/platform 提供**适配器（implementation）** |
| **纯策略可测** | 选台、评分、故障转移、匹配等**决策逻辑为纯 Kotlin**，零 Android 依赖，可直接单测 |
| **平台能力隔离** | 播放器、数据库、网络、XML 解析被封装在平台模块，替换实现不影响上层 |
| **可插拔扩展** | 源、校验器、评分规则、播放引擎、EPG 源、日志 Sink 全部以"集合 + 接口"方式注册 |
| **异常不跨边界** | 模块间用 `AppResult<T>` 传递失败，禁止异常穿透模块边界 |
| **可观测内建** | 每个关键路径都有**事件码**；日志是架构的一等公民，而非事后补丁 |
| **可中断/可恢复** | 长任务（源刷新、EPG 导入）持久化进度，随时可被杀掉并续跑 |
| **性能预算前置** | UI/刷新/播放都有量化指标与埋点，回归可度量 |

---

## 2. 分层模型

```
┌──────────────────────────────────────────────────────────────────────────┐
│ L4 表现层 Presentation                                                    │
│   :app  ·  :feature:channels  :feature:player  :feature:epg               │
│   :feature:settings  :feature:wizard  ·  :core:ui  ·  :core:design        │
│   职责：View / Fragment / Activity / ViewModel / UiState（单向数据流）      │
│   规则：只调用 domain 的 UseCase；不直接触碰 Room / OkHttp / ExoPlayer      │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │ 依赖 UseCase + 领域模型
┌───────────────────────────▼──────────────────────────────────────────────┐
│ L3 领域层 Domain                                                          │
│   :core:domain（纯 Kotlin）  ·  :core:model（纯 Kotlin）                   │
│   职责：UseCase / 领域模型 / 仓储端口(Port) / 纯策略(选源·评分·故障转移)     │
│   规则：零 Android 依赖；不出现 okhttp / room / media3 类型                 │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │ 依赖倒置：domain 定义接口
┌───────────────────────────▼──────────────────────────────────────────────┐
│ L2 数据层 Data                                                            │
│   :core:data（仓储实现）  :core:database（Room）  :core:network（OkHttp）   │
│   职责：Repository 实现、DTO↔Entity↔Domain 映射、缓存与事务                 │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │ 使用平台能力
┌───────────────────────────▼──────────────────────────────────────────────┐
│ L1 平台层 Platform                                                        │
│   :core:source（采集/解析/校验）  :core:epg（XMLTV）  :core:player（播放）   │
│   职责：将 Android/第三方能力适配为可替换的引擎实现                          │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────────────────┐
│ L0 基础设施 Common（被所有层依赖，自身不依赖任何业务）                       │
│   :core:common（AppResult/Logger 接口/Dispatcher/Clock/事件码）             │
│   :core:log（日志实现：内存环 + 文件 + logcat + 崩溃）                       │
└──────────────────────────────────────────────────────────────────────────┘
```

**关键解耦点**：*`FailoverPolicy` / `StreamSelector` / `ScoringRule` 是**纯 Kotlin 策略**（L3），`PlayerEngine` 是**平台适配器**（L1）*。因此"何时切源"可以脱离 Android 单测，"怎么播"可以整体替换（Media3 ⇄ libVLC ⇄ 未来引擎）。

---

## 3. 模块清单与依赖规则

### 3.1 模块清单

| 模块 | 类型 | 职责 | 关键内容 |
|---|---|---|---|
| `:app` | Android App | 组装、Hilt 根、前台服务、导航 | `IptvApplication` `RefreshService` `PlaybackService` `AppNav` |
| `:feature:channels` | Android Lib | 分组浏览 / 频道列表 / 收藏管理 | `ChannelListFragment` `ChannelListViewModel` |
| `:feature:player` | Android Lib | 全屏播放 / 信息条 / 播放设置 / **故障转移协调** | `PlayerActivity` `PlayerViewModel` `InfoBarView` `PlaybackFailoverCoordinator` |
| `:feature:epg` | Android Lib | EPG 时间网格 / 节目详情 / 搜索 | `EpgGridFragment` `EpgGridLayout`(自绘) |
| `:feature:settings` | Android Lib | 设置 / 源管理 / **诊断面板** | `SettingsFragment` `DiagnosticsFragment` `LogViewFragment` |
| `:feature:wizard` | Android Lib | 首启向导 | `WizardActivity` |
| `:core:ui` | Android Lib | 通用 TV 组件与焦点工具 | `ChannelCardView` `FocusExt` `TvDialog` `FrameStats` |
| `:core:design` | Android Lib | 主题/颜色/尺寸/图标/字体 | `TvTheme` `dimens.xml` 10-foot 规范 |
| `:core:domain` | **Kotlin JVM** | UseCase + 端口 + 纯策略（含播放故障转移策略与看门狗） | 见 §4；`FailoverPolicy` `DefaultFailoverPolicy` `PlaybackWatchdog` |
| `:core:model` | **Kotlin JVM** | 领域模型（不可变 data class） | `Channel` `Stream` `Programme` … |
| `:core:data` | Android Lib | 仓储实现 + 映射 + 事务 | `ChannelRepositoryImpl` … |
| `:core:database` | Android Lib | Room 实体/DAO/迁移 | `IptvDatabase` `ChannelDao` … |
| `:core:network` | Android Lib | OkHttp 封装、UA/Referer、编码回退 | `HttpClient` `HttpRequest` `Charsets` |
| `:core:source` | Android Lib | 源提供者 + 解析 + 两级校验 + 评分 | `SourceProvider` 实现、`M3uParser`、`Scorer` |
| `:core:epg` | Android Lib | XMLTV 拉取/流式解析/匹配 | `XmltvPullParser` `EpgMatcher` |
| `:core:player` | Android Lib | 播放引擎适配 + 引擎侧状态机 + 窄钩子（采样 / 呈现回调） | `PlayerEngine` `Media3Engine` `PlaybackSession` `EngineSample` |
| `:core:common` | **Kotlin JVM** | 基础设施契约 | `AppResult` `AppError` `Logger` `LogEvent` `EventCodes` `Clock` `DispatcherProvider` |
| `:core:log` | Android Lib | 日志实现（多 Sink） | `LogBus` `MemoryRingSink` `FileSink` `CrashSink` |
| `:core:testing` | Android Lib | 测试替身与夹具 | `FakeClock` `FakeHttp` `FakePlayerEngine` `Fixtures` |

### 3.2 依赖规则（允许矩阵，行 → 列）

| ↓依赖→ | common | log | model | domain | database | network | source | epg | player | data | ui | design | testing | feature | app |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| common | – | | | | | | | | | | | | | | |
| log | ✅ | – | | | | | | | | | | | | | |
| model | ✅ | | – | | | | | | | | | | | | |
| domain | ✅ | | ✅ | – | | | | | | | | | | | |
| database | ✅ | ✅ | ✅ | | – | | | | | | | | | | |
| network | ✅ | ✅ | | | | – | | | | | | | | | |
| source | ✅ | ✅ | ✅ | | | ✅ | – | | | | | | | | |
| epg | ✅ | ✅ | ✅ | | | ✅ | | – | | | | | | | |
| player | ✅ | ✅ | ✅ | | | | | | – | | | | | | |
| data | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | | – | | | | | |
| design | | | | | | | | | | | | – | | | |
| ui | ✅ | | ✅ | ✅ | | | | | | | – | ✅ | | | |
| testing | ✅ | | ✅ | | | | | | | | | | – | | |
| feature:* | ✅ | ✅ | ✅ | ✅ | | | | | ✅(player) | | ✅ | ✅ | | – | |
| app | ✅ | ✅ | ✅ | ✅ | | | | | | ✅ | ✅ | ✅ | ✅(debug) | ✅ | – |

> 表中 `✅` = 允许的**直接声明**（`implementation`/`api`）；空白 = 不得直接声明。`:core:testing` 只允许 `:app` 以 `debugImplementation` 引用（测试替身不进生产运行期）。

**守护规则（CI 强制；只约束「直接声明」）**：

1. `:core:domain` / `:core:model` / `:core:common` 为 **`java-library`（Kotlin JVM）**，**源码**（`src/`）中出现 `android.` / `androidx.` 引用即失败（`verifyPureKotlin`，含无 `import` 的全限定写法）；
2. `:feature:*` **不得直接声明** `:core:database` / `:core:network` / `:core:source` / `:core:epg`（只能经 domain 端口）；
3. `:app` **不得直接声明** `:core:database` / `:core:network` / `:core:source` / `:core:epg` / `:core:player`；Hilt 绑定由 `:core:data` 提供，`:app` 只做组装；
4. 唯一例外：只有 `:feature:player` 可直接声明 `:core:player`（`PlayerEngine` 是 UI 直连的平台适配器，见 §4.5 规则 C2）；其余 `feature:*` 一律经 domain 端口；
5. **传递依赖不违规，但中间模块必须用 `implementation` 暴露**（不得用 `api` 把内部依赖转出去）；禁止循环依赖。

> **口径与实现（god 裁决 2026-09-21，BUG-002 / CR-03 收口）**：守护以**本矩阵为唯一数据源**，由约定插件 `iptv.module.dependencies`（任务 `verifyModuleDependencies`，挂到 `check`）在构建期校验各模块的 `project(":…")` **直接声明**；传递依赖不算违规，但中间模块必须用 `implementation` 暴露（用 `api` 转出内部依赖会判失败——反证 B：`:core:data` 把 `:core:database` 由 `implementation` 改 `api` → `rule 5 … FAILED`）。本版矩阵按实测修正了两处与规则文字矛盾的地方：`:app` 行原先错标 `database`/`player` 为 ✅（实测 `:app` 的 `releaseRuntimeClasspath` 顶层**不含** database/network/source/epg/player），并补上 `:core:testing` 列/行。规则 1 的守护只扫源码（不扫构建脚本），构建脚本里的依赖声明由本条矩阵守护覆盖（CR-02 同源）。

> **职责三分：引擎=机制 / 策略=domain / 协调=feature（god 裁决 2026-09-22，卡 `DOC-6`；与 `05-过程记录/20-P1-5换台与故障转移验证.md` §8.1 同源，本矩阵仍是唯一判据）**：
> - **机制 = `:core:player`** —— 引擎适配与引擎侧状态机（`PlayerEngine` / `Media3Engine` / `PlaybackSession`），只对上暴露窄钩子（`sample()` 采样、呈现回调、`onRenderedFirstFrame` 等）。矩阵中 `:core:player` 行**没有** `domain` 列 ⇒ 它**不得**直接声明 `:core:domain`。
> - **策略 = `:core:domain`** —— `FailoverPolicy` / `DefaultFailoverPolicy` / `PlaybackWatchdog` 与 `FailoverAction` 等纯策略（§4.3）：零 Android、100% 可 JVM 单测。
> - **协调 = `:feature:player`** —— `PlaybackFailoverCoordinator` 把机制与策略接起来（会话循环：prepare → 看门狗监督 → 决策 → 再 prepare），是 `PLAY_PREPARE_START` / `PLAY_FIRST_FRAME` / `PLAY_FAILOVER` / `PLAY_STALL` / `PLAY_SWITCH_CHANNEL` 的**唯一发出者**。`feature:*` 行允许同时声明 `domain` 与 `player`（规则 4），因此只有这一层能同时看见机制与策略。
>
> **因此不要把故障转移协调器放进 `:core:player`**：那需要 `:core:player → :core:domain`，会直接违反本矩阵（`verifyModuleDependencies` 判失败）。

---

## 4. 关键接口契约

### 4.0 冻结约定（v1，施工前必读）

| # | 约定 | 说明 |
|---|---|---|
| F1 | **类型唯一归属** | 每个类型只在本文档列出的那个模块定义一次；包名 `ilab.iptv.player.<模块>`。`LogEvent` / `LogLevel` / `LogCategory` / `EventCodes` 的**唯一定义来源是 [03 文档](./03-日志与诊断设计.md) §3–§4**，本文档不复制 |
| F2 | **异常不跨边界** | 模块间只返回 `AppResult<T>`；`CancellationException` 例外，必须原样重抛，不得转成 `AppError` |
| F3 | **domain 纯度** | `:core:domain` 的签名只允许出现：`kotlin.*` / `kotlinx.coroutines.*` / `:core:model` / `:core:common`。不得出现 `java.io.File`、Android 类型、`okhttp`/`room`/`media3` 类型 |
| F4 | **可取消** | 每个 `suspend` 入口必须有界（文档给出默认超时），且必须响应取消；`fun`（非 suspend）入口必须是纯计算或无阻塞的即时投放 |
| F5 | **不可变** | 跨模块传递的模型一律 `data class` + `val`；集合用 `List`/`Set`/`Map`，不暴露可变类型 |
| F6 | **变更流程** | 冻结后任何签名改动：arch 提案 → god 批准 → 本文档 v1.x 递增 |

### 4.1 基础设施（`:core:common`，纯 Kotlin）

```kotlin
// ---- 结果与错误（见 §4.6 映射表）----
sealed interface AppResult<out T> {
    data class Ok<T>(val value: T) : AppResult<T>
    data class Err(val error: AppError) : AppResult<Nothing>
}
fun <T> AppResult<T>.valueOrNull(): T?
inline fun <T, R> AppResult<T>.map(transform: (T) -> R): AppResult<R>
inline fun <T> AppResult<T>.onErr(action: (AppError) -> Unit): AppResult<T>

enum class FailureClass {          // 唯一失败分类，决定策略动作
    HTTP_CLIENT, HTTP_SERVER, TIMEOUT, NET_UNREACHABLE, TLS,
    PARSE, DECODE_UNSUPPORTED, DECODE_CORRUPT, EMPTY_MEDIA, PLAYLIST_GONE,
    STORAGE, PERMISSION, NO_CAPABILITY, CANCELLED, UNKNOWN,
}
data class AppError(
    val code: String,                 // 事件码，取值必须来自 EventCodes（03 文档 §3.3）
    val failure: FailureClass,
    val retryable: Boolean,
    val httpStatus: Int? = null,
    val detail: String? = null,       // 已脱敏
    val cause: Throwable? = null,
) {
    companion object {                // 工厂：禁止在业务代码里 new 分类
        fun http(status: Int, code: String, cause: Throwable? = null): AppError
        fun timeout(code: String, cause: Throwable? = null): AppError
        fun network(code: String, cause: Throwable? = null): AppError
        fun parse(code: String, cause: Throwable? = null): AppError
        fun decode(code: String, unsupported: Boolean, cause: Throwable? = null): AppError
        fun storage(code: String, cause: Throwable? = null): AppError
        fun permission(code: String, cause: Throwable? = null): AppError
        fun capability(code: String, detail: String? = null): AppError
        fun unknown(code: String, cause: Throwable? = null): AppError
    }
}

// ---- 日志：类型定义以 03 文档 §3–§4 为准，此处仅冻结门面 ----
interface Logger {
    fun log(event: LogEvent)
    fun v(category: LogCategory, code: String, message: String, fields: Map<String, Any?> = emptyMap())
    fun d(category: LogCategory, code: String, message: String, fields: Map<String, Any?> = emptyMap())
    fun i(category: LogCategory, code: String, message: String, fields: Map<String, Any?> = emptyMap())
    fun w(category: LogCategory, code: String, message: String, fields: Map<String, Any?> = emptyMap(), error: Throwable? = null)
    fun e(category: LogCategory, code: String, message: String, fields: Map<String, Any?> = emptyMap(), error: Throwable? = null)
    fun flush(timeoutMs: Long = 500)
}
interface SessionIdFactory { fun newId(prefix: String): String }     // "play-7f3a" / "refresh-2c11"
interface Redactor { fun redact(url: String): String }               // 03 文档 §11，导出与落盘共用

// ---- 时间与调度 ----
interface Clock { fun nowMs(): Long }
interface DispatcherProvider {
    val io: CoroutineDispatcher
    val default: CoroutineDispatcher
    val main: CoroutineDispatcher
    val engine: CoroutineDispatcher      // 单并行度：播放引擎的唯一执行线程（§4.5 C2）
    val single: CoroutineDispatcher      // 单线程：状态机/串行写入等
}
interface AppScopeProvider { val appScope: CoroutineScope }          // 禁止 GlobalScope
```

### 4.2 领域模型（`:core:model`，纯 Kotlin）

```kotlin
enum class ChannelGroup(val key: String) { CCTV("cctv"), SATELLITE("sat"), HK_MO_TW("hmt"), LOCAL("local"), OTHER("other") }

data class Channel(
    val id: Long, val name: String, val tvgId: String?, val group: ChannelGroup,
    val logoUrl: String?, val channelNo: Int?, val favorite: Boolean, val hidden: Boolean,
    val sortOrder: Int, val epgChannelId: String?, val epgMatch: EpgMatchType,
    val streamCount: Int,                       // 0/1/2：1 表示无备胎（见 §4.6 R5）
)
data class Stream(
    val id: Long, val channelId: Long, val url: String,
    val userAgent: String?, val referrer: String?, val sourceId: String,
    val quality: Quality?, val videoCodec: String?, val audioCodec: String?,
    val width: Int, val height: Int, val score: Int, val priority: Int,
    val lastOkAtMs: Long?, val lastCheckAtMs: Long?, val failCount: Int, val lastError: String?,
    val disabled: Boolean,
)
data class ChannelWithStreams(val channel: Channel, val streams: List<Stream>)   // streams 已按 §4.3 排序
data class ChannelFilter(val group: ChannelGroup?, val favoritesOnly: Boolean, val includeHidden: Boolean, val query: String?)
enum class Quality { UHD_4K, FHD_1080, HD_720, SD, UNKNOWN }
enum class EpgMatchType { TVG_ID, NAME_EXACT, NAME_FUZZY, ALIAS, MANUAL, NONE }
data class Programme(val id: Long, val epgChannelId: String, val startMs: Long, val stopMs: Long,
                     val title: String, val desc: String?, val category: String?)
data class NowNext(val now: Programme?, val next: Programme?)

// ---- 源与订阅 ----
enum class SourceKind { M3U, TXT, XTREAM, JSON, LOCAL_FILE }
data class RawEntry(val name: String, val url: String, val tvgId: String? = null, val tvgName: String? = null,
                    val groupTitle: String? = null, val logo: String? = null, val channelNo: Int? = null,
                    val userAgent: String? = null, val referrer: String? = null, val sourceId: String)
data class SourceConfig(val id: String, val providerId: String, val label: String, val url: String,
                        val kind: SourceKind, val enabled: Boolean, val builtIn: Boolean)
data class StreamTarget(val url: String, val userAgent: String?, val referrer: String?)
data class ProbeContext(val stage: ValidationStage, val timeoutMs: Long, val engineCaps: Set<EngineCapability>, val nowMs: Long)
enum class ValidationStage { SHALLOW, DEEP }
data class ValidationResult(val passed: Boolean, val detail: String, val evidence: Map<String, Any?> = emptyMap())

// ---- 校验与评分 ----
data class StreamOutcome(val streamId: Long, val ok: Boolean, val atMs: Long,
                         val costMs: Long?, val failure: FailureClass?, val detail: String? = null)
data class ScoreInput(val stream: Stream, val probe: ValidationResult, val videoCodec: String?, val audioCodec: String?,
                      val width: Int, val height: Int, val stability: Double, val device: DeviceProfile)
data class ScoreBreakdown(val total: Int, val byRule: Map<String, Int>)
data class DeviceProfile(val abi: String, val sdk: Int, val ramMb: Int,
                         val audioPassthrough: Set<String>, val maxWidth: Int, val maxHeight: Int,
                         val maxFrameRate: Float)          // 由 S1/S2 结论填充，禁止机型硬编码（§4.7）
data class StreamHealth(val attempts: Int, val failures: Int, val lastOkAtMs: Long?, val consecutiveFails: Int)

// ---- 刷新 ----
enum class RefreshTrigger { SCHEDULED, MANUAL, FIRST_RUN, ON_DEMAND_SINGLE_CHANNEL }
enum class RefreshPhase { FETCH, PARSE, NORMALIZE, DEDUPE, SHALLOW, DEEP, SCORE, SELECT, PERSIST, DONE }
data class RefreshOptions(val trigger: RefreshTrigger, val budgetMs: Long = 45 * 60_000L,
                          val respectPlayback: Boolean = true, val channelIdOnly: Long? = null)
data class RefreshProgress(val phase: RefreshPhase, val done: Int, val total: Int, val okCount: Int,
                           val failCount: Int, val elapsedMs: Long,
                           val interrupted: RefreshInterruption? = null)
data class RefreshInterruption(val reason: InterruptionReason, val phase: RefreshPhase)
enum class InterruptionReason { BUDGET_EXCEEDED, CANCELLED, PLAYBACK_PRIORITY, ERROR }

// ---- 播放（引擎与应用层之间唯一的传递模型）----
enum class EngineCapability { HLS, HTTP_TS, RTSP, DASH, AC3_PASSTHROUGH, AUDIO_TRACK_SELECT, ASPECT_RATIO, SUBTITLES }
enum class AspectRatioMode { FIT, FILL, ZOOM, FIXED_4_3 }
data class PlaybackRequest(val channelId: Long, val stream: Stream, val startPositionMs: Long = 0,
                           val timeoutMs: Long = 12_000, val preferPassthrough: Boolean = true, val sessionId: String)
data class PreparedMedia(val streamId: Long, val durationMs: Long?, val isLive: Boolean,
                         val videoCodec: String?, val audioCodec: String?, val width: Int, val height: Int,
                         val audioTracks: List<AudioTrackInfo>)
data class AudioTrackInfo(val id: String, val label: String, val language: String?, val codec: String, val isDefault: Boolean)
enum class EngineState { IDLE, PREPARING, READY, BUFFERING, PLAYING, ENDED, ERROR, RELEASED }
enum class EndReason { USER_STOP, CHANNEL_SWITCH, COMPLETED, ERROR, APP_EXIT }
sealed interface PlaybackEvent {
    data class Prepared(val media: PreparedMedia, val costMs: Long) : PlaybackEvent
    data class FirstFrame(val costMs: Long) : PlaybackEvent                    // PLAY_FIRST_FRAME 的唯一来源（§7.3）
    data class Stalled(val stalledMs: Long, val positionMs: Long) : PlaybackEvent
    data class Error(val error: AppError, val fatal: Boolean) : PlaybackEvent
    data class Ended(val reason: EndReason) : PlaybackEvent
    data class AudioTracks(val tracks: List<AudioTrackInfo>, val selectedId: String?) : PlaybackEvent
    data class Capabilities(val caps: Set<EngineCapability>) : PlaybackEvent
}

// ---- 应用层（UI 消费的唯一状态）----
enum class PlaybackPhase { IDLE, PREPARING, BUFFERING, PLAYING, FAILOVER, STOPPED, ERROR, RELEASED }
data class InfoBarState(val channelName: String, val logoUrl: String?, val qualityLabel: String?,
                        val nowNext: NowNext?, val failoverHint: String? = null)
data class PlaybackUiState(
    val channelId: Long?, val phase: PlaybackPhase, val activeStreamId: Long?,
    val infoBar: InfoBarState?, val aspectRatio: AspectRatioMode, val audioTracks: List<AudioTrackInfo>,
    val selectedAudioTrackId: String?, val failoverCount: Int, val lastError: AppError?, val errorText: String?,
)
data class RefreshUiState(val running: Boolean, val phase: RefreshPhase?, val done: Int, val total: Int,
                          val okCount: Int, val failCount: Int, val lastDoneAtMs: Long?, val interrupted: RefreshInterruption?)

// ---- EPG 与诊断 ----
data class XmltvStream(val sourceId: String, val bytes: Long?)                 // 流式句柄，不整文件入内存
data class EpgChannelIndex(val byId: Map<String, String>, val byNameKey: Map<String, String>)
data class EpgCoverage(val matched: Int, val total: Int, val byGroup: Map<ChannelGroup, Int>)
data class EpgWindowQuery(val fromMs: Long, val toMs: Long, val channelIds: List<Long>, val limit: Int = 64)
data class EpgLoadReport(val providers: Int, val channels: Int, val programmes: Int, val skipped: Int, val coverage: EpgCoverage, val elapsedMs: Long)
data class SearchHit(val channelId: Long, val channelName: String, val matchedProgramme: String?)
data class Metric(val name: String, val value: Double, val atMs: Long, val channelId: Long? = null, val streamId: Long? = null)
data class MetricsSummary(val windowMs: Long, val p50: Map<String, Double>, val p95: Map<String, Double>, val counts: Map<String, Int>)
data class DiagnosticPackage(val path: String, val sizeBytes: Long, val createdAtMs: Long)
```

> **`SourceKind` 与「本地文件」来源口径（卡 `DOC-5`，对齐 P2-6 本地导入实现，`f9574ba`）**：
> - `SourceKind` 描述**源怎么送达**：`M3U` / `TXT` 是当前解析器**唯一产出**的两种（按正文自动识别，不看扩展名）；`XTREAM` / `JSON` 是预留的订阅/接口源；**`LOCAL_FILE` = 来自设备本地文件的源**（用户在应用内选中的清单），是冻结枚举里的**预留值**。
> - **来源标识用 `channel.source_id`**：内置夹具 = **`p1-2-fixture`**（`AssetBundledPlaylist.DEFAULT_SOURCE_ID`）；本地导入的清单 = **`local:<文件名>`**（`LocalPlaylistImport.sourceIdFor`，如 `local:mylist.m3u`）。导入是**整体替换**当前频道表，因此任一时刻 `source_id` 要么是夹具、要么是一个 `local:` 导入，二者不会同时在表里。
> - **实现现状**：P2-6 最小可用尚未把 `SourceKind` 写进任何表/配置（频道表只有 `source_id` 文本列），`LOCAL_FILE` 目前只用于**语义表达与未来的 `SourceConfig.kind`**，解析器不产 `LOCAL_FILE`；订阅 URL 管理、SAF/持久化 URI 属 P2-6 正篇。

> **`ScoreInput` 评分主体改 `Stream`（卡 `DOC-7`，对齐 P2-4b 实现，`f7a8bbe`）**：上文 `ScoreInput` 的首字段由 `entry: RawEntry` 改为 **`stream: Stream`**——P2-1 落库后，被评分的对象是**已持久化的流**（有 `id`、健康度、探针字段），`RawEntry` 没有这些，做不了评分主体；其余字段名/语义照旧。`videoCodec`/`audioCodec`/`width`/`height` 是**本次探测的读数**（可能比行内已存值更新），`stability` 是 0–1 健康因子（见 §4.3/§6.1，已含源端起播耗时扣分）。

> **`DeviceProfile` 收敛目标（卡 `DOC-7`，留待 dev 落地）**：单实例探测现分处两处——`:core:player` 的私有 `PlaybackSession.deviceProfile(context)`（§7.4）与 `:core:data` 的 `RefreshModule.provideDeviceProfile`（§6.1 设备兼容维度用；`:core:data` 不能依赖 `:core:player`，§3.2 规则 3）。两处取值相同、无同步机制，**目标 = 单一来源**：抽一个共享 `DeviceProfileProvider`（放 `:core:common` 或 `:core:platform` 级别的模块，两层都能依赖），由 Hilt 单例提供，`:core:player` 与 `:core:data` 都注入它，禁止各自 `Build`/`ActivityManager` 读取。**转实现任务（dev），本卡不改码。**

### 4.3 端口与策略（`:core:domain`，纯 Kotlin）

```kotlin
// ---- 仓储端口（由 :core:data 实现） ----
interface ChannelRepository {
    fun observe(filter: ChannelFilter): Flow<List<ChannelWithStreams>>
    suspend fun get(channelId: Long): ChannelWithStreams?
    suspend fun setFavorite(channelId: Long, favorite: Boolean)
    suspend fun setHidden(channelId: Long, hidden: Boolean)
    suspend fun reorder(channelId: Long, newIndex: Int)
    suspend fun setChannelNo(channelId: Long, channelNo: Int?)     // 见 01 F5：用户编辑优先
    suspend fun setEpgBinding(channelId: Long, epgChannelId: String?, match: EpgMatchType)
    suspend fun countByGroup(): Map<ChannelGroup, Int>
}
interface StreamRepository {
    suspend fun candidates(channelId: Long): List<Stream>            // 已按 score/priority 排序
    suspend fun upsertAll(streams: List<Stream>)
    suspend fun recordOutcome(streamId: Long, outcome: StreamOutcome)
    suspend fun health(streamId: Long): StreamHealth
    suspend fun startCostMedianMs(streamId: Long, windowMs: Long): Long?   // W-S1-2：play_history 起播耗时中位数
    suspend fun markStale(beforeMs: Long): Int
}
interface EpgRepository {
    fun observeWindow(query: EpgWindowQuery): Flow<List<Programme>>   // 上限：≤64 频道、≤24 h（§8.3）
    suspend fun nowNext(channelId: Long, atMs: Long): NowNext?
    suspend fun coverage(): EpgCoverage                               // 供诊断页
    suspend fun replaceAll(channelKey: String, programmes: List<Programme>)
    suspend fun prune(keepFromMs: Long, keepToMs: Long): Int
}
interface SourceRepository { suspend fun all(): List<SourceConfig>; suspend fun upsert(cfg: SourceConfig); suspend fun remove(id: String) }
interface SettingsRepository { val settings: Flow<AppSettings>; suspend fun update(transform: (AppSettings) -> AppSettings) }
interface MetricsRepository { suspend fun record(m: Metric); suspend fun summary(windowMs: Long): MetricsSummary }

// ---- 纯策略（零 Android，100% 可单测） ----
interface StreamSelector { fun rank(input: SelectionInput): List<Stream> }        // 主源在前，备胎按 score 降序
interface Scorer { fun score(input: ScoreInput): ScoreBreakdown }
interface ScoringRule { val id: String; val weight: Int; fun evaluate(input: ScoreInput): Int }      // 0..weight

data class SelectionInput(val channel: Channel, val candidates: List<Stream>, val nowMs: Long,
                          val engineCaps: Set<EngineCapability>, val device: DeviceProfile,
                          val health: Map<Long, StreamHealth>)
// 排序键（冻结，保证同分稳定）：score desc → priority asc → lastOkAtMs desc(null 最后) → id asc

data class StallSignal(val stalledMs: Long, val positionMs: Long, val bufferedPositionMs: Long)
data class FailoverInput(val channelId: Long, val attempt: Int, val activeStreamId: Long?,
                         val candidates: List<Stream>, val failure: AppError?, val stall: StallSignal?,
                         val health: Map<Long, StreamHealth>, val nowMs: Long, val limits: FailoverLimits)
data class FailoverLimits(val maxRetrySame: Int = 1, val maxSwitchPerSession: Int = 3,
                          val stallThresholdMs: Long = 8_000, val backoffMs: Long = 1_000,
                          val allowFreshResolve: Boolean = true)
sealed interface FailoverAction {
    data class RetrySame(val delayMs: Long) : FailoverAction
    data class SwitchTo(val stream: Stream, val reason: AppError) : FailoverAction
    data class ResolveFresh(val reason: AppError) : FailoverAction      // 无备胎：对该频道按需复检一次
    data class Backoff(val delayMs: Long) : FailoverAction
    data object GiveUp : FailoverAction
}
interface FailoverPolicy { fun decide(input: FailoverInput): FailoverAction }

// ---- UseCase（每个用例一个类，可组合） ----
data class AppSettings(
    val logLevel: LogLevel, val fileLogging: Boolean, val memoryRingSize: Int,
    val anrWatchdog: Boolean, val perfSampling: Boolean, val refreshHour: Int,
    val refreshWifiOnly: Boolean, val aspectRatio: AspectRatioMode, val preferPassthrough: Boolean,
    val bufferTargetMs: Long, val overscanPercent: Float,
)

class RefreshSourcesUseCase(/* ports 构造注入 */)      { operator fun invoke(opts: RefreshOptions): Flow<RefreshProgress> }
class WatchChannelUseCase(/* PlaybackController */)     { operator fun invoke(channelId: Long): Flow<PlaybackUiState> }
class LoadEpgUseCase(/* EpgProvider 集合 + EpgRepository */) { suspend operator fun invoke(force: Boolean): AppResult<EpgLoadReport> }
class SearchChannelsUseCase(/* ChannelRepository + EpgRepository */) { suspend operator fun invoke(q: String): List<SearchHit> }
class ExportDiagnosticsUseCase(/* Logger/Repositories/Redactor */)   { suspend operator fun invoke(): AppResult<DiagnosticPackage> }
```

**播放控制端口（domain 定义，实现见 §7.2）**——业务状态的唯一入口：

```kotlin
sealed interface PlaybackCommand {
    data class Watch(val channelId: Long, val streamId: Long? = null) : PlaybackCommand
    data object Play : PlaybackCommand
    data object Pause : PlaybackCommand
    data object Stop : PlaybackCommand
    data class SelectAudioTrack(val trackId: String?) : PlaybackCommand
    data class SetAspectRatio(val mode: AspectRatioMode) : PlaybackCommand
}
interface PlaybackController {
    val state: StateFlow<PlaybackUiState>
    suspend fun send(command: PlaybackCommand)      // 串行处理；调用方可随时取消
    fun attachSurface(surface: Surface?, owner: LifecycleOwner)   // 由 :feature:player 调用
}
```

### 4.4 平台引擎接口

```kotlin
// :core:source
interface SourceProvider {                       // 扩展点 E1
    val id: String; val label: String; val kind: SourceKind      // M3U / TXT / XTREAM / JSON / LOCAL_FILE
    suspend fun fetch(clock: Clock): AppResult<List<RawEntry>>
}
interface StreamValidator {                      // 扩展点 E2（校验链）
    val id: String; val order: Int; val stage: ValidationStage   // 顺序执行，短路
    suspend fun validate(target: StreamTarget, ctx: ProbeContext): ValidationResult
}
data class PipelineLimits(                       // 并发/超时上限：唯一定义处（§4.5 C3）
    val fetchConcurrency: Int = 4, val fetchTimeoutMs: Long = 15_000, val maxBytes: Long = 8L * 1024 * 1024,
    val shallowConcurrency: Int = 12, val shallowTimeoutMs: Long = 6_000,
    val deepConcurrency: Int = 6, val deepTimeoutMs: Long = 12_000,
    val shallowFirstBytes: Long = 4L * 1024,      // 浅校验 HEAD 被拒时的回退读取上限（§6.1 边沿分片）
    val deepFirstBytes: Long = 256L * 1024,       // 深探每腿读取上限（清单/变体/分片）
    val capCandidates: Int = 3_000, val batchSize: Int = 500,
)

// :core:epg
interface EpgProvider { val id: String; val label: String; suspend fun fetch(): AppResult<XmltvStream> }   // E4
interface ChannelMatcher { val id: String; val order: Int; fun match(c: Channel, epg: EpgChannelIndex): EpgMatchType? }  // E5

// :core:player
interface PlayerEngine {                         // 扩展点 E3
    val id: String
    val capabilities: Set<EngineCapability>
    val state: StateFlow<EngineState>
    val events: SharedFlow<PlaybackEvent>
    fun attach(surface: Surface?)                 // 只由 PlaybackController 在 engine 调度器上调用
    suspend fun prepare(request: PlaybackRequest): AppResult<PreparedMedia>
    fun play(); fun pause(); fun stop(); fun release()
    fun setAspectRatio(mode: AspectRatioMode)
    fun audioTracks(): List<AudioTrackInfo>
    fun selectAudioTrack(id: String?): Boolean
}
interface PlayerEngineFactory {
    val id: String
    val caps: Set<EngineCapability>
    fun create(dispatcher: CoroutineDispatcher): PlayerEngine
}
interface EngineSelector { fun select(required: Set<EngineCapability>, device: DeviceProfile): PlayerEngineFactory }  // §7.1

// :core:log
interface LogSink { fun write(event: LogEvent); fun flush(); val id: String }     // 扩展点 E8
```

> **`deepFirstBytes` 语义（卡 `DOC-7`，对齐 P2-4b，`f7a8bbe`）**：深探（`DEEP`）**只取每腿的首片字节**——`GET` 清单 → 首个变体 → **直播边沿分片（末片，失败再试首片）**，读到分片 `2xx` + 有 body 即通过，**不做长播放、不建渲染管线、不等首帧**（P2-4b 界内要求）。因此 `deepFirstBytes = 256 KiB` 是「一份清单或一段分片前缀」的上限，远小于真实播放缓冲；拿到的是**清单声明的** `vcodec/acodec/分辨率`（除非绑定了 Media3 `TrackProbe` 真解码，`:core:source` 侧接缝，未接线时标 `codecSource=declared`）。同一语义对直连 TS/裸流：`GET` 到字节即「能拉到」，同样不播。

### 4.5 并发与线程契约（冻结）

| # | 规则 |
|---|---|
| C1 | **状态单一写者**：`PlaybackController` 是 `PlaybackUiState` 的唯一写者；`PlayerEngine` 只暴露 `EngineState` 与 `PlaybackEvent`，UI **不得**订阅引擎。`EngineState → PlaybackPhase` 映射：IDLE→IDLE、PREPARING→PREPARING、READY/BUFFERING→BUFFERING、PLAYING→PLAYING、ENDED/RELEASED→STOPPED、ERROR→ERROR；发生切换决策时置 `FAILOVER`（覆盖映射） |
| C2 | **引擎线程**：一个进程内 **1 个** `PlayerEngine` 实例；`attach/prepare/play/pause/stop/release/setAspectRatio` 全部在 `DispatcherProvider.engine`（单并行度）上串行执行；`prepare` 由 `Mutex` 互斥——新请求到达时**取消旧请求并等待其清理完成**，再执行新请求 |
| C3 | **流水线并发**：并发上限与超时只在 `PipelineLimits` 定义，各阶段用 `Semaphore` 背压；播放会话进行中且 `respectPlayback=true` 时，Fetch/Shallow/Deep 并发减半 |
| C4 | **数据库**：只在 `:core:data` 调用；批量写按 `PipelineLimits.batchSize=500` 行/事务；`Metric` 写入与节目表修剪不得与播放路径共享线程 |
| C5 | **取消语义**：所有长任务响应 `CancellationException` 并重抛；播放会话取消 = `stop()` + 释放引擎 + 结束前台服务 |
| C6 | **作用域**：只用注入的 `AppScopeProvider.appScope` / 会话级 `CoroutineScope`；禁止 `GlobalScope`、禁止 `runBlocking`（测试除外） |

### 4.6 错误模型（冻结：现象 → 分类 → 策略动作）

| 现象 | `FailureClass` | `retryable` | `FailoverPolicy` 动作（§6.2 生效） |
|---|---|---|---|
| HTTP 403 / 404 / 410 | `HTTP_CLIENT` | false | `SwitchTo(next)` + 该流永久降权 |
| HTTP 429 / 5xx | `HTTP_SERVER` | true | `Backoff(1s)` → `RetrySame(≤1)` → `SwitchTo(next)` |
| 连接/读取超时 | `TIMEOUT` | true | `RetrySame(≤1)` → `SwitchTo(next)` |
| DNS 失败 / 网络不可达 | `NET_UNREACHABLE` | true | `SwitchTo(next)` 优先换源 |
| TLS 握手失败 | `TLS` | false | `SwitchTo(next)` |
| 清单/EPG 解析失败 | `PARSE` | false | 播放：`SwitchTo`；刷新：该源标记失败（不阻塞其他源） |
| 解码器不支持该编码 | `DECODE_UNSUPPORTED` | false | `SwitchTo(next)` + 该 codec 组合记负权 |
| 解码中断/花屏（流损坏） | `DECODE_CORRUPT` | true | `RetrySame(≤1)` → `SwitchTo(next)` |
| 清单为空 / 无分片 | `EMPTY_MEDIA` | false | `SwitchTo(next)` |
| 频道所有候选均失效 | `PLAYLIST_GONE` | false | `ResolveFresh`（单频道按需复检）→ 仍失败则 `GiveUp` + UI「该频道暂时不可用」 |
| 设备不支持（无解码器/无音轨） | `NO_CAPABILITY` | false | 先关透传重试一次 → `SwitchTo(next)` |
| 数据库/存储写入失败 | `STORAGE` | true | **不切换**：内存态继续可用，报错降级，下次启动重试 |
| SAF/存储权限缺失 | `PERMISSION` | false | 引导用户授权，不重试播放 |
| 协程取消 | `CANCELLED` | false | 不视为失败，不发 `PLAY_FAILOVER` |
| 其他 | `UNKNOWN` | false | 记 `ERROR` 事件 + `SwitchTo(next)` |

**看门狗（停滞）不是 `AppError`**：`StallSignal` 由 §6.2 看门狗直接产出 → `Backoff(1s)` → `SwitchTo(next)`。

### 4.7 冻结项与 Spike 的挂钩（P0 出口回写点）

| Spike（`04` §2.2） | 判据 | 影响的冻结项 | 未出结论前的默认行为 |
|---|---|---|---|
| **S1** Media3 能力 | 成功率 ≥90%、平均起播 ≤3 s，**以 Media3 复测为口径**（`dsh` 的 MediaPlayer 结果仅作对照） | §7.1 引擎选型、`DeviceProfile` 的 codec/分辨率上限、§6.1 设备兼容分权重 | 默认 Media3 为主引擎 |
| **S2** 音频 | 音频×编码矩阵 {AAC / MP2 / AC3} × {透传 / 软解} 均出声；AC3/EAC3 占比需一并统计 | §7.6 透传判定与降级顺序、`:core:player-vlc` 是否引入（规则：AC3/EAC3 占比 ≥5% 才引入） | 默认 `preferPassthrough=true`，失败降级关透传 |
| **S3** EPG 网格 | 658 台 × 6 h 滚动 ≥50 fps（**仅原型**，产品级网格在 P3-1） | §8.4 网格实现路线、`EpgWindowQuery.limit` | 默认自绘 + 二维虚拟化 |
| **S4** 构建链 | 空工程 → 签名 APK → 装机启动全通 | §14 构建约定、`minSdk/targetSdk` | 用 Android Studio 向导版本组合 |
| **S5** 换台 | 30 次连续换台平均 ≤3 s、无内存增长；**按「有备胎/无备胎」分组统计** | §4.3 `StreamSelector` 排序键、§7.5 复用策略、`FailoverLimits` 阈值 | 默认引擎单实例复用 + `stop()+prepare()` |
| **S6** 冷启动 | 点击图标到可看 ≤5 s，release 构建 5 次取 max | §8.5 启动期预加载范围 | 默认只预加载频道表 + 上次频道元数据 |

---

## 5. 数据模型与数据库

### 5.1 表结构（Room，schema 导出到 `schemas/` 以支持迁移测试）

```sql
CREATE TABLE channel (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL, name_key TEXT NOT NULL,          -- 归一化名，用于去重/匹配
  tvg_id TEXT, group_key TEXT NOT NULL, group_title TEXT,
  logo TEXT, channel_no INTEGER,
  favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL DEFAULT 0,
  epg_channel_id TEXT, epg_match TEXT NOT NULL DEFAULT 'NONE',
  created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL
);
CREATE INDEX idx_channel_group ON channel(group_key, sort_order);
-- W4：同名台可跨分组共存（不同源、内容不同），唯一性按 (name_key, group_key)
CREATE UNIQUE INDEX idx_channel_namekey ON channel(name_key, group_key);

CREATE TABLE stream (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  channel_id INTEGER NOT NULL REFERENCES channel(id) ON DELETE CASCADE,
  url TEXT NOT NULL, url_hash TEXT NOT NULL,
  user_agent TEXT, referrer TEXT, source_id TEXT NOT NULL,
  quality TEXT, vcodec TEXT, acodec TEXT, width INTEGER, height INTEGER,
  score INTEGER NOT NULL DEFAULT 0, priority INTEGER NOT NULL DEFAULT 0,
  last_ok_at INTEGER, last_check_at INTEGER, fail_count INTEGER NOT NULL DEFAULT 0,
  last_error TEXT, disabled INTEGER NOT NULL DEFAULT 0
);
-- W4：同一 URL 可服务多频道（同源多台），唯一性按 (channel_id, url_hash)
CREATE UNIQUE INDEX idx_stream_hash ON stream(channel_id, url_hash);
CREATE INDEX idx_stream_channel ON stream(channel_id, score DESC, priority ASC);

CREATE TABLE programme (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  epg_channel_id TEXT NOT NULL, start_ms INTEGER NOT NULL, stop_ms INTEGER NOT NULL,
  title TEXT NOT NULL, desc TEXT, category TEXT
);
CREATE INDEX idx_prog_lookup ON programme(epg_channel_id, start_ms);
-- W4：EPG 重复拉取必须幂等 —— 唯一键 + INSERT OR REPLACE；窗口修剪按 epg_channel_id 批量删除
CREATE UNIQUE INDEX idx_prog_slot ON programme(epg_channel_id, start_ms);

CREATE TABLE epg_source (id TEXT PRIMARY KEY, label TEXT, url TEXT, enabled INTEGER, last_fetch_at INTEGER, last_result TEXT);
CREATE TABLE source (id TEXT PRIMARY KEY, provider_id TEXT, label TEXT, url TEXT, kind TEXT,
                     enabled INTEGER, last_fetch_at INTEGER, last_result TEXT, entry_count INTEGER);
CREATE TABLE play_history (id INTEGER PRIMARY KEY AUTOINCREMENT, channel_id INTEGER, stream_id INTEGER,
                     started_at INTEGER, start_cost_ms INTEGER, result TEXT, failover_count INTEGER);
CREATE TABLE metric (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, value REAL,
                     channel_id INTEGER, stream_id INTEGER, at INTEGER);
```

**数据量估算**：频道 ≈ 1k、流 ≈ 5k、节目（7 天窗）≈ 10–30 万 → SQLite 完全胜任；节目表按 `[now-6h, now+48h]` 滚动窗口修剪，避免无限增长。

> **索引与幂等（W4，冻结）**：`stream` 唯一键为 `UNIQUE(channel_id, url_hash)`；`channel` 唯一键为 `UNIQUE(name_key, group_key)`；`programme` 唯一键为 `UNIQUE(epg_channel_id, start_ms)` 且写入用 `INSERT OR REPLACE`，窗口修剪按 `epg_channel_id` 批量删除。原始唯一索引会挡住"同一 URL 服务多频道"（同源多台）与"跨分组同名台"两类真实数据。落库实现由 dev-A 在 P0-3 执行。

> **`group_key` 推导（god 决策 2026-09-22，卡 `DOC-3`）**：`group_key` = **分组标题的归一化键**——取 M3U 的 `group-title`（缺省时用 `#EXTGRP`，TXT 取第三列，仍缺则视为无分组），做归一键（全角→半角、去空白、小写，算法同 `name_key`）；**缺失时回落 `other`**。`group_title` 另存**原始标题**用于显示，两者不同职责。
> **理由**：`group_key` 参与 `UNIQUE(name_key, group_key)`，必须是"这份播放列表里的分组身份"。若用固定的 5 个枚举值（如 `新闻/体育/…`）当键，**跨分组同名台会被错误合并成一条**（正是 W4 要避免的那类真实数据）。键用归一化标题后，同一列表内分组名相同即同组、不同即不同组，行为可预测。

> **频道级「不可用」的口径（卡 `DOC-7`，P2-4b 结论 `f7a8bbe`）——本期不加数据列**：`channel` 表**不**加可用性列。当前口径 = **「该频道全部流 `disabled`」即该频道不可用**，并配 `SRC_SELECT.unavailable` / `SRC_REFRESH_DONE.unavailable` 事件字段（§11「频道标记 ⚠️，仍可尝试播放」是 UI 表达）。
> **理由**：① 可用性是**派生态**，权威数据在 `stream.disabled`，另存一列就有了两个写者，刷新/导入/播放各写一处必然漂移（正是 §4.5 C1 要避免的）；② 频道保留可见（不删行）与「无可用流」是两件事，后者用一次 `EXISTS(stream WHERE channel_id=? AND disabled=0)` 即可判定，UI 派生成本可忽略；③ `stream` 行的 `disabled` 已是甄选（§6.1 Select 阶段）的落地点，不新增第二处状态。
> **若将来列表渲染 ⚠️ 有性能问题**（千级频道逐行 `EXISTS` 太慢），再加**只读的 `channel.available` 生成列或视图**，由该 `EXISTS` 派生，仍不允许独立写入——**改动须走「arch 提案 → god 批准 → 版本递增」**。当前 P2-4b 实现与本口径一致，无需改码。

### 5.2 DTO / Entity / Domain 三层映射

```
network DTO ──map──▶ Room Entity ──map──▶ Domain Model ──▶ UiState
(JSON/M3U)          (持久化/索引)        (纯 Kotlin)      (不可变)
```

映射函数集中在 `:core:data/mapper/`，**禁止**让 Room Entity 或 DTO 泄漏到 feature 层。

---

## 6. 关键流程设计

### 6.1 源刷新流水线（F1/F2，**全程本机**）

```
 Fetch ─▶ Parse ─▶ Normalize ─▶ Dedupe ─▶ Cap ─▶ Shallow ─▶ Deep ─▶ Score ─▶ Select ─▶ Persist
  (N源)   (M3U/TXT)  (URL/名称)  (url_hash) (4000) (可达性)  (可解码) (100分) (主+备)  (Room)
```

| 阶段 | 并发 | 超时 | 说明 | 事件码 |
|---|---|---|---|---|
| Fetch | 4 | 15 s/源，8 MB | 并行拉取；UTF-8 → GB18030 回退；失败不阻塞其他源 | `SRC_FETCH_OK/FAIL` |
| Parse | 串行 | – | M3U（`#EXTINF`/`#EXTVLCOPT`）与 TXT（`#genre#`、`名,url`）双格式；**解析器绝不因未知行/未知属性中断整份清单** | `SRC_PARSE_OK/FAIL` |
| Normalize | 并行 | – | URL 规范化（scheme/host 小写、去默认端口、去 fragment）；名称全角→半角/去空白 | – |
| Dedupe | 串行 | – | 按 `url_hash` 去重；**按源交错**保留多样性 | `SRC_DEDUPE` |
| Shallow | 12 | 6 s | 清单可达 + master 变体（前 4）+ **直播边沿分片**（末片→首片） | `VAL_SHALLOW_*` |
| Deep | 6 | 12 s | Media3 `Probe` 解出真实 vcodec/acodec/分辨率 | `VAL_DEEP_*` |
| Score | 串行 | – | 六维加权（和 97）→ 归一化 0–100（下表） | `SRC_SCORE` |
| Select | 串行 | – | 每频道：最高分为主、次高分（≥55）为备胎，最多 3 条 | `SRC_SELECT` |
| Persist | 串行 | – | Room 事务批量 upsert；保留历史健康度 | `DB_UPSERT` |

> 上表的并发/超时数值即 §4.4 `PipelineLimits` 的默认值（唯一定义处），改动只改 `PipelineLimits`。

> **解析容错与 `#EXTVLCOPT` 采集口径（god 批准 2026-09-22，卡 `DOC-3`）**：解析器**容忍 `#EXTVLCOPT` 的任意键**——不认识的键直接忽略，既不报错也不中断整份清单（`#EXTVLCOPT` 是 VLC 的私有扩展，键集合没有标准，未知键必须是无害的）；其中 **`http-user-agent` / `http-referrer` 采集进 `RawEntry`**，落库对应 `stream.user_agent` / `stream.referrer`，供防盗链源在起播时带上正确请求头（评分模型的「防盗链」维度也依赖这两列）。除此之外不做任何头部改写。

**评分模型（六维加权和 = 97 → 归一化到 0–100，可插拔 `ScoringRule`）**

| 维度 | 权重 | 规则 |
|---|---|---|
| 可用性 | 35 | 判定可播放即得 |
| 稳定性 | 25 | 按轮询失败率线性扣；媒体序号不前进再扣 10 |
| 设备兼容 | 20 | H.264=20；H.265=12；AV1=4；**未知=12（中性）**；AC3/EAC3 −5；>1080p −3 |
| 画质/码率 | 10 | 1080p=10；720p=8；480p=5 |
| 防盗链 | 5 | 无需特殊头=5；需要则 3（并记录 UA/Referer） |
| 分片成功 | 2 | 边沿分片 2xx 加分 |

> **权重和 97 与归一化（卡 `DOC-7`，P2-4b 口径，`f7a8bbe`；依据 §8.4 E6「总分归一化到 100」）**：上表六维逐项相加 = 35+25+20+10+5+2 = **97，不是 100**。**权重不改**，按 §8.4 E6 把加权原始和**线性缩放到 0–100**：`total = round(raw × 100 / 97)`；`ScoreBreakdown.byRule` **保留各维原始分**（`SRC_SCORE` 逐维度上报的就是原始分，不是归一化后）。因此 §6.1「Select」行与本节所有阈值（如备胎 `≥55`）**是在归一化后的 0–100 尺度上**判定的。
> **未知编码取中性 12（脚注，P2-4b 决定）**：「设备兼容」维度上表只列了 H.264/H.265/AV1 三档，**探不出编码**（`SRC_SCORE.vcodec=null`）时取**中性 12**——给 0 会因缺元数据误杀、给 20 会白送满分，取中间值且可从事件字段看出原因。该默认值随 S1/S2 复测结论调整（同「设备兼容分」条）。

> **设备兼容分（W6，冻结）**：该维度分数由 `DeviceProfile` + S1/S2 实测结论计算，扣分阈值（H.265/AC3/>1080p 等）随实测调整，**禁止硬编码机型字符串**（如 `MT5895`）；S1/S2 出结论前用上表默认值。

> **源端起播耗时（W-S1-2，S1 实测 2026-09-21）**：S1 的起播耗时呈**双峰**——p50 ≈1.2 s，但 5 条境外/远端源落在 3.7–6.2 s，**尾部完全由源端/网络决定**（同一条源在 S5 复现同样慢，与设备/引擎无关）。评分须把**实测起播耗时**作为一个扣分因子：对历史 `PLAY_FIRST_FRAME.costMs` 中位数 >3 s 的源降权（先并入「稳定性」维度扣分，P1 全量复测后再定是否单列权重与分值），**禁止硬编码源名/地区**。
> **该因子的输入端口（卡 `DOC-7`，留待 dev 接线）**：起播中位数从 `play_history.start_cost_ms` 读，端口为 `StreamRepository.startCostMedianMs(streamId, windowMs)`（§4.3）。P2-4b 已实现扣分**纯函数**（`:core:domain` `Stability`，`>3 s` 起线性扣、`≥6 s` 扣满 0.20），但端口未接线，管线暂传 `null`（尾部扣分 0）；接线属实现任务（dev），不影响 0–1 因子的其余项。

**增量与可恢复（关键设计）**

| 规则 | 值（可配置） | 说明 |
|---|---|---|
| 健康流 TTL | 24 h | 未过期则跳过复检 |
| 失败流 TTL | 6 h | 失败源更快重试 |
| 全量复扫 | 7 d | 兜底发现新增/变更 |
| 时间预算 | 45 min/次 | 截止时间驱动准入；超时优雅收尾并保存进度 |
| 断点续跑 | 每条结果即时落库 | 进程被杀后重入自动跳过已完成项 |
| **播放避让** | 播放中并发减半或暂停 | 不与播放抢带宽/CPU |
| 充电/网络约束 | WorkManager | 定时任务默认"非计量网络 + 充电（可选）" |

**预算与准入（冻结规则）**：默认 `RefreshOptions.budgetMs = 45 min`，候选上限 `capCandidates = 3000`；**每个阶段开始前检查剩余预算，若 `remaining < 阶段 p50 耗时` 则不再接纳新条目**，按 §6.1 的"即时应答落库"直接收尾并置 `RefreshInterruption(BUDGET_EXCEEDED)`。
（原因：原「20 min + 4000 候选 + 浅校验 12 并发 × 6 s」算术上不可达——冷启动全量约 33 min 就超预算。数值已由 god 批准定稿，见下。）
> **定稿（W3，2026-09-21 god 批准）**：预算 **45 min**、候选上限 **3000**、截止时间驱动准入——即上段数值为最终值；后续调整须走「arch 提案 → god 批准 → 本文档版本 v1.x 递增」。

### 6.2 播放与故障转移状态机（F3/F4）

```
        ┌──────────┐  prepare()   ┌───────────┐  first frame  ┌──────────┐
        │  IDLE    │─────────────▶│ PREPARING │──────────────▶│ PLAYING  │
        └──────────┘              └─────┬─────┘               └────┬─────┘
             ▲                          │ error/timeout            │ stall(N s)
             │ release()                ▼                          ▼
        ┌────┴─────┐   give up     ┌───────────┐  switch stream  ┌──────────┐
        │ RELEASED │◀──────────────│ FAILOVER  │────────────────▶│ BUFFERING│
        └──────────┘               └───────────┘                 └──────────┘
```

`FailoverPolicy`（纯 Kotlin）决策输入 → 输出：

| 错误类别 | 判定 | 动作 |
|---|---|---|
| 4xx（403/404/410） | 源失效 | 立即 `SwitchTo(next)`，永久降权 |
| 超时 / 连接重置 | 瞬时 | `RetrySame`（≤1 次）→ 失败则 `SwitchTo(next)` |
| 解码失败 / 不支持编码 | 设备不兼容 | `SwitchTo(next)`，并标记该 codec 组合负面 |
| 缓冲停滞 ≥ N 秒 | 卡顿 | `Backoff(1s)` → `SwitchTo(next)` |
| 候选耗尽 | – | `GiveUp` + 提示"该频道暂时不可用" |

> 上表与 §4.6 的分类映射等值；`FailoverLimits` 阈值默认值见 §4.3。**无备胎频道（`streams.size == 1`）**：`RetrySame(≤maxRetrySame)` → `ResolveFresh`（该频道按需复检一次）→ `GiveUp`。实测基线 417/579 频道只有一个流，此路径是常态而非例外。

**看门狗**：独立协程，采样 `playbackPosition / bufferedPosition / isLoading`，静止超过阈值即触发上层决策；**换台时复用同一个 `PlayerEngine` 实例**（`stop() + prepare()`），降低起播耗时。

> **状态机归属（冻结，优先于上图）**：上图是**引擎侧**状态；业务态（含故障转移过程）由 **`PlaybackController` 唯一维护**（§4.5 C1）。引擎不自行切源——它只上报 `PlaybackEvent.Error/Stalled`，由控制器调 `FailoverPolicy` 决策，再下发新 `PlaybackRequest`。图中的 `RELEASED` 是引擎状态；UI 侧 `PlaybackPhase.RELEASED` 仅在播放会话结束时出现。

### 6.3 EPG 流程（F6）

```
EpgProvider.fetch() ─▶ 流式 Pull 解析（XmlPullParser） ─▶ 时间窗过滤 ─▶ ChannelMatcher 链 ─▶ Room 事务写入 ─▶ now/next 查询
     (可 gzip)            (不整文件入内存)              [now-6h,+48h]    id→name→fuzzy→alias→manual
```

- **匹配链可插拔**（E5）：① `tvg-id` 精确 → ② 归一化名称精确 → ③ 前缀/包含模糊 → ④ 人工别名表 → ⑤ 用户手动绑定（存 `epg_match='MANUAL'`，永不被覆盖）。
- **覆盖率可观测**：`EpgCoverage(matched, total, byGroup)` 写入诊断面板；P3 目标 ≥60%。
- **失败降级**：EPG 拉取失败保留旧数据，UI 只显示频道名，不阻塞播放。

---

## 7. 播放引擎设计（`:core:player`）

### 7.1 引擎与选型（冻结规则）

| 项 | 设计 |
|---|---|
| 主引擎 | **Media3 ExoPlayer 1.11.1**（`media3-exoplayer` + `-hls` + `-ui` + `-session` + 可选 `-exoplayer-rtsp`） |
| 抽象 | `PlayerEngine` 接口 + `PlayerEngineFactory` + `EngineSelector`；引擎实现互不感知 |
| 选型 | `EngineSelector.select(required, device)` 按 `capabilities ⊇ required` 选第一个可用工厂；默认仅注册 Media3 |
| 兜底引擎 | `:core:player-vlc`（libVLC 3.7.6，仅 `armeabi-v7a`，LGPL）→ **引入判定（冻结）**：S2 显示 AC3/EAC3 占比 ≥5% 且无法透传时才引入；否则不打包（当前基线 AC3 仅 2/741 ≈ 0.3%） |
| 不可用处理 | `EngineSelector` 找不到满足能力的引擎 → `AppError.capability(...)`（`NO_CAPABILITY`），按 §4.6 降级顺序处理，不静默降级画质 |

> **依据（S1 实测，2026-09-21，`05-过程记录/07-Spike报告` §2）**：Media3 在本机 **20/20 起播**（成功率 100% ≥ 判据 90%）、平均 **2.14 s**（≤ 判据 3 s）；对照集是同一批 MediaPlayer 深探阳性样本，故 `Media3 − MediaPlayer = 0` 个百分点（小样本，只能说明无大规模回归）。据此**维持 Media3 为主引擎**；全量复测作为 P1 入口（见 §7.8）。

### 7.2 生命周期与归属（冻结）

| # | 规则 |
|---|---|
| P1 | 进程内 **1 个** 引擎实例，由 `:app` 的 `PlaybackService`（前台服务）持有；会话范围 = `Watch` 到 `Stop`/退出应用 |
| P2 | 引擎引用由 `:core:player` 的 `PlaybackSession`（机制层：持有唯一实例 + `engine` 调度器 + 窄钩子）与 `:feature:player` 的 `PlaybackFailoverCoordinator`（协调层：**唯一**调用引擎所有方法的角色）共同承担，且只在 `DispatcherProvider.engine` 上调用（§4.5 C2；落位口径见本表后 note） |
| P3 | 进入/离开播放页 **不创建/释放引擎**：`PlayerActivity` 只 `attachSurface(surface, owner)` / `attachSurface(null, owner)`；`null` = 停渲染不停音频 |
| P4 | 前台服务在会话期间常驻（防冻结）；会话结束才 `stop()` + `release()` + 停服务 |
| P5 | `release()` 之后引擎不可复用；控制器必须重新 `create()`（保证状态机干净） |

> **落位口径（god 裁决 2026-09-22，卡 `DOC-6`；替代本表 P2 原先「实现位于 `:core:player`」的写法）**：`PlaybackController` 是 §4.3 的 domain 端口，仍是业务态的唯一入口；其**实现职责拆成两层** ——
> - **机制层** `:core:player`：`PlaybackSession`（唯一引擎实例 + `engine` 调度器 + 窄钩子 `sample()` / 呈现回调）。
> - **协调层** `:feature:player`：`PlaybackFailoverCoordinator`（会话循环，以及 §7.3 播放事件码的唯一发出者）。
>
> 协调器放 `:feature:player` 的原因：`:core:player` 被 §3.2 矩阵禁止直接声明 `:core:domain`，而协调器必须同时看见引擎钩子与 `FailoverPolicy`；`:feature:player` 是唯一同时满足矩阵与 JVM 单测的层。**下一位开发者不要照旧文档把协调器写进 `:core:player`。**

### 7.3 起播契约（冻结：S1/S5/S6 的指标口径来源）

| 项 | 规则 |
|---|---|
| 入口 | `suspend fun prepare(request: PlaybackRequest): AppResult<PreparedMedia>`；`request.timeoutMs` 默认 12 s，超时返回 `AppError.timeout(...)` |
| 互斥 | 同一时刻只允许一个进行中的 `prepare`（`Mutex`）；新请求 = 取消旧请求 → 等旧请求清理完成 → 起新请求 |
| 可取消 | `prepare` 必须响应取消；被取消的请求返回 `CancellationException`（不记 `PLAY_PREPARE_FAIL`） |
| **起播开始** | `playbackPrepareStartUptimeMs` = 控制器下发 `prepare` 的系统 uptime |
| **首帧** | 引擎 `onRenderedFirstFrame` → 发 `PlaybackEvent.FirstFrame(costMs)`；`costMs = firstFrameUptimeMs − prepareStartUptimeMs` |
| **埋点归属** | `PLAY_PREPARE_START` / `PLAY_FIRST_FRAME` 由 `PlaybackController` **唯一发射**（引擎只发事件，不写日志），`sessionId` 取自 `SessionIdFactory.newId("play")` |
| 失败 | 失败必须带 `AppError.failure`（§4.6 映射表），并附 `attempt`（从 1 起） |

### 7.4 Surface 与显示（冻结）

| 项 | 设计 |
|---|---|
| Surface 归属 | `PlayerActivity` 持有 `SurfaceView`（独立于信息条 View 层，`SurfaceView` 在底层、信息条与遮罩在其上），把 `surface` 交给控制器 `attachSurface`；引擎**不**创建 View |
| 显示模式 | `AspectRatioMode` 四档（FIT/FILL/ZOOM/FIXED_4_3）由引擎适配到 `AspectRatioFrameLayout`；过扫描微调（`AppSettings.overscanPercent`）作用于外层容器，不进引擎 |
| 切换时序 | 换台 = 保持 Surface 不变，仅 `prepare` 新流；旧流须显式 `stop()` 后再 `prepare`，禁止两个 `MediaItem` 并存渲染 |
| 音频 | 引擎 `capabilities` 必须先经 `AudioCapabilities` + `AudioManager.getDevices()` 探测后发布；`preferPassthrough=true` 时优先透传，失败按 §7.6 降级 |

### 7.5 复用与调优默认值（冻结；S1/S5 出结论后按 §4.7 回写）

| 项 | 默认值 | 说明 |
|---|---|---|
| 实例复用 | 单实例 `stop()+prepare()` | S5 验证复用是否真的更快 |
| 起播缓冲 | `minBufferMs=1500`、`maxBufferMs=5000`、`bufferForPlaybackMs=800` | 追起播速度优先 |
| 直播边沿 | `targetOffsetMs=3000`、`minPlaybackSpeed=0.97`、`maxPlaybackSpeed=1.03` | 追边沿、避免超追 |
| 卡顿阈值 | `stallThresholdMs=8000`（§4.3 `FailoverLimits`） | 看门狗采样 1 s 一次 |
| 重试 | `LoadErrorHandlingPolicy` 仅在 `retryable=true` 时重试；重试次数由 `FailoverLimits.maxRetrySame` 统一控制 | 与 §4.6 一致，避免双重重试 |
| 缓存 | `SimpleCache` **默认关闭**（P1 前不引入） | 直播收益低 |
| 系统集成 | `MediaSession`（media3-session）+ 音频焦点 + `setWakeMode` + 屏幕常亮 | 遥控播放键走 MediaSession |

> **依据（S5 实测，2026-09-21）**：复用单实例 30 次：平均 **946 ms**、PSS **+3.9 MB**；重建实例 10 次：平均 765 ms、PSS **+12.5 MB**（**~1.25 MB/次**），建实例仅 ~4 ms。**重建的代价是内存而非速度**——所以「单实例复用」的价值是内存与状态可控，**不是更快**。本节默认值维持不变。

### 7.6 音频能力与降级顺序（冻结；S2 判定）

```
探测设备能力（AudioCapabilities + 已连接设备）
  → 优先直通（AC3/EAC3/MP2 若设备声明支持）
  → 失败/无声：关闭直通重试同一流（preferPassthrough=false）
  → 仍失败：SwitchTo(备胎)（按 §4.6 的 NO_CAPABILITY）
  → 无备胎：ResolveFresh → GiveUp + 明确提示"该频道音频不兼容"
```

- **术语（S2 实测修订）**：原「软解」措辞改为「**引擎/设备解码为 PCM**」。本机实测（S2）显示 AC3/EAC3 走的是**压缩码流直通 HAL**（设备侧解码），AAC/MP2 才解成 PCM 走混音线程——两种配置下音频路径一致，故不应笼统叫「软解」。
- **新增观测项「码流直通 HAL」**：真机上「引擎解码 vs 直通」必须可区分，判据是 `dumpsys media.audio_flinger` 中本进程的 `AUDIO_FORMAT_AC3` / `AUDIO_FORMAT_E_AC3` DIRECT 线程活动与 `underruns`（写入 `PLAY_FIRST_FRAME` / 诊断日志），否则日志无法判定实际走了哪条路径。
- **MP2 的 mime**：Media3 1.6 上 MP2 的 mime 是 **`audio/mpeg-L2`**（不是 `audio/mpeg`）；能力探测须以 `MediaCodecList` 的实际类型为准，禁止按 `audio/mpeg` 误判「不支持」。

> 依据：基线 741 条流中 AAC 695、**MP2 30**、AC3 2、缺元数据 14。MP2 与"无元数据"两类必须在 S2 全覆盖，不能只测 AC3。
> **S2 结论（2026-09-21）**：AAC / MP2 / AC3 / EAC3 四类在本机**全部可解码播出**（AC3/EAC3 走设备直通，AAC/MP2 走 PCM 路径），本节降级顺序维持不变，**无需引入 libVLC**（见 §7.8）。
> **限制（如实记录）**：S2 的「出声」是**路径级证据**（音轨选中 + 播放位置前进 + HAL 活动输出 + `underruns` 0），**不是听感证据**（本轮无人耳/无麦克风）；**未做 HDMI ARC/AVR 透传实测**（本机只挂 `builtin_speaker`）；AC3/EAC3 用的是**裸 ES** 公开测试向量（经本机 HTTP 分发），**非 HLS/TS 直播封装**，即「直播边沿 + AC3 直通」这条路径**未验证**。

### 7.7 与日志/诊断的接口

| 事件 | 触发者 | 关键字段 |
|---|---|---|
| `PLAY_PREPARE_START` | 控制器 | channelId、streamId、engine、attempt、sessionId |
| `PLAY_FIRST_FRAME` | 控制器（来自 `PlaybackEvent.FirstFrame`） | **costMs**、vcodec、acodec、w/h |
| `PLAY_PREPARE_FAIL` | 控制器 | failure(FailureClass)、httpStatus、attempt |
| `PLAY_FAILOVER` | 控制器（来自 `FailoverPolicy` 决策） | from→to、reason、attempt |
| `PLAY_STALL` | 控制器（来自 `PlaybackEvent.Stalled`） | stalledMs、positionMs |
| `PLAY_ENGINE_INIT/RELEASE` | 控制器 | engine、capabilities |

### 7.8 P0 Spike 结论（2026-09-21 定稿，依据 `05-过程记录/07-Spike报告`）

P0 的 S1/S2/S5/S6 全部通过，本节原「待定项」逐条落为结论：

| 原待定项 | 由谁定 | 结论（2026-09-21） | 依据 |
|---|---|---|---|
| Media3 是否作主引擎（成功率/起播） | S1 | **维持 Media3 为主引擎** | 20/20 起播（100% ≥90%）、平均 2.14 s；对照集 MediaPlayer 亦 100%，差值 0（小样本）|
| 是否需要 libVLC 兜底模块 | S2 | **不打包 libVLC**（维持） | AC3/EAC3 占比 **0.27%**（2/741，且这 2 条已源失效）≪ 5% 阈值，且本机**可解码、可透传**——两个引入条件都不满足；S2 音频矩阵全绿（AAC/MP2/AC3/EAC3 均出声路径 OK）|
| 引擎复用是否优于重建实例 | S5 | **维持单实例 `stop()+prepare()`** | 复用 30 次平均 946 ms、PSS +3.9 MB；重建平均 765 ms、PSS +12.5 MB（~1.25 MB/次）——代价在内存而非速度（§7.5）|
| 启动期预加载范围 | S6 | **维持「只预加载频道表 + 上次频道元数据」** | 夹具级冷启动到可看 1.39–1.55 s（≤5 s）；夹具不含频道表/Room，属**下限**（§8.6）|
| `DeviceProfile` 的 codec/分辨率上限 | S1/S2 | 维持 H.264 ≤1080p 为默认最优档 | 实测可播含 HEVC 1080p/2160p、MPEG-2、MP2；上限随 P1 全量复测校准 |
| **「20 条样本」升级为全量复测** | 新增（W-S1-3） | **P1 入口条件 = 用 Media3 全量复测 658 条** | 20 条样本无法暴露细小差异（对照集本身是 MediaPlayer 阳性集）；全量复测用于补齐 `Media3 − MediaPlayer` 差值 |

---

## 8. UI 架构（Views + Leanback）

### 8.1 导航结构

```
WizardActivity（仅首次）
      │
      ├─▶ BrowseActivity ──┬─▶ ChannelListFragment（分组 Tab + 列表）
      │                    ├─▶ EpgGridFragment（时间网格）
      │                    └─▶ SettingsActivity ─▶ DiagnosticsFragment / LogViewFragment
      └─▶ PlayerActivity（全屏）
```

- **Activity 边界**：播放独立 Activity（生命周期清晰、避免与浏览页互相拖累）。
- **单向数据流**：`ViewModel` 暴露 `StateFlow<UiState>`，View 只渲染 + 发事件；`UiState` 为不可变 data class。
- **本地导入入口（卡 `DOC-5`，P2-6 最小可用，`f9574ba`）**：频道表页（`BrowseActivity`）顶部工具区新增「**导入播放列表**」按钮（`@id/import_playlist`，位于 `browse_header` 与频道列表之间；列表首行按 `DPAD_UP` 可达）。层级 = **浏览页 → 文件选择对话框（`AlertDialog`，列出投放目录里的候选清单）→ 选中某项即原地导入并刷新列表**；**不新增 Activity、不改返回栈**。返回键 = **关对话框、留在浏览页**（导入成功后同样不新开页面）；投放目录为空时对话框只显示目录路径与「关闭」按钮。

**导航契约（冻结）**

| 项 | 约定 |
|---|---|
| 播放入参 | `PlayerActivity` 只认 `PlayerContract` 三个 extra：`EXTRA_CHANNEL_ID: Long`（必填）、`EXTRA_STREAM_ID: Long?`（可选，指定起始流）、`EXTRA_AUTOPLAY: Boolean`（默认 true） |
| 播放返回 | `setResult(RESULT_OK, Intent().putExtra(EXTRA_RESULT_CHANNEL_ID, channelId))`；浏览页收到后**按 channelId 恢复焦点**（找不到则回到分组首项） |
| 浏览页状态 | 分组 Tab 索引、滚动位置、焦点位置在 `onSaveInstanceState` 内保存；进程重建后必须恢复 |
| 向导退栈 | 向导完成 → 启动 `BrowseActivity` 并 `FLAG_ACTIVITY_CLEAR_TASK or FLAG_ACTIVITY_NEW_TASK`（返回键不再回到向导） |
| 返回键层级 | 播放页 → 浏览页；浏览页 → 系统（App 不从浏览页直接退出播放会话，播放由控制器停止）；设置/诊断 → 浏览页；对话框 → 关对话框 |
| 事件通道 | ViewModel 暴露 `StateFlow<UiState>`（状态）+ `Channel<UiEvent>`（一次性事件，buffer = 8，`DROP_OLDEST`）；View 不得直接调 Repository/引擎 |
| 导入播放列表入口 | 频道表页（`BrowseActivity`）的「导入播放列表」按钮 → `AlertDialog` 选择设备投放目录里的本地清单；选中后**整体替换**当前频道表并原地刷新（不新增 Activity/层级，返回键只关对话框）。详见上方「本地导入入口」，实现见 `docs/05-过程记录/19-本地导入验证.md` |

> **真机/QA 验证入口（卡 `DOC-6`，与 `TESTABLE-1` 一致，替代早期做法）**：真机验证与 QA 冒烟**用浏览页的本地播放列表导入入口**（`LocalPlaylistImport`：把 M3U/TXT 投放到 `getExternalFilesDir(null)/playlists` 再导入，整体替换当前频道表），**不再靠临时替换内置 asset 重打包**。P1-4/P1-5 早期验证记录里的 asset 替换只在 `00-索引与模板.md` 第 19 行留档，作历史追溯用；新卡一律走导入入口。

### 8.2 焦点与遥控（TV 核心）

| 关注点 | 做法 |
|---|---|
| 焦点可见性 | 焦点态用**缩放 + 描边 + 高亮**三重反馈；禁用系统默认高亮（`defaultFocusHighlightEnabled=false`）后自绘 |
| 焦点路由 | 显式 `nextFocusUp/Down/Left/Right`；列表用 `LinearLayoutManager` + `focusSearch` 兜底 |
| 按键映射 | **播放页（冻结；卡 `DOC-6` 对齐 P1-5 现状）**：`KEYCODE_DPAD_UP/DOWN`（含 `KEYCODE_CHANNEL_UP/DOWN`）=**换台**（同分组、按浏览列表顺序；**不做焦点移动**）；`KEYCODE_0..9`=**跳台**（冻结的 **2 s 输入窗口**，支持多位频道号，输入中在信息条回显）；`KEYCODE_DPAD_LEFT/RIGHT`=进入信息条的「画幅」与失败卡片的「重试」（上下键已让给换台）；`KEYCODE_MEDIA_PLAY/PAUSE`=播放控制；`KEYCODE_BACK`=**两级**（信息条可见 → 先收起信息条；再按 → 回浏览页）。浏览页/网格页键位见下方焦点路由表 |
| 过扫描安全区 | 5% 边距（`dimens` 统一），所有可点元素落在安全区内 |
| 10-foot 规范 | 正文 ≥ 18sp、标题 ≥ 28sp、焦点态最小 48dp 命中区 |
| Leanback 使用 | `BrowseSupportFragment`/`ListRow` 用于分组与卡片；列表虚拟化依赖 `RecyclerView`（658+ 台必须虚拟化） |

- **数字键跳台**：缓冲窗口冻结为 **2 s**；频道号优先级冻结为「用户编辑 > 源 `tvg-chno` > 自动编号」（`01` F5 附注）。
- **焦点路由表**：播放页的上下键**不**做焦点移动（直接换台，见上表按键映射），「画幅」与「重试」改由**左右键**到达（`PlayerActivity.renderOverlay` 按相位重设左右目标，避免失败卡片出现后的焦点死区）——**P1-4 的上下焦点布线已被 P1-5 按本条冻结条款替换**；浏览页左右键切分组 Tab、上下键在列表内移动；网格页上下键跨行取同列节目，左右键按时间步进。

### 8.3 EPG 时间网格（自绘，性能关键）

- **自定义 `View` + `Canvas`**：二维虚拟化，**只绘制可视矩形内的节目块**；
- 横向 = 时间轴（`OverScroller` 惯性滚动），纵向 = 频道（与左侧频道栏联动）；
- 当前时间红线；节目块点击 → 详情/切台；`StaticLayout` 缓存文本布局；避免滚动中触发布局；
- 调试期用 `FrameMetricsAggregator` 采集帧耗时，写入 `metric` 表并在诊断页展示。

> **二维虚拟化是硬性要求（S3 实测，2026-09-21）**：自绘 + 二维虚拟化 **60 fps**（p95 16.72 ms，30 s 内 0 掉帧）；**不做虚拟化**（每帧遍历全部 658 行）只有 **4.3 fps**（p95 266 ms，帧耗时是前者的 ~14 倍）。因此「只绘可视矩形」是冻结契约的**硬性项**，任何实现都不得退化为全量遍历。

**数据与性能契约（冻结）**

| 项 | 约定 |
|---|---|
| 数据来源 | 网格**只能**经 `EpgRepository.observeWindow(EpgWindowQuery)`（§4.3）；禁止 UI 直接查 Room |
| 窗口上限 | 单次 ≤64 频道、窗口 ≤24 h（`EpgWindowQuery.limit`）；翻页用游标，不一次性加载 658 台 × 多日 |
| 绘制 | 二维虚拟化，只绘可视矩形；`StaticLayout` 结果按 (text,width,style) 缓存，缓存上限 512 条 |
| 埋点 | `PERF_EPG_GRID` 事件（fps、**drawnBlocks＝每帧绘制块数**、**layoutCacheHit＝文本布局缓存命中率**、scrollMs）在 debug 全量、release 采样（默认 10% 会话）；`drawnBlocks` 应稳定在可视块数量级（实测 ≈119 块/帧），若逼近总块数（≈5922）即为虚拟化失效 |

### 8.4 性能预算

| 指标 | 目标 |
|---|---|
| 频道列表滚动 | ≥ 50 fps |
| EPG 网格滚动 | ≥ 50 fps |
| 冷启动到可看 | ≤ 5 s |
| 换台（同引擎复用） | 平均 ≤ 3 s |
| 播放内存增量 | ≤ 150 MB |

### 8.5 测量口径（冻结；S3/S6 按此复现）

| 指标 | 口径 |
|---|---|
| 帧率 | **release 构建**，连续滚动 30 s，`Choreographer` 采样帧间隔，报 p50/p95；**达标线 = 无掉帧（60 Hz 输出下 p95 ≤ 17 ms，即 ≥50 fps 的等效均值）**——原「p95 ≤ 16.7 ms」是与 60 Hz vsync 死磕，实测正常值就落在 16.72 ms（S3），故给 0.3 ms 容差 |
| 冷启动 | release 构建，杀进程后冷启动 **5 次取最大值**；起点 = `Process.getStartUptimeMillis()`，终点 = 首帧渲染完成（`PLAY_FIRST_FRAME`） |
| 换台 | release 构建，连续换台 30 次，报平均与 p95；按「有备胎 / 无备胎」分组 |
| 内存 | 播放 10 min 后 `Debug.MemoryInfo` 采样，对比进入播放页前基线 |
| 记录 | 每次测量写 `PERF_*` 事件 + `metric` 表，结论回写本文档 §7/§8 与 `04` 的 Spike 判据 |

### 8.6 P0 Spike 结论（2026-09-21 定稿，依据 `05-过程记录/07-Spike报告`）

| 原待定项 | 由谁定 | 结论（2026-09-21） | 依据 |
|---|---|---|---|
| 自绘网格方案是否成立 | S3 | **成立（自绘 + 二维虚拟化）**，虚拟化为硬性要求（§8.3） | 60 fps vs 4.3 fps |
| 列表/网格达标档位（是否降级为「列表 + 精简网格」） | S3 + 真机 | **不降级** | 原型 60 fps，对达标线（§8.5）有余量 |
| 启动期预加载到哪一步（频道表 / 台标 / EPG now-next） | S6 | **维持「频道表 + 上次频道元数据」** | 冷启动到可看 1.39–1.55 s |
| 过扫描微调是否需要进设置首屏 | S3 真机观察 | 放设置「显示」二级页（不变） | S3 未纳入该观测，维持原判 |

> **限制（如实记录）**：S6 是**夹具级下限**——夹具首页不解析 658 条频道表、不初始化 Room、不取台标，因此 1.39–1.55 s 只是绘制路径的下限，**真实产品冷启动须在 P1 带频道表/Room 后复测**；S3 只覆盖绘制与滚动，**不覆盖数据加载**（真实 EPG 分页、`StaticLayout` 命中分布未测）。

---

## 9. 扩展点清单

| # | 扩展点 | 新增步骤 |
|---|---|---|
| E1 | **新聚合源** | 实现 `SourceProvider` → Hilt `@IntoSet` 注册 → 设置页可见（无需改流水线） |
| E2 | **新校验器** | 实现 `StreamValidator`（指定 `order`）→ `@IntoSet` 注册 → 自动进入校验链 |
| E3 | **新播放引擎** | 实现 `PlayerEngine` + `PlayerEngineFactory` → 按 `EngineCapability` 选择；上层无感 |
| E4 | **新 EPG 源** | 实现 `EpgProvider` → `@IntoSet` 注册 |
| E5 | **新匹配策略** | 实现 `ChannelMatcher`（指定 `order`）→ 插入匹配链 |
| E6 | **新评分维度** | 实现 `ScoringRule`（`weight`）→ 自动纳入总分（总分归一化到 100） |
| E7 | **新选源策略** | 替换 `StreamSelector` 实现 |
| E8 | **新日志出口** | 实现 `LogSink` → `@IntoSet` 注册（如上传、屏幕叠加 HUD） |
| E9 | **新故障策略** | 替换 `FailoverPolicy` 实现 |
| E10 | **新界面语言** | `values-zh-rCN` / `values` 资源，无代码改动 |

> 所有扩展点均为 **Hilt 多绑定（`@IntoSet` / `@IntoMap`）**，新增实现不改动既有代码（开闭原则）。

---

## 10. 并发与线程模型

| 场景 | 调度器 | 并发上限 | 说明 |
|---|---|---|---|
| UI | `Dispatchers.Main.immediate` | – | 只做渲染与事件分发 |
| 网络（源/EPG） | `Dispatchers.IO` | Fetch 4 / Shallow 12 | `Semaphore` 背压 |
| 深度探测（Media3） | `Dispatchers.IO` + 专用单线程池 | 6 | Media3 实例较重，限制总量 |
| 解析（M3U/XMLTV） | `Dispatchers.Default` | CPU 核数 | 大文件流式，避免整文件入内存 |
| 数据库 | Room 自带执行器 | – | 批量事务写入 |
| 播放 | **单线程 confined dispatcher** | 1 | 引擎状态机串行化，避免竞态 |
| 看门狗 | `Dispatchers.Default` | 1 | 定时采样，超阈值发事件 |

**规则**：所有 `Dispatcher` 通过 `DispatcherProvider` 注入（可测）；长任务必须响应 `CancellationException`；不允许在 `GlobalScope` 起协程。
本表的并发上限与超时数值由 §4.4 `PipelineLimits` 唯一定义；播放线程、状态写者、取消语义以 §4.5 的冻结契约 C1–C6 为准（本节不重复定义）。

> **指标写入（W5，冻结）**：`metric` 表的**唯一写者**是 `MetricsRepository`（`:core:data`）；`MetricBridge`（`:core:log`）只把白名单事件转成 `Metric` 交给端口，**不直接依赖 Room**（Room 绑定留在 `:core:data`，与 §3.2 矩阵一致）。

---

## 11. 错误处理与降级

| 层 | 责任 |
|---|---|
| platform | 把技术异常转成 `AppError`（带事件码 + `FailureClass`，§4.1），不抛给上层 |
| domain | 决策（重试/切换/放弃），不关心具体实现 |
| data | 事务一致性；写失败不破坏已有数据 |
| presentation | 把 `AppError.code` 映射为用户可读文案（映射表随 `values/strings.xml`）；**绝不**直接显示堆栈 |

> 分类与策略动作的唯一出处是 §4.6 的映射表；本节只描述各层职责，不另立规则。

**降级矩阵**

| 故障 | 降级行为 |
|---|---|
| EPG 不可用 | 隐藏节目信息，只显示频道名；播放不受影响 |
| 源刷新失败 | 保留上次数据，提示"使用缓存数据" |
| 某源全部候选不可用 | 频道标记 ⚠️，仍可尝试播放（可能临时恢复） |
| 播放引擎不支持该流 | 切备胎；若配置了 libVLC 则切换引擎 |
| 存储写入失败 | 内存态继续可用，下次启动重试 |

---

## 12. 安全与合规

- `usesCleartextTraffic="true"`（IPTV 源大量明文 HTTP）；
- **不内置源地址**；仅内置"源清单地址"，且可在设置中关闭；
- **日志脱敏**：URL 查询串中的 `token`/`key`/`pwd`/`sign` 等字段一律替换为 `***`（见 03 文档 §11）；
- 密钥口令不入库（ADR-003）；仓库公开，文档不含序列号/MAC/内网 IP；
- 若引入 libVLC（LGPL-2.1）：独立模块 + 动态链接 + 保留声明；**不引入 GPL/AGPL 代码**。

---

## 13. 测试策略

| 层级 | 范围 | 工具 |
|---|---|---|
| 单元测试（主力） | 解析器（M3U/TXT/XMLTV）、URL 归一化、去重、评分规则、选源、故障策略、匹配链、映射函数 | JUnit5 + kotlin.test + Turbine（Flow） |
| 夹具驱动 | 用 `tools/fixtures/` 的真实样本回归（含畸形/编码异常样本） | 解析器参数化测试 |
| 集成测试 | Room DAO（内存库）、迁移测试（`MigrationTestHelper`）、刷新流水线端到端（FakeHttp） | AndroidX Test |
| UI 冒烟 | 播放 Activity 起播、焦点导航路径、诊断导出 | Espresso（TV）+ adb 脚本 |
| 真机验收 | 见 [04 文档](./04-开发计划与验收.md) 的阶段指标 | adb + 人工 |

**覆盖率要求**：`:core:domain` 与解析/评分逻辑 **≥ 80%**；其他模块不强制但有下限 50%。

---

## 14. 构建与工程约定

| 项 | 约定 |
|---|---|
| 构建 | Gradle + AGP（由 Android Studio 2025.3.4 向导生成版本组合为准），Kotlin DSL |
| 版本管理 | `gradle/libs.versions.toml`（版本目录），禁止散落硬编码版本 |
| 约定插件 | `build-logic/` included build：`iptv.android.library` / `iptv.android.feature` / `iptv.kotlin.library` / `iptv.hilt` / `iptv.room` / **`iptv.module.dependencies`（依赖矩阵守护，任务 `verifyModuleDependencies`，挂 `check`）** |
| JDK | `org.gradle.java.home` 指向 Android Studio JBR（本机无系统 Java）；**非 macOS 机器必须用 `-Dorg.gradle.java.home=…` 覆盖**——命令行 `-D` 优先于本文件（CR-12 实测） |
| namespace | Android 模块 `namespace = "ilab.iptv.player" + 模块路径`（`:`→`.`、`-`→`_`，如 `:core:database` → `ilab.iptv.player.core.database`）；**改目录名会静默改 namespace**，重命名模块须同步（CR-08） |
| SDK | `compileSdk 36` / `targetSdk 35` / `minSdk 21`；`local.properties` 写 `sdk.dir`（gitignore） |
| ABI | 无 native 时不需要；引入 libVLC 时 `abiFilters += "armeabi-v7a"` |
| DI | **Hilt**（KSP）；`@IntoSet` 承载全部扩展点 |
| DB | Room + `exportSchema = true`，`schemas/` 入库 |
| 签名 | `keystore/release.jks` 入仓；口令走 `local.properties`（不入仓）或 CI Secrets |
| 静态检查 | Android Lint（`abortOnError=true`）+ ktlint（可选）+ `dependency-analysis`（依赖规则守护） |
| CI | GitHub Actions：`assembleDebug` + `testDebugUnitTest` + `lint`；Release 需 Secrets |

---

## 15. 目标目录结构

```
iptv-player/
├── README.md · LICENSE · .gitignore
├── settings.gradle.kts · build.gradle.kts · gradle.properties
├── build-logic/                      # 约定插件（included build）
├── gradle/libs.versions.toml         # 版本目录
├── app/                              # 组装 + 前台服务 + 导航
├── feature/
│   ├── channels/  player/  epg/  settings/  wizard/
├── core/
│   ├── common/  model/  domain/      # 纯 Kotlin JVM
│   ├── log/  database/  network/  source/  epg/  player/  data/  ui/  design/  testing/
├── keystore/
│   ├── release.jks                   # 入仓
│   └── README.md                     # 说明口令来源（口令不入仓）
├── tools/
│   ├── dev-pipeline/                 # 可选：Mac 侧 Python 管线（非运行时依赖）
│   └── fixtures/                     # 测试夹具（m3u / guide.xml 样本）
├── docs/
│   ├── 01-需求与可行性.md  02-技术架构设计.md  03-日志与诊断设计.md  04-开发计划与验收.md
│   └── reference/                    # 背景资料（已脱敏）
└── .github/workflows/ci.yml
```
