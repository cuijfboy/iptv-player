<?xml version="1.0" encoding="utf-8"?>
<!--
  One settings row (docs/04 P2-8 item 1). Focusable on its own so the remote walks the page with
  UP/DOWN and the default focus search finds each row; the title carries the 10-foot size, the summary
  the value the row currently holds (docs/02 §8.2).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="24dp"
    android:layout_marginVertical="4dp"
    android:background="@drawable/settings_row_background"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <TextView
        android:id="@+id/settings_row_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#FFF2F2F7"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/settings_row_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="#FF9A9AA5"
        android:textSize="14sp" />

</LinearLayout>
